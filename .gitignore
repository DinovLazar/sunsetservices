# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example
!.env.local.example

# vercel
.vercel

# sanity studio (Phase 2.03)
/dist/
/.sanity/

# typescript
*.tsbuildinfo
next-env.d.ts

# claude code local config
/.claude/

# lighthouse audit outputs (Phase 2.08 convention — committed scores live in completion reports)
/lighthouse/

# Phase B.04 — validation harness temp artifacts (the committed copy of the
# report lives at src/_project-state/Phase-B-04-Validation-Report.md)
/scripts/.schema-validation-report.json
/scripts/.schema-validation-cache.json

# Phase B.05 — SEO validation harness temp artifacts (the committed copy
# of the table lives at src/_project-state/Phase-B-05-Completion.md)
/scripts/.seo-validation-report.json
/scripts/.seo-validation-cache.json

# Phase B.06 — a11y validation harness temp artifacts (the committed copy
# of the findings table lives at src/_project-state/Phase-B-06-Completion.md)
/scripts/.a11y-validation-report.json
/scripts/.a11y-validation-cache.json
/test-results/
/playwright-report/
/.playwright/

# Phase B.11 — photo-upload harness temp artifact (committed summary
# lives at src/_project-state/Phase-B-11-Completion.md)
/scripts/.photo-upload-validation-report.json

# Phase M.11b — link-integrity validation harness temp artifacts (committed
# summary lives at src/_project-state/Phase-M-11b-Completion.md)
/scripts/.links-validation-report.json
/scripts/.links-validation-cache.json

# Phase M.11c — mobile validation harness temp artifacts (committed summary
# lives at src/_project-state/Phase-M-11c-Completion.md)
/scripts/.mobile-validation-report.json
/scripts/.mobile-validation-cache.json
/scripts/.mobile-screenshots/

# Phase M.02 — Lighthouse harness temp artifact (committed snapshot of the
# table lives at src/_project-state/Phase-M-02-Completion.md)
/scripts/.lighthouse-report.json
