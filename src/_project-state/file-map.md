# File Map

> One line per non-trivial file or directory. Keep it skim-able. Updated end of every phase.

## Project root

- `Sunset-Services-Plan.md` — master site plan and spec (canonical).
- `docs/design-handovers/Part-1-Phase-{03,05,06,08,11,13}-Design-Handover.md` — Claude Design handovers, one per design phase. Phase 1.11 covers About + Contact (input for Phase 1.12); Phase 1.13 covers Service Areas index + 6 location pages (input for Phase 1.14).
- `package.json` — npm manifest, pinned dependencies.
- `tsconfig.json` — TypeScript strict config, `@/*` → `src/*` alias.
- `next.config.ts` — Next.js config wrapped in `createNextIntlPlugin`.
- `postcss.config.mjs` — PostCSS config for Tailwind v4.
- `eslint.config.mjs` — ESLint flat config (Next + TS).
- `.gitignore` — standard Next gitignore + `.env*`.
- `.env.local.example` — placeholder env vars for future phases.
- `next-env.d.ts` — Next.js TypeScript ambient types (auto-generated).
- `README.md` — generated by create-next-app (replace later).
- `AGENTS.md` / `CLAUDE.md` — pointers + Next 16 caveat.

## docs/

- `docs/design-handovers/` — design `.md` handovers from Claude Design (one per phase).
- `docs/runbooks/` — operational runbooks (Sanity workflow, deploy steps, etc.).
- `docs/decisions/` — append-only decision log.

## reports/

- `reports/` — phase completion reports written by Claude Code.

## public/

- `public/images/` — brand SVGs and static assets.
- `public/og/` — generated OG images (Phase 2.x).

## src/

- `src/proxy.ts` — Next 16 proxy (formerly middleware), runs next-intl locale routing.
- `src/i18n/routing.ts` — next-intl routing config (locales, defaultLocale, localePrefix).
- `src/i18n/request.ts` — server-side request config (loads messages per locale).
- `src/i18n/navigation.ts` — locale-aware `<Link>`, `useRouter`, etc.
- `src/messages/en.json` — English UI strings; top-level keys: `home.*` + `audience.*` (Phase 1.09) + `servicePage.*` (Phase 1.09) + `chrome.*` + `about.*` (1.12) + `contact.*` (1.12) + `serviceAreas.*` (1.14) + `location.*` (1.14) + `projects.*` (1.16) + `project.*` (1.16). Per-service strings live in `src/data/services.ts`; per-city strings live in `src/data/locations.ts`; per-project strings live in `src/data/projects.ts`.
- `src/messages/es.json` — Spanish UI strings; mirror of `en.json`. `about.*` and `contact.*` ship as first-pass Spanish drafts; `serviceAreas.*`, `location.*`, `projects.*` and `project.*` carry `[TBR]` markers on prose strings pending Phase 2.13 native review.
- `src/app/globals.css` — Tailwind v4 entry; full `@theme` token block (added `--color-sunset-amber-200`) + `@layer base` + `@layer components`. Added `[data-audience]` custom-property block (Phase 1.09 §3X.1) plus the Phase 1.07 `.btn-ghost.btn-on-dark` modifier.
- `src/app/favicon.ico` — placeholder favicon.
- `src/app/[locale]/layout.tsx` — locale-aware root layout. Mounts fonts, `NextIntlClientProvider`, `<MotionRoot>`, chrome (`<SkipLink>`, `<Navbar>`, `<main id="main">`, `<Footer>`) plus `LocalBusiness` JSON-LD `<script>` in `<head>`.
- `src/app/[locale]/page.tsx` — homepage. Composes the seven `Home*` sections, locale-aware metadata, `WebSite` JSON-LD.
- `src/app/[locale]/[audience]/page.tsx` — audience-landing dynamic route. **Modified (1.10)**: tile-photo lookup now uses `s.imageKey ?? s.slug` so commercial snow-removal resolves to its own placeholder; removed unused `BUSINESS_URL` import.
- `src/app/[locale]/[audience]/[service]/page.tsx` — service-detail dynamic route. **Modified (1.10)**: lookup uses audience-aware `getService(service, audience)`; hero asset lookup uses `svc.imageKey ?? svc.slug`; related-services loop uses new `getRelatedService(slug, audience)` helper that prefers same-audience match (so commercial/landscape-maintenance's `related: ['snow-removal']` resolves to commercial/snow-removal, not residential).
- `src/app/[locale]/about/page.tsx` — **NEW (1.12)** server. Composes 6 About sections + reuses `HomeProjects` for the projects teaser (handover §3.6). Emits `Person` × 3 + `BreadcrumbList` JSON-LD. Locale-aware metadata.
- `src/app/[locale]/contact/page.tsx` — **Modified (1.14)** server. Phase 1.12 base unchanged; the `<ContactServiceAreaStrip>` import was retargeted to the promoted `<ServiceAreaStrip>` at `src/components/sections/ServiceAreaStrip.tsx` (D7b promotion).
- `src/app/[locale]/service-areas/page.tsx` — **NEW (1.14)** server. Service Areas index. Composes ServiceAreasHero + CitiesGrid + OutsideAreaBand + ServiceAreasCTA. Emits `BreadcrumbList` + `ItemList` of 6 Place items, same-source with the visible breadcrumb + cities grid. Locale-aware metadata + hreflang.
- `src/app/[locale]/service-areas/[city]/page.tsx` — **NEW (1.14)** server, dynamic. Six city slugs × 2 locales = 12 routes. Composes the 9-section location template per Phase 1.13 §2.D10. Emits `BreadcrumbList` + `Place` + `ItemList` of 6 Service items + `FAQPage`, all same-source with the visible DOM. Locale-aware metadata + hreflang. Unknown slugs → `notFound()`.
- `src/app/[locale]/projects/page.tsx` — **NEW (1.16)** server (dynamic). Projects index. Reads `searchParams.audience` + `searchParams.page` (sanitized + clamped); renders ProjectsHero → FilterChipStrip → (ProjectsGrid + Pagination) OR EmptyState → shared `<CTA>`. Emits `BreadcrumbList` + `ItemList` of 12 `CreativeWork` JSON-LD. Self-canonical to the unfiltered/unpaginated route per handover §5.1. Canonical/hreflang use `process.env.NEXT_PUBLIC_SITE_URL || BUSINESS_URL` and drop the trailing slash (matches Next 16's served URL).
- `src/app/[locale]/projects/[slug]/page.tsx` — **NEW (1.16)** server (SSG). Project detail. `generateStaticParams` returns the 12 `projects.ts` slugs × 2 locales = 24 pre-rendered pages. Composes ProjectHero (with embedded breadcrumb) → ProjectNarrative → ProjectGallery (lightbox) → ProjectFacts → BeforeAfterToggle (when `hasBeforeAfter`) → RelatedProjects → shared `<CTA>` with `tokens={{city}}` and destination `/request-quote/?from=project&slug={slug}`. Same-source rules: breadcrumb items array drives both the visible Breadcrumb and `BreadcrumbList` JSON-LD; gallery image array drives both rendered `<img>` and `CreativeWork.image`. Unknown slugs → `notFound()`.
- `src/app/[locale]/dev/system/page.tsx` — dev-only design-system smoke test (Phase 1.04).
- `src/app/[locale]/dev/system/_client-demos.tsx` — client-only Dialog and Tooltip demos.
- `src/components/global/Logo.tsx` — server component, two skins (light/dark).
- `src/components/global/motion/easings.ts` — three named easings + four duration constants.
- `src/components/global/motion/variants.ts` — six `<AnimateIn>` variants.
- `src/components/global/motion/stagger.ts` — `staggerContainer` + `staggerItem` Framer-style variants.
- `src/components/global/motion/AnimateIn.tsx` — client, polymorphic, fires `whileInView` once.
- `src/components/global/motion/StaggerContainer.tsx` — client.
- `src/components/global/motion/StaggerItem.tsx` — client.
- `src/components/global/motion/MotionRoot.tsx` — client wrapper for `MotionConfig reducedMotion="user"`.
- `src/components/layout/Navbar.tsx` — server composer.
- `src/components/layout/NavbarScrollState.tsx` — client island. **Modified (1.09)**: pathname-based over-hero detection extended to recognize `/{audience}/` and `/{audience}/{service}/` routes (5-line addition).
- `src/components/layout/NavbarDesktop.tsx`, `NavbarMobile.tsx`, `NavbarLink.tsx`, `MegaPanelTrigger.tsx`, `ServicesMegaPanel.tsx`, `ResourcesMegaPanel.tsx`, `LanguageSwitcher.tsx`, `PhoneLink.tsx`, `SkipLink.tsx`, `SocialIcons.tsx` — Phase 1.05 chrome, unchanged.
- `src/components/layout/Footer.tsx` + `FooterBrand.tsx` + `FooterLinks.tsx` + `FooterServiceAreas.tsx` + `FooterNewsletter.tsx` + `FooterLegal.tsx` — Phase 1.05 footer, unchanged.
- `src/components/layout/icons/{GoogleBusinessProfileIcon,FacebookIcon,InstagramIcon,YoutubeIcon}.tsx` — hand-rolled brand glyphs, Phase 1.05.
- `src/components/sections/home/Home*.tsx` — homepage section components, Phase 1.07. **`HomeProjects.tsx` modified (1.16)**: now consumes the new `<ProjectCard>` primitive (extracted JSX); placeholder hrefs remapped from the 1.07 placeholders (`naperville-patio`, etc.) to real `projects.ts` seed slugs (`naperville-hilltop-terrace`, etc.). Tile titles still read the 1.07 `home.projects.tile.*` strings — copy alignment is Erick's Part-2 pass.
- `src/components/sections/audience/AudienceHero.tsx` — **NEW (1.09)** server. Full-bleed photo hero with kicker + H1 + subhead + CTA pair. No entrance animation (LCP discipline).
- `src/components/sections/audience/AudienceQualifier.tsx` — **NEW (1.09)** server. "Who this is for" eyebrow + H2 + body + 4 trust pills (badge-pill).
- `src/components/sections/audience/AudienceServicesGrid.tsx` — **NEW (1.09)** server. 4:3 photo card tiles (D2). 6 tiles for residential/hardscape, 4 for commercial.
- `src/components/sections/audience/AudienceFeaturedProjects.tsx` — **NEW (1.09)** server. 3 project tiles with View-all CTA-link top-right.
- `src/components/sections/audience/AudienceWhyUs.tsx` — **NEW (1.09)** server. 4 value-prop cards on cream surface, audience-accent applied to icon-tile bg.
- `src/components/sections/audience/AudienceUnilockBand.tsx` — **NEW (1.09)** server. Hardscape-only charcoal band per D6 + §3X.5. Hand-rolled Unilock badge placeholder.
- `src/components/sections/audience/AudienceSocialProof.tsx` — server. Testimonial cards + 1px hairline + credentials row. **Modified (1.10)**: replaced raw `"…"` quote characters with `&ldquo;…&rdquo;` to clear two react/no-unescaped-entities lint errors.
- `src/components/sections/audience/AudienceFAQ.tsx` — **NEW (1.09)** server. Wraps `<FaqAccordion>`. Per-item `<AnimateIn>` deliberately absent (handover §10).
- `src/components/sections/audience/AudienceCTA.tsx` — **NEW (1.09)** server. The page's only amber CTA in `<main>`. Cream surface for residential/commercial; charcoal for hardscape (D6).
- `src/components/sections/service/ServiceHero.tsx` — **NEW (1.09)** server. 52vh / 44vh hero with 3-level breadcrumb. Secondary CTA is `tel:+16309469321` (D10).
- `src/components/sections/service/ServiceWhatsIncluded.tsx` — **NEW (1.09)** server. Adaptive grid per D3: 1-col @ 3 items, 2-col @ 4, 3-col @ 5–6.
- `src/components/sections/service/ServiceProcess.tsx` — **NEW (1.09)** server. Numbered cards on dashed connecting hairline (D4); rotates to vertical timeline on mobile.
- `src/components/sections/service/ServiceWhyUs.tsx` — **NEW (1.09)** server. 3 cream-fill tiles on white surface.
- `src/components/sections/service/ServicePricing.tsx` — **NEW (1.09)** server. State A (price) and State B (explainer) — both occupy the same vertical footprint to keep alternation invariant. All 16 services in Part 1 ship with State B.
- `src/components/sections/service/ServiceFeaturedProjects.tsx` — **NEW (1.09)** server. 2–3 tiles, View-all CTA-link (404s in Part 1).
- `src/components/sections/service/ServiceFAQ.tsx` — **NEW (1.09)** server. Wraps `<FaqAccordion>`. Per-item `<AnimateIn>` deliberately absent.
- `src/components/sections/service/ServiceRelated.tsx` — server. 3–4 no-photo nav tiles. D7-driven from `services.ts` `related` array. **Modified (1.10)**: removed hard-coded `aria-label="Learn more about ${service.name[locale]}"` (English bleed-through to ES routes; the inner `<h3>` already serves as the link's accessible name natively).
- `src/components/sections/service/ServiceCTA.tsx` — **NEW (1.09)** server. The page's only amber CTA in `<main>`. Cream surface (no charcoal on service-detail per D6).
- `src/components/sections/about/AboutHero.tsx` — **NEW (1.12)** server. 50vh desktop / 40vh mobile photo-led hero with on-dark breadcrumb, eyebrow chip, H1, 2-line lead. NO entrance animation (LCP discipline). Hero photo IS the LCP candidate.
- `src/components/sections/about/AboutBrandStory.tsx` — **NEW (1.12)** server. Two-column desktop (image 4:5 + 3-paragraph copy) on `--color-bg-cream`. `<AnimateIn fade-left>` on image, `<AnimateIn fade-up>` on copy.
- `src/components/sections/about/AboutTeam.tsx` — **NEW (1.12)** server. 3 peer cards via `<TeamCard>` + `<StaggerContainer>`/`<StaggerItem>` with `auto-fit minmax(280px, 1fr)` grid.
- `src/components/sections/about/AboutCredentials.tsx` — **NEW (1.12)** server. 4-column row desktop / 2×2 mobile of `<CredentialBadge>` instances. SINGLE `<AnimateIn>` on the row (not staggered, per Phase 1.07 lesson).
- `src/components/sections/about/AboutCTA.tsx` — **NEW (1.12)** server. The page's only body amber CTA. Mirrors `HomeCTA` structure with `about.cta.*` namespace (D16 — duplicate component, not refactor; consolidate Part-2).
- `src/components/sections/contact/ContactHero.tsx` — **NEW (1.12)** server. Text-only (D7), no photo. Visible breadcrumb (light variant) + eyebrow + H1 + 2-line subhead. H1 is the LCP candidate.
- `src/components/sections/contact/ContactInfoForm.tsx` — **NEW (1.12)** server WRAPPER. Two-column section: left info block (`<dl>`/`<address>`), right form card with nested `<ContactForm/>` client island. Single section-level `<AnimateIn>` (never animate individual form fields).
- `src/components/sections/contact/ContactMapPlaceholder.tsx` — **NEW (1.12)** server. Inline-SVG abstract street grid + pin + address card overlay. Wrapping `<a>` opens Google Maps. ZERO third-party load (D8).
- `src/components/sections/contact/ContactCalendlyPlaceholder.tsx` — **NEW (1.12)** server. "Coming soon" amber-100 chip + decorative mock calendar grid + tel: fallback CTA (D9). NO embed in Part 1.
- ~~`src/components/sections/contact/ContactServiceAreaStrip.tsx`~~ — **Removed (1.14)**: promoted to `src/components/sections/ServiceAreaStrip.tsx` (Phase 1.13 D7b).
- `src/components/sections/ServiceAreaStrip.tsx` — **NEW (1.14)** server, promoted from `ContactServiceAreaStrip.tsx`. Centered eyebrow + city `<Link>`s separated by middle dots; reads `contact.area.*` strings. Accepts `excludeSlug?: string` (Phase 1.13 D7b) to hide the current city on location pages; default behaviour (no prop) renders all 6 cities for `/contact/`. All city links go to `/service-areas/<slug>/` (Phase 1.13 D1).
- `src/components/sections/CTA.tsx` — **Modified (1.16)** shared CTA. Phase 1.14 base unchanged (`copyNamespace` / `destination` / opt-in `tokens` / `surface` / `ariaId`). The amber `<Link>` now passes `prefetch={false}` so Lighthouse doesn't log the `/request-quote/` 404-by-design RSC prefetch as a Best-Practices error. Existing CTAs (`HomeCTA`, `AboutCTA`, `ServiceCTA`, `AudienceCTA`) intentionally not migrated.
- `src/components/sections/service-areas/ServiceAreasHero.tsx` — **NEW (1.14)** server. Split layout: copy 60% + ServiceAreaMap 40%. NO entrance animation (LCP discipline).
- `src/components/sections/service-areas/ServiceAreaMap.tsx` — **NEW (1.14)** server. Production SVG (Phase 1.13 §3.2): viewBox 600×500, 6 pins each as a `<Link>` from `@/i18n/navigation`, locale-aware `<title>` + `<desc>`, hover `translateY(-2px)` over `--motion-fast` with `prefers-reduced-motion` short-circuit. No JS, no third-party load.
- `src/components/sections/service-areas/CitiesGrid.tsx` — **NEW (1.14)** server. 3×2 desktop / 1-col mobile grid of `LocationCard`s. Single section-level `<AnimateIn>`.
- `src/components/sections/service-areas/OutsideAreaBand.tsx` — **NEW (1.14)** server. Two-column desktop / stacked mobile. Phone + email inline links; no CTA button.
- `src/components/sections/service-areas/ServiceAreasCTA.tsx` — **NEW (1.14)** server. Thin wrapper around shared `<CTA>` (no `tokens`). Cream surface.
- `src/components/sections/location/LocationHero.tsx` — **NEW (1.14)** server. Compact split: copy 60% / hero photo 40% (D5.B). Hero photo carries `priority` + `fetchPriority="high"` (LCP candidate). Microbar of 3 chips (years/projects/response). NO entrance animation.
- `src/components/sections/location/LocalTrustBand.tsx` — **NEW (1.14)** server. 3-column stat row (D9.A): years serving / projects completed / response time. Hairline borders + vertical dividers desktop. No icons, no hydration cost.
- `src/components/sections/location/LocationServicesGrid.tsx` — **NEW (1.14)** server. Renders exactly 6 service cards mapped from `location.featuredServices` (each entry pairs slug + audience to disambiguate cross-audience slugs). Section-level `<AnimateIn>` only.
- `src/components/sections/location/LocalProjectsStrip.tsx` — **NEW (1.14)** server. 3 placeholder project tiles with city captions. Leading code comment documents the Phase 1.16 D7.A real-projects fallback rule.
- `src/components/sections/location/LocalTestimonials.tsx` — **NEW (1.14)** server. Locked `card--testimonial` style: cream surface, 4px green left border, 5 filled amber stars (decorative role), italic Manrope quote, attribution caption. Quote prose is plain (NOT a `<blockquote>` — author + city are visible inside the card).
- `src/components/sections/location/WhyLocalPanel.tsx` — **NEW (1.14)** server. Two-column desktop / stacked mobile. Shared portrait left (4:5, lazy) + per-city ~120-word prose right + "Read more about Erick →" link.
- `src/components/sections/location/LocationFaq.tsx` — **NEW (1.14)** server. Wraps the locked `<FaqAccordion>` with 4 native `<details>` items per city. NO per-item `<AnimateIn>` (1.08 §3.7).
- `src/components/sections/location/LocationCTA.tsx` — **NEW (1.14)** server. Wraps shared `<CTA>` with `tokens={{city: location.name}}`. White surface (D10 alternation).
- `src/components/ui/LocationCard.tsx` — **NEW (1.14)** server. Photo-card style (locked `.card-photo`, 1.03 §6.2): full-card `<a>`, 4:3 aspect, lazy image, bottom-up dark gradient overlay, city name + tagline + ArrowUpRight chip. Reduced-motion respects locked card-hover rule.
- `src/components/ui/ProjectCard.tsx` — **NEW (1.16)** server. Locked photo-card primitive for project tiles, extracted from `HomeProjects.tsx` per Phase 1.15 handover §11.2. Receives already-resolved display props (`href`, `photo`, `alt`, `title`, optional `meta`/`audienceLabel`/`priority`/`loading`/`sizes`); does not reach into `projects.ts` or i18n. No motion wrapper — callers wrap the grid in ONE `<AnimateIn>` (Phase 1.07 P=86 lesson). Consumed by `HomeProjects` (homepage + About teaser via reuse), `ProjectsGrid` (index), and `RelatedProjects` (detail strip). `AudienceFeaturedProjects` and `LocalProjectsStrip` left on their own tile JSX (structural deltas; out-of-scope migrate).
- `src/components/sections/projects/ProjectsHero.tsx` — **NEW (1.16)** server. Compact text-only hero on `--color-bg-cream`. Eyebrow + H1 + dek + dynamic count line `{count} projects · {cities} cities · {minYear}–{maxYear}` interpolated from the unfiltered `PROJECTS` array. NO entrance animation (1.03 §7).
- `src/components/sections/projects/FilterChipStrip.tsx` — **NEW (1.16)** `"use client"`. Single-select audience filter (All / Residential / Commercial / Hardscape) wired to `?audience` URL state via `useRouter().replace()` with `{scroll: false}`. Filter changes always reset `?page`. Each chip is a `<button type="button" aria-pressed>` with hover/focus-visible transitions; mobile = horizontally scrollable strip with `mask-image` right-edge fade. Counts are static at request time, passed in from the server route (chip strip never recomputes them).
- `src/components/sections/projects/ProjectsGrid.tsx` — **NEW (1.16)** server. Renders the filtered + paginated slice via `<ProjectCard>`. SR-only H2 keeps page heading hierarchy sequential (H1 → H2 → tile H3). Index 0 carries `priority` (LCP candidate); all other tiles `loading="lazy"` to free up mobile bandwidth on first paint.
- `src/components/sections/projects/Pagination.tsx` — **NEW (1.16)** server. Pure URL-driven `<a>` per page, with prev/next chevrons. Disabled at boundaries via `aria-disabled="true"` + `pointer-events:none`. Returns `null` when `totalPages <= 1` (the current 12-row seed renders single-page so pagination is hidden today).
- `src/components/sections/projects/EmptyState.tsx` — **NEW (1.16)** server. Cream panel inside the white grid band, H3 + body + reset text-link to `/projects/`. Renders only when filtered subset length === 0 (page CTA still appears below).
- `src/components/sections/projects/detail/ProjectHero.tsx` — **NEW (1.16)** server. 60/40 split desktop / stacked mobile. Embeds `<Breadcrumb>` (light variant) at the top of the hero band. Lead photo carries `priority` + `fetchPriority="high"` (LCP candidate). Audience pill renders the on-light variant (cream bg, green-700 label). NO entrance animation.
- `src/components/sections/projects/detail/ProjectNarrative.tsx` — **NEW (1.16)** server. Cream surface, single `<AnimateIn fade-up>`. Splits per-project `narrative.{locale}` on blank lines for natural paragraph breaks. `text-wrap: pretty` on every `<p>`. H2 falls back to `title` when `narrativeHeading` is absent.
- `src/components/sections/projects/detail/ProjectGallery.tsx` — **NEW (1.16)** `"use client"`. Native `<dialog>` lightbox via `dialog.showModal()` — focus-trap + Esc native, focus-restore via `useRef` to the originating thumbnail button, ←/→ navigate, counter `aria-live="polite"`, backdrop click closes. 200ms cross-fade with reduced-motion → instant. First 4 thumbnails eager, photos 5–8 lazy. Same-source: the `photos` prop drives both rendered `<img>` and `CreativeWork.image` JSON-LD (composed in the route).
- `src/components/sections/projects/detail/ProjectFacts.tsx` — **NEW (1.16)** server. `<dl>` semantics, 6 rows (Year / City / Audience / Services / Materials / Duration). Service slugs resolve via audience-aware `getService` to localized names + deep links to the matching service detail page. City row deep-links to `/service-areas/<slug>/`. Audience row deep-links to `/<audience>/`. Single `<AnimateIn fade-up>` wraps the section.
- `src/components/sections/projects/detail/BeforeAfterToggle.tsx` — **NEW (1.16)** `"use client"`. Renders only when `hasBeforeAfter:true`. Two `<button type="button" aria-pressed>` in a segmented control. Default state shows AFTER (so SSR'd HTML is the better state for no-JS visitors). Two stacked `<Image>`s with 200ms opacity cross-fade; reduced-motion → instant. Keyboard: ←/→ swap state.
- `src/components/sections/projects/detail/RelatedProjects.tsx` — **NEW (1.16)** server. 3 tiles via `<ProjectCard>`. Selection delegates to `selectRelatedProjects(current)` in `projects.ts`: same-audience → same-city → most-recent excluding self, sort `year desc, slug asc` deterministic. Footer "See all projects →" link is body-link style (not a button). Single `<AnimateIn fade-up>`.
- `src/data/projects.ts` — **NEW (1.16)** typed bilingual seed of 12 placeholder projects (Hardscape × 5, Residential × 4, Commercial × 3; 2 per service-area city). Per-project: `slug`, `audience`, `serviceSlugs[]`, `citySlug`, `year`, `durationWeeks`, `materials`, `hasBeforeAfter`, `photoCount`, `title`, `shortDek`, optional `narrativeHeading`, `narrative`, `leadAlt`, `gallery[]` (file + alt), optional `beforeAlt` / `afterAlt`. Build-time assertions verify every `serviceSlugs[]` entry resolves in `services.ts` and every `citySlug` resolves in `locations.ts` — `next build` and `npm run dev` fail loudly on drift. Helpers: `getProject(slug)`, `isProjectAudience(value)`, `selectRelatedProjects(current, count=3)` with deterministic same-audience → same-city → most-recent tiers.
- `src/data/locations.ts` — **NEW (1.14)** typed bilingual seed for the 6 cities. Mirrors `services.ts` shape; per-city: `geo`, `pin`, `hero`, `trust` (with placeholder numbers), `featuredServices` (length 6, slug + audience pairs), `whyLocal` (~120 words), `testimonials` (1 per city, 5-star placeholders), `faq` (length 4), `meta`. Helpers: `getLocation(slug)`, `isLocationSlug(slug)`.
- `src/lib/schema/location.ts` — **NEW (1.14)** JSON-LD payload builders: `buildServiceAreasItemList(locations, locale)` (index page), `buildPlaceSchema(location, description)` (city page; references sitewide `LocalBusiness` via `@id`), `buildLocationServicesItemList(location, featured, locale)` (city page Service-ItemList), `buildLocationFaqSchema(location, locale)` (city page FAQPage).
- `src/data/imageMap.ts` — **Modified (1.16)** appended `PROJECT_LEAD`, `PROJECT_GALLERY`, and `PROJECT_BEFORE_AFTER` exports for the 12 portfolio projects. Each project aliases to existing service-project / audience-project sharp-generated tiles. Phase 2.04 swaps to real per-project photography by editing only these maps; canonical Phase 2 path is `/public/images/projects/{slug}/{lead,01..08,before,after}.avif` (handover §6.3). Phase 1.14 base unchanged.
- `src/components/forms/ContactForm.tsx` — **NEW (1.12)** CLIENT. The ONLY client component on /contact/. Honeypot + `<fieldset>`+`<legend>` for email-or-phone (D14) + four validation states (empty submit / invalid email / both empty / success) + `aria-busy`/`role="alert"`/`role="status"` wiring. Submit is `btn-primary btn-lg` (NOT amber).
- `src/components/ui/TeamCard.tsx` — **NEW (1.12)** server. `card--cream` variant with 4:5 portrait, eyebrow-chip role, H3 name, 1-2 sentence bio.
- `src/components/ui/CredentialBadge.tsx` — **NEW (1.12)** server. `<dl>`-marked credential card with kind-driven head ('unilock'|'years'|'tribune'|'google'). Reuses `<ServiceIcon unilock />` for the locked Unilock mark.
- `src/data/team.ts` — **NEW (1.12)** typed seed for the 3 team members (Erick, Nick, Marcin). Static-imports `src/assets/about/team-{slug}.jpg` placeholders. `roleKey`/`bioKey` resolve to `about.team.*` translations.
- `src/lib/schema/person.ts` — **NEW (1.12)** `buildPersonSchema(member)` JSON-LD. Each Person references the sitewide LocalBusiness via `worksFor.@id`.
- `src/lib/schema/contactPage.ts` — **NEW (1.12)** `buildContactPageSchema(locale)` JSON-LD. References sitewide LocalBusiness via `mainEntity.@id`. `availableLanguage: ['en', 'es']`.
- `src/components/ui/Breadcrumb.tsx` — **NEW (1.09)** server. Locale-aware breadcrumb with `aria-current="page"`. `light` and `on-dark` variants.
- `src/components/ui/FaqAccordion.tsx` — **NEW (1.09)** client. SSR `<details>`/`<summary>` with progressive enhancement for chevron rotation. Multi-open by default — every answer in SSR HTML for FAQPage schema validity.
- `src/components/ui/ServiceIcon.tsx` — **NEW (1.09)** server. Curated lucide-react icon map + hand-rolled Unilock placeholder mark.
- `src/data/services.ts` — typed seed for 16 services. Per-service H1, subhead, what's-included items, process steps, why-us cards, pricing, FAQ Q&As, related slugs. EN+ES strings throughout (D8). `pricing.mode: 'explainer'` for all 16 in Part 1. **Modified (1.10)**: added optional `imageKey?: string` to the `Service` type (asset-key disambiguation for slugs that exist across audiences). Renamed slug `'commercial-snow-removal'` → `'snow-removal'` and added `imageKey: 'commercial-snow-removal'` to keep its assets resolving. Updated `commercial/landscape-maintenance` `related` from `['commercial-snow-removal', ...]` → `['snow-removal', ...]`. Added `'driveways'` to `hardscape/patios-walkways`'s `related` (cross-link audit fix). New helpers: `getService(slug, audience?)` (now audience-aware) and `getRelatedService(slug, parentAudience)` (same-audience-first resolver for D7).
- `src/data/team.ts` — **NEW (1.12)** typed seed for the 3 About-page team members.
- `src/data/imageMap.ts` — **NEW (1.09)** static-import map for placeholder images. Audience heroes (3), audience project tiles (9), service heroes (16), service tile photos (16), service project tiles (~33).
- `src/lib/schema/breadcrumb.ts` — **NEW (1.09)** `buildBreadcrumbList(items)` JSON-LD payload builder.
- `src/lib/schema/service.ts` — **NEW (1.09)** `buildServiceSchema(service, locale)` + `buildFaqPageSchema(faq, locale)` + `buildAudienceItemList(...)` + `localePath(locale, path)` helpers.
- `src/lib/schema/person.ts` — **NEW (1.12)** `buildPersonSchema(member)` JSON-LD payload builder for About team members.
- `src/lib/schema/contactPage.ts` — **NEW (1.12)** `buildContactPageSchema(locale)` JSON-LD payload builder for /contact/.
- `src/lib/schema/project.ts` — **NEW (1.16)** JSON-LD payload builders. `buildProjectsItemList({projects, locale, leadUrlForSlug})` builds the `ItemList` of `CreativeWork` for the index (each item carries `creator.@id` referencing the sitewide `LocalBusiness`). `buildProjectCreativeWork({project, locale, imageUrls})` builds the detail `CreativeWork` (canonical `@id`, `image` array driven by the same source the gallery renders, `locationCreated.address` Place, `keywords` = audience + localized service names).
- `src/lib/constants/business.ts` — single source of truth for NAP.
- `src/lib/constants/navigation.ts` — single source of truth for nav IA.
- `src/hooks/useScrollState.ts` — client, rAF-throttled scroll boolean.
- `src/hooks/useBodyScrollLock.ts` — client, body scroll lock for the mobile drawer.
- `src/_project-state/` — this folder; living docs.
- `src/_project-state/Part-1-Phase-{01,02,03,04,05,07,09,10}-Completion.md` — phase completion reports written by Claude Code at the end of each code phase.

## src/assets/

- `src/assets/home/*.jpg` — Phase 1.07 home placeholders (hero, audience tiles, service tiles, project tiles, about portrait).
- `src/assets/audience/hero-{residential,commercial,hardscape}.jpg` — **NEW (1.09)** 16:9 audience hero placeholders, ~78KB each (noise-tuned).
- `src/assets/audience/projects/{residential|commercial|hardscape}-{1,2,3}.jpg` — **NEW (1.09)** 4:3 audience-landing project tile placeholders, ~3KB each.
- `src/assets/service/hero-{slug}.jpg` — **NEW (1.09)** 16:9 service hero placeholders for all 16 services, ~80KB each.
- `src/assets/service/tiles/{slug}.jpg` — **NEW (1.09)** 4:3 service-tile placeholders for the 16 audience-landing service grids, ~3KB each.
- `src/assets/service/projects/{slug}-{1,2,3}.jpg` — **NEW (1.09)** 4:3 service-detail featured-project tile placeholders, ~3KB each, ~33 total.
- `src/assets/about/hero.jpg` — **NEW (1.12)** 16:9 About hero placeholder, ~300KB. LCP candidate; below the 350KB budget. Real photo arrives Phase 2.04.
- `src/assets/about/brand-story.jpg` — **NEW (1.12)** 4:5 portrait placeholder for the brand-story image column, ~60KB.
- `src/assets/about/team-{erick,nick,marcin}.jpg` — **NEW (1.12)** 4:5 portrait placeholders for the three team-card photos, ~45KB each. Real photos + Marcin's last name (D13) arrive Phase 2.04.

## scripts/

- `scripts/gen-home-placeholders.mjs` — Phase 1.07 home asset generator (sharp + gradient + per-pixel noise).
- `scripts/gen-audience-service-placeholders.mjs` — **NEW (1.09)** audience + service asset generator. Same sharp + gradient + noise pattern. Run via `node scripts/gen-audience-service-placeholders.mjs` from the repo root.
- `scripts/gen-about-placeholders.mjs` — **NEW (1.12)** About-page asset generator (5 placeholder images: hero + brand-story + 3 team cards). Same sharp pattern. Run via `node scripts/gen-about-placeholders.mjs` from the repo root.

## sanity/

- `sanity/schemas/` — empty until Phase 2.03; `.gitkeep` only.
