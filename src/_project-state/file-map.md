# File Map

> One line per non-trivial file or directory. Keep it skim-able. Updated end of every phase.

## Project root

- `Sunset-Services-Plan.md` — master site plan and spec (v2 in-progress rewrite kept at the root; v1 archived at `archive/v1/Sunset-Services-Plan.md`).
- `Sunset-Services-Decisions.md` — **NEW (Phase 2.01)** append-only project-level decision log (ServiceM8 deferral, Anthropic alert-routing risk, Step 7 → Phase 2.13.2 deferral).
- `package.json` — npm manifest, pinned dependencies. **Phase 2.03 added** `sanity@^5.25.0` + `@sanity/vision@^5.25.0` + `@sanity/image-url@^2.1.1` + `styled-components@^6.4.1` to `dependencies`, and `studio:dev` + `studio:build` + `studio:deploy` to `scripts`. **Phase 2.05 added** `@portabletext/react@^4.0.3` + `@portabletext/block-tools@^5.1.1` + `jsdom@^25.0.1` to `dependencies`, and `tsx@^4.21.0` to `devDependencies`. **Phase 2.06 added** `zod@^3.25.76` to `dependencies` (resolved from spec's `^3.23` range).
- `tsconfig.json` — TypeScript strict config, `@/*` → `src/*` alias. **Phase 2.05 added** `@sanity-lib/*` → `./sanity/lib/*` for clean imports from page code into `sanity/lib/`.
- `next.config.ts` — Next.js config wrapped in `createNextIntlPlugin`.
- `postcss.config.mjs` — PostCSS config for Tailwind v4.
- `eslint.config.mjs` — ESLint flat config (Next + TS).
- `.gitignore` — standard Next gitignore + `.env*`. **Phase 2.03 added** `/dist/` and `/.sanity/` (Sanity build + dev cache).
- `.env.local.example` — placeholder env vars for future phases. **Phase 2.02 appended** a Vercel-sync note + caveat about the CLI plugin's `action_required` JSON for preview env adds without an explicit branch. **Phase 2.03 appended** 3 new lines (`NEXT_PUBLIC_SANITY_PROJECT_ID`, `_DATASET`, `_API_VERSION`) under the existing Sanity section.
- `.vercel/` — **NEW (Phase 2.02, gitignored)** Vercel project linkage. Contains `project.json` (`projectId`: `prj_OZ7kKRwIgpqoJGlWD7YguA7qYKbX`, `orgId`: `team_rRKMRUuOrwJk08a4BkSgNYAe`), `README.txt`, plus a `poll-deploy.js` helper script and `events.json` diagnostic dump from Phase 2.02 debugging. Re-run `vercel link` to restore if accidentally deleted.
- `next-env.d.ts` — Next.js TypeScript ambient types (auto-generated).
- `README.md` — generated by create-next-app (replace later).
- `AGENTS.md` / `CLAUDE.md` — pointers + Next 16 caveat.
- `sanity.config.ts` — **NEW (Phase 2.03)** Studio configuration. `projectId: 'i3fawnrl'`, `dataset: 'production'`, plugins `structureTool()` + `visionTool()`, schema `types` from `./sanity/schemas`. Loaded by `sanity dev` / `sanity build` / `sanity deploy`.
- `sanity.cli.ts` — **NEW (Phase 2.03)** Sanity CLI config. `api.projectId` + `api.dataset` for default CLI context, `studioHost: 'sunsetservices'` to pre-lock the deploy hostname, `deployment.appId: 'hza6xflhrkuygkrhketq6uhj'` so subsequent deploys are fully non-interactive.

## archive/

- `archive/` — **NEW (Phase 2.01)** root of historical-doc archive.
- `archive/v1/` — **NEW (Phase 2.01)** v1 docs archive folder.
- `archive/v1/Sunset-Services-Plan.md` — **NEW (Phase 2.01)** v1 master plan (was `Sunset-Services-Plan-old.md` at the repo root pre-2.01; renamed during the archive move).

## docs/

- `docs/design-handovers/` — design `.md` handovers from Claude Design. Phases 1.03, 1.05, 1.06, 1.08, 1.11, 1.13, 1.15, 1.17, 1.19. **Phase 2.01 housekeeping:** 1.08 moved here from repo root (was duplicated as `Part-1-Phase-08-Design-Handover (1).md` at root); 1.13, 1.17, 1.19 committed for the first time; root-level dupes of 1.05 and 1.06 deleted.
- `docs/runbooks/` — operational runbooks (Sanity workflow, deploy steps, etc.).
- `docs/decisions/` — append-only decision log.

## reports/

- `reports/` — phase completion reports written by Claude Code.
- `reports/phase-10-lh/` — **Committed in Phase 2.01 (pre-existing data)** Phase 1.10 Lighthouse JSON exports for 8 routes (baseline-lawn-care-mobile, commercial-snow-removal-desktop/mobile, hardscape-outdoor-kitchens-desktop/mobile, residential-tree-services-desktop/mobile, tree-desktop).

## public/

- `public/images/` — brand SVGs and static assets.
- `public/og/` — generated OG images (Phase 2.x).

## src/

- `src/proxy.ts` — Next 16 proxy (formerly middleware), runs next-intl locale routing.
- `src/i18n/routing.ts` — next-intl routing config (locales, defaultLocale, localePrefix).
- `src/i18n/request.ts` — server-side request config (loads messages per locale).
- `src/i18n/navigation.ts` — locale-aware `<Link>`, `useRouter`, etc.
- `src/messages/en.json` — English UI strings; top-level keys: `home.*` + `audience.*` (Phase 1.09) + `servicePage.*` (Phase 1.09) + `chrome.*`. Per-service strings live in `src/data/services.ts` (D8). Unchanged in 1.10.
- `src/messages/es.json` — Spanish UI strings; mirror of `en.json`. Phase 2.13 native review covers all es: strings end-to-end. Unchanged in 1.10.
- `src/app/globals.css` — Tailwind v4 entry; full `@theme` token block (added `--color-sunset-amber-200`) + `@layer base` + `@layer components`. Added `[data-audience]` custom-property block (Phase 1.09 §3X.1) plus the Phase 1.07 `.btn-ghost.btn-on-dark` modifier.
- `src/app/favicon.ico` — placeholder favicon.
- `src/app/[locale]/layout.tsx` — locale-aware root layout. Mounts fonts, `NextIntlClientProvider`, `<MotionRoot>`, chrome (`<SkipLink>`, `<Navbar>`, `<main id="main">`, `<Footer>`) plus `LocalBusiness` JSON-LD `<script>` in `<head>` + `<Analytics />` mount (Phase 2.02). **Phase 2.10 added** `<GTMNoScript/>` at the top of `<body>`, `<ConsentBanner/>` inside the provider (needs translations), and `<AnalyticsBridge/>` + `<GTMScript/>` + `<ClarityScript/>` after `<Analytics/>` (outside the provider).
- `src/app/[locale]/page.tsx` — homepage. Composes the seven `Home*` sections, locale-aware metadata, `WebSite` JSON-LD.
- `src/app/[locale]/[audience]/page.tsx` — audience-landing dynamic route. **Modified (1.10)**: tile-photo lookup now uses `s.imageKey ?? s.slug` so commercial snow-removal resolves to its own placeholder; removed unused `BUSINESS_URL` import.
- `src/app/[locale]/[audience]/[service]/page.tsx` — service-detail dynamic route. **Modified (1.10)**: lookup uses audience-aware `getService(service, audience)`; hero asset lookup uses `svc.imageKey ?? svc.slug`; related-services loop uses new `getRelatedService(slug, audience)` helper that prefers same-audience match (so commercial/landscape-maintenance's `related: ['snow-removal']` resolves to commercial/snow-removal, not residential).
- `src/app/[locale]/dev/system/page.tsx` — dev-only design-system smoke test (Phase 1.04).
- `src/app/[locale]/projects/page.tsx` — **Modified (Phase 2.05)** projects index. Reads from `getAllProjects()` via Sanity. `export const revalidate = 1800`. Index is ƒ (dynamic) due to searchParams; the 12-project list is fetched via ISR cache.
- `src/app/[locale]/projects/[slug]/page.tsx` — **Modified (Phase 2.05)** project detail. Reads via `getProjectBySlug` + `getAllProjectSlugs` (generateStaticParams). 24 SSG routes (12 × 2 locales) with 30m ISR. Adapter at `src/lib/sanity-adapters.ts` converts Sanity shape → TS `Project` shape so downstream components are unchanged.
- `src/app/[locale]/blog/page.tsx` — **Modified (Phase 2.05)** blog index. Reads via `getAllBlogPosts()`.
- `src/app/[locale]/blog/[slug]/page.tsx` — **Modified (Phase 2.05)** blog detail. Reads via `getBlogPostBySlug` + `getFaqsForBlog`. PortableText body via `ProseLayoutPT`. 10 SSG routes with 30m ISR.
- `src/app/[locale]/resources/page.tsx` — **Modified (Phase 2.05)** resources index. Reads via `getAllResources()`.
- `src/app/[locale]/resources/[slug]/page.tsx` — **Modified (Phase 2.05)** resource detail. Reads via `getResourceBySlug` + `getFaqsForResource`. PortableText body via `ProseLayoutPT`. HowTo step extraction via `extractHowToStepsFromBlocks`. 10 SSG routes with 30m ISR.
- `src/app/[locale]/[audience]/[service]/page.tsx` — **Modified (Phase 2.05)** service detail. FAQs read via `getFaqsForService(audience, slug)`. `export const revalidate = 1800`. 32 SSG routes with 30m ISR.
- `src/app/[locale]/service-areas/[city]/page.tsx` — **Modified (Phase 2.05)** city detail. FAQs read via `getFaqsForCity(slug)`. `export const revalidate = 1800`. 12 SSG routes with 30m ISR.
- `src/app/[locale]/dev/system/_client-demos.tsx` — client-only Dialog and Tooltip demos.
- `src/components/global/Logo.tsx` — server component, two skins (light/dark).
- `src/components/global/motion/easings.ts` — three named easings + four duration constants.
- `src/components/global/motion/variants.ts` — six `<AnimateIn>` variants.
- `src/components/global/motion/stagger.ts` — `staggerContainer` + `staggerItem` Framer-style variants.
- `src/components/global/motion/AnimateIn.tsx` — client, polymorphic, fires `whileInView` once.
- `src/components/global/motion/StaggerContainer.tsx` — client.
- `src/components/global/motion/StaggerItem.tsx` — client.
- `src/components/global/motion/MotionRoot.tsx` — client wrapper for `MotionConfig reducedMotion="user"`.
- `src/components/layout/Navbar.tsx` — server composer.
- `src/components/layout/NavbarScrollState.tsx` — client island. **Modified (1.09)**: pathname-based over-hero detection extended to recognize `/{audience}/` and `/{audience}/{service}/` routes (5-line addition).
- `src/components/layout/NavbarDesktop.tsx`, `NavbarMobile.tsx`, `NavbarLink.tsx`, `MegaPanelTrigger.tsx`, `ServicesMegaPanel.tsx`, `ResourcesMegaPanel.tsx`, `LanguageSwitcher.tsx`, `PhoneLink.tsx`, `SkipLink.tsx`, `SocialIcons.tsx` — Phase 1.05 chrome, unchanged.
- `src/components/layout/Footer.tsx` + `FooterBrand.tsx` + `FooterLinks.tsx` + `FooterServiceAreas.tsx` + `FooterLegal.tsx` — Phase 1.05 footer chrome. **Phase 2.08 restructure:** the newsletter signup was lifted out of the 3-column grid (now 2-column: brand + links) into a new TOP section above the grid; `FooterNewsletter.tsx` (the Part-1 placeholder) was deleted, replaced by `src/components/forms/NewsletterSignup.tsx` (wired component, hidden on `/request-quote/`).
- `src/components/layout/icons/{GoogleBusinessProfileIcon,FacebookIcon,InstagramIcon,YoutubeIcon}.tsx` — hand-rolled brand glyphs, Phase 1.05.
- `src/components/sections/home/Home*.tsx` — homepage section components, Phase 1.07, unchanged.
- `src/components/sections/audience/AudienceHero.tsx` — **NEW (1.09)** server. Full-bleed photo hero with kicker + H1 + subhead + CTA pair. No entrance animation (LCP discipline).
- `src/components/sections/audience/AudienceQualifier.tsx` — **NEW (1.09)** server. "Who this is for" eyebrow + H2 + body + 4 trust pills (badge-pill).
- `src/components/sections/audience/AudienceServicesGrid.tsx` — **NEW (1.09)** server. 4:3 photo card tiles (D2). 6 tiles for residential/hardscape, 4 for commercial.
- `src/components/sections/audience/AudienceFeaturedProjects.tsx` — **NEW (1.09)** server. 3 project tiles with View-all CTA-link top-right.
- `src/components/sections/audience/AudienceWhyUs.tsx` — **NEW (1.09)** server. 4 value-prop cards on cream surface, audience-accent applied to icon-tile bg.
- `src/components/sections/audience/AudienceUnilockBand.tsx` — **NEW (1.09)** server. Hardscape-only charcoal band per D6 + §3X.5. Hand-rolled Unilock badge placeholder.
- `src/components/sections/audience/AudienceSocialProof.tsx` — server. Testimonial cards + 1px hairline + credentials row. **Modified (1.10)**: replaced raw `"…"` quote characters with `&ldquo;…&rdquo;` to clear two react/no-unescaped-entities lint errors.
- `src/components/sections/audience/AudienceFAQ.tsx` — **NEW (1.09)** server. Wraps `<FaqAccordion>`. Per-item `<AnimateIn>` deliberately absent (handover §10).
- `src/components/sections/audience/AudienceCTA.tsx` — **NEW (1.09)** server. The page's only amber CTA in `<main>`. Cream surface for residential/commercial; charcoal for hardscape (D6).
- `src/components/sections/service/ServiceHero.tsx` — **NEW (1.09)** server. 52vh / 44vh hero with 3-level breadcrumb. Secondary CTA is `tel:+16309469321` (D10).
- `src/components/sections/service/ServiceWhatsIncluded.tsx` — **NEW (1.09)** server. Adaptive grid per D3: 1-col @ 3 items, 2-col @ 4, 3-col @ 5–6.
- `src/components/sections/service/ServiceProcess.tsx` — **NEW (1.09)** server. Numbered cards on dashed connecting hairline (D4); rotates to vertical timeline on mobile.
- `src/components/sections/service/ServiceWhyUs.tsx` — **NEW (1.09)** server. 3 cream-fill tiles on white surface.
- `src/components/sections/service/ServicePricing.tsx` — **NEW (1.09)** server. State A (price) and State B (explainer) — both occupy the same vertical footprint to keep alternation invariant. All 16 services in Part 1 ship with State B.
- `src/components/sections/service/ServiceFeaturedProjects.tsx` — **NEW (1.09)** server. 2–3 tiles, View-all CTA-link (404s in Part 1).
- `src/components/sections/service/ServiceFAQ.tsx` — **NEW (1.09)** server. Wraps `<FaqAccordion>`. Per-item `<AnimateIn>` deliberately absent.
- `src/components/sections/service/ServiceRelated.tsx` — server. 3–4 no-photo nav tiles. D7-driven from `services.ts` `related` array. **Modified (1.10)**: removed hard-coded `aria-label="Learn more about ${service.name[locale]}"` (English bleed-through to ES routes; the inner `<h3>` already serves as the link's accessible name natively).
- `src/components/sections/service/ServiceCTA.tsx` — **NEW (1.09)** server. The page's only amber CTA in `<main>`. Cream surface (no charcoal on service-detail per D6).
- `src/components/calendly/CalendlyEmbed.tsx` — **NEW (Phase 2.07), Phase 2.10 EXTENDED** client component. Calendly inline-widget loader with consent-gated `IntersectionObserver`-driven lazy load (200px rootMargin) + `<noscript>` fallback + static fallback card (tel + URL anchor) when flag off / URL missing / consent denied. Reads `NEXT_PUBLIC_CALENDLY_URL` + `NEXT_PUBLIC_CALENDLY_ENABLED`. Idempotent script injection (checks `script[src*="calendly.com/.../widget.js"]` first); cleanup on unmount via a `data-sunset-calendly="true"` ownership marker so concurrent instances don't trample each other. Takes `{locale, namespace, minHeight?, surface?}`. **Phase 2.10 added** (1) `fireWidgetLoadedOnce()` inside the existing IntersectionObserver `inject()` callback so `calendly_widget_loaded` fires once on viewport approach, and (2) a `message` listener that maps Calendly's broadcast events (`calendly.event_type_viewed` / `date_and_time_selected` / `event_scheduled`) into `sunset:calendly-event` CustomEvents. Existing consent stub default-true preserved (booking widget loads regardless of cookie banner state).
- `src/components/sections/contact/ContactCalendlyPlaceholder.tsx` — **Modified (Phase 2.07)** delegates to `<CalendlyEmbed namespace="contact.calendly" minHeight={720} surface="cream" />`. Phase 1.11 section chrome (cream surface, eyebrow + h2 + body, `AnimateIn`) preserved verbatim; the mock calendar grid is gone. A small secondary `tel:` link sits below the widget as a phone-preference backup CTA. Filename kept (Approach B per the Phase 2.07 plan) so the `/contact/` page import stays untouched.
- `src/app/[locale]/contact/page.tsx` — **Unmodified content** — still imports `ContactCalendlyPlaceholder`; the delegation lives inside that component.
- `src/app/[locale]/thank-you/page.tsx` — **Modified (Phase 2.07)** Section 2 now renders the section chrome (eyebrow + new `thanks.calendly.h2` + new `thanks.calendly.sub`) + `<CalendlyEmbed namespace="thanks.calendly" minHeight={680} surface="bg" />`. Removed the unused `BUSINESS_PHONE`/`BUSINESS_PHONE_TEL` imports — the fallback `tel:` button is now owned by `CalendlyEmbed`. Surface alternation unchanged (cream/white/cream/white/cream).
- `src/messages/en.json` — **Modified (Phase 2.07)** added `contact.calendly.{sub,fallbackCta,fallbackLink,iframeLabel}` (existing `h2` value preserved) + `thanks.calendly.{h2,sub,fallbackCta,fallbackLink,iframeLabel}`.
- `src/messages/es.json` — **Modified (Phase 2.07)** same key additions; ES values flagged `[TBR]` for Phase 2.13 native review.
- `.env.local.example` — **Modified (Phase 2.07)** new comment block documenting `NEXT_PUBLIC_CALENDLY_URL` + `NEXT_PUBLIC_CALENDLY_ENABLED`. **Modified (Phase 2.08)** new comment block documenting `RESEND_DOMAIN_VERIFIED` + `CONTACT_SUBMIT_ENABLED` + `NEWSLETTER_SUBMIT_ENABLED`.
- `src/messages/en.json` — **Modified (Phase 2.08)** updated `chrome.footer.newsletter.{heading,helper}` copy (was Phase 1.05 placeholder text); added `successMessage`, `alreadySubscribed`, `invalidEmail`, `errorMessage`. `placeholderNote` is now orphaned (component no longer references it).
- `src/messages/es.json` — **Modified (Phase 2.08)** same key set as en.json, every value flagged `[TBR]` for Phase 2.13 native review.
- `package.json` — **Modified (Phase 2.08)** added `@react-email/components@^1.0.12` + `@react-email/render@^2.0.8`.
- `sanity/schemas/index.ts` — **Modified (Phase 2.08)** registers `contactSubmission` + `newsletterSubscriber` in the `schemaTypes` array.
- `src/components/ui/Breadcrumb.tsx` — **NEW (1.09)** server. Locale-aware breadcrumb with `aria-current="page"`. `light` and `on-dark` variants.
- `src/components/ui/FaqAccordion.tsx` — **NEW (1.09)** client. SSR `<details>`/`<summary>` with progressive enhancement for chevron rotation. Multi-open by default — every answer in SSR HTML for FAQPage schema validity.
- `src/components/ui/ServiceIcon.tsx` — **NEW (1.09)** server. Curated lucide-react icon map + hand-rolled Unilock placeholder mark.
- `src/data/services.ts` — typed seed for 16 services. Per-service H1, subhead, what's-included items, process steps, why-us cards, pricing, related slugs. EN+ES strings throughout (D8). **Modified (Phase 2.05)**: inline `faq` arrays removed across all 16 entries (Sanity is now the FAQ source of truth). `FaqItem` type deleted. Line count 2979 → 2374. Earlier (Phase 1.10) modifications still hold (imageKey, audience-aware getService + getRelatedService helpers, snow-removal slug dedupe).
- `src/data/imageMap.ts` — **NEW (1.09)** static-import map for placeholder images. Audience heroes (3), audience project tiles (9), service heroes (16), service tile photos (16), service project tiles (~33).
- `src/data/locations.ts` — **Modified (Phase 2.05)**: inline `faq` arrays removed from all 6 city entries (Sanity is now the FAQ source of truth). `LocationFaqItem` type deleted. Line count 676 → 425.
- `src/lib/sanity-adapters.ts` — **NEW (Phase 2.05)** adapter functions that convert Sanity-fetched data shapes (`ProjectSummary`, `ProjectDetail`) into the Phase 1.16 TS `Project` shape so existing Phase 1.16 components (ProjectsGrid, ProjectHero, ProjectFacts, etc.) work unchanged.
- `src/lib/quote/validation.ts` — **NEW (Phase 2.06)** Zod schemas for `/api/quote` (full) and `/api/quote/partial` (Steps 1–3 abandoner) payloads. `QuoteSubmitSchema` accepts the union of all audience-conditional Step 3 fields. Top-level objects use `.strict()`. Honeypot accepts any length (the route handler consumes the bot-deflection signal upstream). **Phase 2.08 added** `locale: z.enum(['en','es']).default('en')` so emails render in the lead's language.
- `src/lib/quote/mautic.ts` — **NEW (Phase 2.06)** Mautic CRM stub. `pushFullLeadToMautic()` + `pushPartialLeadToMautic()` log a single no-op line when `MAUTIC_ENABLED=false`. Real implementation lives inside the `TODO Phase 2.x` block in each function.
- `src/lib/quote/resend.ts` — **NEW (Phase 2.06), REWRITTEN (Phase 2.08).** Thin wrapper around `src/lib/email/send.ts`. Exports `sendQuoteLeadAlertEmail(input, primaryServiceDisplayName, sanityDocId)` and `sendQuoteVisitorConfirmationEmail(input, primaryServiceDisplayName)`. Both build the right `<QuoteLeadAlertEmail/>` / `<QuoteConfirmationEmail/>` React element and call `sendBrandedEmail()` so sandbox routing is automatic.
- `src/lib/quote/session.ts` — **NEW (Phase 2.06).** Client-side session-ID helper. `getOrCreateSessionId()` returns a persistent UUID at localStorage key `sunset_wizard_session_id`. `clearSessionId()` is called from the Submit success path. **Phase 2.08:** `generateUuid()` extracted to `src/lib/sessionId.ts` and re-imported here so contact + newsletter forms can share it.
- `src/lib/sessionId.ts` — **NEW (Phase 2.08)** shared `generateUuid()` (RFC 4122 v4 via `crypto.randomUUID()` with a small fallback). Used by the wizard's localStorage-backed session helper AND by contact + newsletter forms (fresh UUID per submit, no persistence).
- `src/app/api/quote/route.ts` — **NEW (Phase 2.06), REFACTORED (Phase 2.08).** POST handler now sends TWO branded emails (lead alert to Erick + visitor confirmation) via `sendBrandedEmail()`. Adds `primaryServiceDisplayName` resolver (audience+slug → human name in the lead's locale). Master flag (`WIZARD_SUBMIT_ENABLED`), honeypot-before-Zod, durable-first Sanity write, best-effort emails — Phase 2.06 architecture preserved.
- `src/app/api/quote/partial/route.ts` — **NEW (Phase 2.06)** POST handler for abandoner pushes (Steps 1, 2, 3 only). Deterministic `_id` (`quoteLeadPartial-<sessionId>`). Fetch-then-patch-or-create preserves `firstSeenAt` across the three pushes. Returns `simulated` when `WIZARD_SUBMIT_ENABLED=false`.
- `src/lib/email/tokens.ts` — **NEW (Phase 2.08)** email design tokens. Mirrors `globals.css` palette as plain hex/px values (CSS variables don't work in email HTML). Includes `business.*` block with the public-facing identity (`info@sunsetservices.us` + Aurora address + tel + website) used in every email footer regardless of actual sender.
- `src/lib/email/send.ts` — **NEW (Phase 2.08)** `sendBrandedEmail({to, subject, react, intendedRecipient?})` shared utility. Reads `RESEND_DOMAIN_VERIFIED`; when false, reroutes to `RESEND_TO_EMAIL` + injects `intendedRecipient` via `React.cloneElement` for the banner + prefixes subject `[SANDBOX → <intended>]`. Never throws; returns `{ok, messageId?, error?}`.
- `src/lib/email/components/EmailLayout.tsx` — **NEW (Phase 2.08)** shared React Email primitive. Wraps every template with `<Html>/<Head>/<Preview>/<Body>/<Container>`, conditional sandbox banner (when `intendedRecipient` is set), brand wordmark header, body slot, NAP footer, optional unsubscribe link. Exports `EmailButton` (primary/amber/ghost variants).
- `src/lib/email/templates/QuoteLeadAlertEmail.tsx` — **NEW (Phase 2.08)** to-Erick lead-alert template. Contact + project + audience-conditional details + notes blockquote + mailto/tel CTAs + Sanity Studio deep link in the meta footer.
- `src/lib/email/templates/QuoteConfirmationEmail.tsx` — **NEW (Phase 2.08)** visitor receipt. H1 with first-name + "What happens next" 3-step block + Calendly link (via `/thank-you/?firstName=`) + tel: secondary. Bilingual COPY object; ES flagged `[TBR]`.
- `src/lib/email/templates/ContactAlertEmail.tsx` — **NEW (Phase 2.08)** to-Erick contact-form alert. Contact + category + message blockquote + mailto/tel CTAs + Studio deep link.
- `src/lib/email/templates/ContactConfirmationEmail.tsx` — **NEW (Phase 2.08)** visitor receipt for `/contact/` submissions. Short — H1 with first-name + "In the meantime" with a `/projects` link + tel: secondary. ES flagged `[TBR]`.
- `src/lib/email/templates/NewsletterWelcomeEmail.tsx` — **NEW (Phase 2.08)** new-subscriber welcome. Lead paragraph + 3 ghost-button quick-links (Recent projects / From the blog / Free estimate). `unsubscribeUrl` plumbed but Phase 2.08 passes `undefined` (no unsubscribe endpoint yet). ES flagged `[TBR]`.
- `src/lib/contact/contactSchema.ts` — **NEW (Phase 2.08)** server-side Zod schema for `/api/contact`. Matches the live ContactForm shape: single `name`, optional email + phone (route enforces D14 at-least-one-of after parsing), optional `category` enum, optional `message`. Honeypot accepts any length (route consumes upstream).
- `src/lib/contact/mauticStub.ts` — **NEW (Phase 2.08)** `pushContactToMautic(input)`. Same gating + log line as `src/lib/quote/mautic.ts`.
- `src/app/api/contact/route.ts` — **NEW (Phase 2.08)** POST handler for `/contact/` form. Mirrors `/api/quote`: master flag (`CONTACT_SUBMIT_ENABLED`), honeypot-before-Zod, D14 server-side guard, durable-first Sanity write, branded alert + visitor confirmation via `sendBrandedEmail()`, Mautic stub. `runtime = 'nodejs'`.
- `src/app/api/newsletter/route.ts` — **NEW (Phase 2.08)** POST handler for footer newsletter signup. Master flag (`NEWSLETTER_SUBMIT_ENABLED`), honeypot, Zod, fresh / resubscribe / `already_subscribed` branches via lookup-then-create-or-patch, branded welcome email, Mautic stub line. Sanity Studio's async unique-email validator runs at publish time only; this route is the authoritative duplicate guard.
- `src/components/forms/NewsletterSignup.tsx` — **NEW (Phase 2.08)** real footer newsletter signup (replaces `FooterNewsletter.tsx`). Client component. Honors `NEXT_PUBLIC_NEWSLETTER_ENABLED` flag (default true); hidden on `/request-quote/` via `usePathname()` regex to protect the conversion surface (D17 pattern). 2-column desktop / stacked mobile. White input + green-500 button on the charcoal footer surface. Honeypot field. Fires `sunset:newsletter-event` CustomEvents for Phase 2.11 GTM bridge. Inline success / already_subscribed / invalid / error states with `aria-live="polite"`.
- `src/components/forms/ContactForm.tsx` — **NEW (Phase 1.11), WIRED (Phase 2.08).** Phase 2.08 swapped the Part-1 simulation handler for a real POST to `/api/contact` with `keepalive: true`. Fires `sunset:contact-event` CustomEvents for the GTM bridge. Inline error messages on network/server failure; success state unchanged from Phase 1.11.
- `src/components/wizard/WizardShell.tsx` — **Modified (Phase 2.06)** `handleNext()` fires a fire-and-forget `keepalive: true` POST to `/api/quote/partial` after step 1, 2, or 3 advances (NEVER step 4). New `extractPartialDetails()` helper at the bottom of the file. Imports `getOrCreateSessionId` from `@/lib/quote/session`. Phase 1.20 wizard composition + URL-driven step state + autosave behavior unchanged.
- `src/components/wizard/WizardStep5Review.tsx` — **Modified (Phase 2.06)** Submit handler now POSTs to `/api/quote` instead of `console.log`-ing. New `submitError` state with `role="alert"` display above Submit button. New off-screen + `aria-hidden` + `tabIndex={-1}` honeypot input (`#company_website`). Clears `localStorage` (Step 1–3 autosave + session ID) on success. Preserves the `wizard_submit_succeeded` analytics event firing for the Phase 2.10/2.11 GTM bridge. Imports `getOrCreateSessionId` + `clearSessionId` from `@/lib/quote/session`, `clearStep1to3` from `@/lib/wizard/storage`.
- `.env.local.example` — **Modified (Phase 2.06)** documents six new env vars: `WIZARD_SUBMIT_ENABLED` (flipped to `true` default), `MAUTIC_ENABLED`, `MAUTIC_BASE_URL`, `MAUTIC_API_KEY`, `RESEND_FROM_EMAIL`, `RESEND_TO_EMAIL`. **Modified (Phase 2.09)** new `# Phase 2.09 — AI chat backend` comment block documenting `AI_CHAT_ENABLED`, `ANTHROPIC_MODEL`, `CHAT_DAILY_LIMIT_PER_IP`, `CHAT_BURST_INTERVAL_MS`. `NEXT_PUBLIC_AI_CHAT_ENABLED` flipped from `false` to `true` and moved into the feature-flags block.
- `src/lib/chat/knowledgeBase.ts` — **NEW (Phase 2.09)** Sanity-grounded knowledge digest builder. Exports `buildKnowledgeDigest(locale)` (30-min TTL memoised) + `warmKnowledgeDigest()`. Composes 8 sections: identity / where-we-work / Residential services / Commercial services / Hardscape services (Unilock line) / team bios / top 10 FAQs / contact + hours. Inline ES `LocaleLabels` block flagged `[TBR]`.
- `src/lib/chat/systemPrompt.ts` — **NEW (Phase 2.09)** persona + tool definition. Exports `buildSystemPrompt({locale, digest})` returning a 2-block `system` array (persona + KNOWLEDGE_DIGEST), both blocks carrying `cache_control: {type: 'ephemeral'}` for Anthropic prompt caching. Exports `FLAG_HIGH_INTENT_TOOL` (single-arg `reason: string` JSON schema). PERSONA_EN spec; PERSONA_ES hand-authored (NOT machine-translated), flagged `[TBR]`.
- `src/lib/chat/rateLimit.ts` — **NEW (Phase 2.09)** in-memory two-tier rate limiter. Burst (1 msg / 2s) + daily (50 / IP / 24h). Module-scoped `Map<string, ...>` counters that reset on every Vercel function cold start. Top-of-file banner flags the replacement carryover to Phase 3.10. Single export `checkRateLimit(ip)`.
- `src/lib/chat/getIp.ts` — **NEW (Phase 2.09)** `getClientIp(headers)` — prefers `x-forwarded-for[0]`, falls back to `x-real-ip`, then `'unknown'`. Used by `/api/chat` for the rate limiter.
- `src/lib/chat/streamClient.ts` — **NEW (Phase 2.09)** client-side SSE consumer. `streamFromBackend({messages, locale, sessionId, onToken, onHighIntent, onDone, onError})` POSTs `/api/chat`, manually reads the SSE stream via `Response.body.getReader()`, maps `data: {type:'token'|'high_intent'|'done'|'error'}` events to callback fires. Maps HTTP 429/503 to typed error kinds.
- `src/lib/chat/mauticStub.ts` — **NEW (Phase 2.09)** `pushChatLeadToMautic(input)` — log-only no-op when `MAUTIC_ENABLED=false`, same shape as `src/lib/quote/mautic.ts`. Imports nothing from sibling chat modules.
- `src/lib/chat/storage.ts` — **Modified (Phase 2.09)** added `SESSION_ID_KEY = 'sunset_chat_session_id'` + `getOrCreateChatSessionId()`. `clearHistory(locale)` now removes the session ID as well so Reset starts a fresh server-side conversation.
- `src/lib/chat/flags.ts` — unchanged from Phase 1.20.
- `src/lib/chat/events.ts` — **Modified (Phase 2.10)** dispatcher rename pass. `PANEL_OPENED` → `OPENED` with wire value `'chat_panel_opened'` → `'chat_opened'`. Added `BANNER_BOOK_CLICKED: 'chat_banner_book_clicked'` and `BANNER_QUOTE_CLICKED: 'chat_banner_quote_clicked'` (Phase 2.09 set the `data-analytics-event` attributes on `ChatHighIntentBanner` Links; Phase 2.10 added the matching CustomEvent dispatch the AnalyticsBridge needs).
- `src/app/api/chat/route.ts` — **NEW (Phase 2.09)** POST handler. `runtime='nodejs'` + `dynamic='force-dynamic'`. Order: `AI_CHAT_ENABLED` kill switch → rate limit (before body parse) → Zod-validate → build (or read-cached) digest → stream from Anthropic → relay token deltas as SSE → after stream, inspect `finalMessage()` for `tool_use` blocks, emit `high_intent` events → emit `done` with usage. Catches relay errors and emits `data: {type:'error',...}`.
- `src/app/api/chat/lead/route.ts` — **NEW (Phase 2.09)** lead-capture POST. Mirrors Phase 2.06 durable-first pattern: kill switch (returns `{status:'simulated'}` when off) → honeypot-before-Zod (silent 200) → Zod-validate → Sanity `chatLead` write → branded `ChatLeadEmail` via `sendBrandedEmail()` → Mautic stub → returns `{status:'ok', sanityDocId}`. Sanity failure → 500; email/Mautic failure → logged, non-blocking.
- `src/lib/email/templates/ChatLeadEmail.tsx` — **NEW (Phase 2.09)** branded to-Erick email. Contact block → optional page-context line → optional amber-tinted "Why this was flagged ready to book" callout → transcript excerpt (last 10 turns, alternating bg) → primary "Reply by email" + ghost "Call now" CTAs → "View in Sanity Studio →" footer link. EN-only body (Erick reads English regardless of lead locale).
- `src/components/chat/ChatPanel.tsx` — **Modified (Phase 2.09)** canned-stream stub replaced with `streamFromBackend` SSE consumer. New `highIntent` state slot (clears at start of each new outgoing turn). New `errorKind` widened to `'network' | 'rate_burst' | 'rate_daily' | 'disabled' | 'generic'`. Lead form submit now POSTs to `/api/chat/lead` (mirrors Phase 2.08 contact pattern: keepalive POST, status-driven UI, CustomEvent analytics). High-intent banner moved to between message log and composer per the Phase 2.09 plan visual spec. Session ID materialised on mount via `getOrCreateChatSessionId()`.
- `src/components/chat/ChatHighIntentBanner.tsx` — **Modified (Phase 2.09)** rewritten. New `highIntent: {reason: string} | null` prop. Renders amber-tinted (`#FDF7E8` + `border-left: 3px solid amber-700`) banner with `chat.banner.intro` body + two CTAs (primary `/contact#calendly` + ghost `/request-quote`) + × dismiss. No auto-dismiss timer (replaced by user-driven dismiss / new-turn clearing).
- `src/components/chat/ChatErrorState.tsx` — **Modified (Phase 2.09)** kinds widened from `'network' | 'rate' | 'api'` to `'network' | 'rate_burst' | 'rate_daily' | 'disabled' | 'generic'`. Maps each kind to its `chat.errors.*` i18n key. Existing `chat.error.{network,networkRetry,rate,api}` keys kept (`network` + `networkRetry` still used by `'network'` kind retry button).
- `src/components/chat/ChatLeadForm.tsx` — **Modified (Phase 2.09)** added hidden honeypot `<input name="website">` (off-screen) + `honeypot` state. Submit callback signature extended from `{firstName, email, phone}` to `{firstName, email, phone, honeypot}`. Visible fields unchanged.
- `sanity/schemas/chatLead.ts` — **NEW (Phase 2.09)** Sanity document type. 10 fields across 3 groups (contact / transcript / meta). `name`, `email` (validator), `locale`, `pageContext` (URL), `sessionId`, `capturedAt`, `triggerReason`, `status` (new/contacted/qualified/closed), `mauticSynced`, `transcriptExcerpt` (max 20 inline `{role, content, ts}` messages). Ordered by `capturedAt desc`.
- `sanity/schemas/index.ts` — **Modified (Phase 2.09)** registers `chatLead`.
- `sanity/lib/queries.ts` — **Modified (Phase 2.09)** added chat-specific projection helpers `getAllServicesForChat`, `getAllLocationsForChat`, `getTopFaqsForChat` + their return types (`ChatServiceEntry`, `ChatLocationEntry`, `ChatFaqEntry`). FAQ helper fetches 3 per audience scope + tops up from service-scoped FAQs to hit the limit.
- `src/messages/en.json` — **Modified (Phase 2.09)** added `chat.banner.{intro, primaryCta, secondaryCta, closeAriaLabel}` (existing `highIntent` kept as orphan), `chat.leadCapture.{confirmed, error}`, `chat.errors.{rateLimitedBurst, rateLimitedDaily, disabled, generic}`.
- `src/messages/es.json` — **Modified (Phase 2.09)** same key set, every new ES value flagged `[TBR]` for Phase 2.13 native review.
- `src/messages/en.json` — **Modified (Phase 2.10)** added `chrome.consent.{heading, body, acceptCta, declineCta, ariaLabel}`.
- `src/messages/es.json` — **Modified (Phase 2.10)** same key set, every new ES value flagged `[TBR]` for Phase 2.13 native review.
- `src/lib/wizard/events.ts` — **Modified (Phase 2.10)** dispatcher rename pass. `SUBMIT_SUCCEEDED` wire value `'wizard_submit_succeeded'` → `'quote_submit_succeeded'` (CONVERSION — Cowork's GTM Key Event tag references the new name). New `STEP_ADVANCED: 'wizard_step_advanced'` replaces the per-step `STEP_COMPLETED(n)` function family on the forward-transition fire site (constant + `{step}` payload instead of dynamic suffix).
- `src/components/wizard/WizardShell.tsx` — **Modified (Phase 2.10)** the forward-transition fire site (`handleNext` success path) now calls `fireWizardEvent(WIZARD_EVENTS.STEP_ADVANCED, {step, locale})` instead of `WIZARD_EVENTS.STEP_COMPLETED(urlStep)`. Phase 2.06 partial-push behavior + Phase 1.20 wizard composition unchanged.
- `src/components/wizard/WizardStickyNav.tsx` — **Modified (Phase 2.10)** Next button's `data-analytics-event` attribute switched from removed `STEP_COMPLETED(n)` function to the new `STEP_ADVANCED` constant. Caught by `npm run build` after the rename pass.
- `src/components/chat/ChatPanel.tsx` — **Modified (Phase 2.10)** mount-effect call site uses `CHAT_EVENTS.OPENED` (was `CHAT_EVENTS.PANEL_OPENED`). Wire value moved from `'chat_panel_opened'` to `'chat_opened'`.
- `src/components/chat/ChatHighIntentBanner.tsx` — **Modified (Phase 2.10)** added `onClick` handlers that fire `CHAT_EVENTS.BANNER_BOOK_CLICKED` and `CHAT_EVENTS.BANNER_QUOTE_CLICKED` CustomEvents alongside the existing `data-analytics-event` attributes (Phase 2.09 set the attrs; 2.10 added the dispatch so the AnalyticsBridge picks them up). Component now imports `CHAT_EVENTS` + `fireChatEvent` + `useLocale`.
- `src/components/forms/NewsletterSignup.tsx` — **Modified (Phase 2.10)** dispatcher wire-value renames: `'newsletter_submit_succeeded'` → `'newsletter_subscribed'` (CONVERSION) and `'newsletter_submit_already_subscribed'` → `'newsletter_already_subscribed'`. Inline `fireNewsletterEvent` shape unchanged.
- `src/app/globals.css` — **Modified (Phase 2.10)** added `--z-consent: 60` token to the z-index ladder (banner sits between `--z-chat: 50` and `--z-banner: 70`; shares numeric tier with `--z-toast: 60`).
- `.env.local.example` — **Modified (Phase 2.10)** new comment block documenting `NEXT_PUBLIC_GTM_ID`, `NEXT_PUBLIC_GA4_MEASUREMENT_ID`, `NEXT_PUBLIC_CLARITY_PROJECT_ID`, `NEXT_PUBLIC_ANALYTICS_ENABLED`. Real values upserted on Vercel Prod + Preview (REST API, IDs in 00_stack-and-config.md Phase 2.10 section). The Cowork-flagged GCP carryover block is NOT in this commit — left to the user or Phase 2.13.2 per the Cowork handover note.
- `lighthouse/phase-2-10/` — **NEW (Phase 2.10, local-only/gitignored)** 4 JSON reports: home-desktop, home-mobile, contact-desktop, contact-mobile. Scores match or beat Phase 2.09 baseline.
- `lighthouse/phase-2-09/` — **NEW (Phase 2.09, local-only/gitignored)** 4 JSON reports: home-desktop, home-mobile, contact-desktop, contact-mobile. Scores match or beat Phase 2.08 baseline.
- `.claude/launch.json` — **NEW (Phase 2.09)** Preview MCP launch config. Single `sunset-prod` configuration that runs `npm run start -- -p 3030` for browser smoke testing.
- `src/lib/images/resolveProjectImage.ts` — **NEW (Phase 2.05)** image-resolution helper. Sanity asset wins when present; falls back to `imageMap.ts` placeholders. Returns `StaticImageData`-compatible shape so existing consumers don't change.
- `src/components/content/portableTextComponents.tsx` — **NEW (Phase 2.05)** PortableText serializers mapping Sanity Portable Text blocks to the `.prose__*` class shape Phase 1.18's `renderProse` Markdown renderer emitted. Server-safe (no `'use client'`).
- `src/components/content/portableTextHelpers.ts` — **NEW (Phase 2.05)** server-safe utility helpers (`slugify`, `blockToPlainText`, `blocksToPlainText`, `extractHeadingsFromBlocks`, `countWordsInBlocks`, `extractHowToStepsFromBlocks`).
- `src/components/content/ProseLayoutPT.tsx` — **NEW (Phase 2.05)** PortableText counterpart to `ProseLayout` (Phase 1.18 Markdown). Same surrounding layout — sticky right-rail TOC at xl+, inline collapsed `<details>` below xl, inline `<ServiceCard>` cross-link splice between H2s, inline `<ServiceAreaStrip>` near body bottom.
- `src/components/sections/location/LocationFaq.tsx` — **Modified (Phase 2.05)** accepts already-projected `items: {id, question, answer}[]` + `cityName: string` props instead of reading `location.faq` internally. Matches the ServiceFAQ component contract.
- `src/lib/schema/breadcrumb.ts` — **NEW (1.09)** `buildBreadcrumbList(items)` JSON-LD payload builder.
- `src/lib/schema/service.ts` — **NEW (1.09)** `buildServiceSchema(service, locale)` + `buildFaqPageSchema(faq, locale)` + `buildAudienceItemList(...)` + `localePath(locale, path)` helpers.
- `src/lib/constants/business.ts` — single source of truth for NAP.
- `src/lib/constants/navigation.ts` — single source of truth for nav IA.
- `src/hooks/useScrollState.ts` — client, rAF-throttled scroll boolean.
- `src/hooks/useBodyScrollLock.ts` — client, body scroll lock for the mobile drawer.
- `src/hooks/useConsent.ts` — **NEW (Phase 2.10)** thin React hook around `src/lib/analytics/consent.ts`. Returns `{state, accept, decline}`. First render returns `'unknown'` for SSR determinism; mount effect hydrates real value + subscribes to live updates.
- `src/lib/analytics/events.ts` — **NEW (Phase 2.10)** single source of truth for every dataLayer event Sunset Services fires (`ANALYTICS_EVENTS` constants, 25 entries) plus `PII_KEYS` set (13 forbidden payload keys). Cowork's GTM tags in Phase 2.10 Part B reference these wire values exactly.
- `src/lib/analytics/consent.ts` — **NEW (Phase 2.10)** pure module (no React) for consent state. Exports `getConsent/setConsent/subscribeConsent` + `ConsentState` type. Storage key `sunset_consent_v1`; CustomEvent name `sunset:consent-changed`.
- `src/lib/analytics/dataLayer.ts` — **NEW (Phase 2.10)** safe `window.dataLayer.push(...)` helper. Three guards: SSR (`typeof window`), kill switch (`NEXT_PUBLIC_ANALYTICS_ENABLED`), consent (`getConsent() === 'accepted'`). Plus defensive PII filter that strips any payload key in `PII_KEYS`.
- `src/components/analytics/ConsentBanner.tsx` — **NEW (Phase 2.10)** fixed-bottom cookie banner. 2-col desktop / stacked mobile, ≥48px buttons, iOS safe-area-inset padding. Hidden on `/request-quote/` (D17). Uses next-intl `useTranslations('chrome.consent')` so it must mount inside `NextIntlClientProvider`.
- `src/components/analytics/GTMScript.tsx` — **NEW (Phase 2.10)** client component. `next/script` (`afterInteractive`) loads GTM's canonical bootstrap snippet once env + consent gates are green.
- `src/components/analytics/GTMNoScript.tsx` — **NEW (Phase 2.10)** server component. Emits Google's mandatory `<noscript>` iframe at the top of `<body>`. Gated only by env vars (JS-disabled visitors can't see/dismiss the banner).
- `src/components/analytics/ClarityScript.tsx` — **NEW (Phase 2.10)** client component. Loads Microsoft Clarity's bootstrap snippet directly (not through GTM, per Decisions log) once env + consent gates are green.
- `src/components/analytics/AnalyticsBridge.tsx` — **NEW (Phase 2.10)** mounted once at layout level. Subscribes to 5 `sunset:*-event` CustomEvents on **`document`** (off-spec — plan said `window`; dispatchers fire on `document` without bubbles) and forwards `detail` to `pushDataLayer`. Pure relay; consent + PII gates live in `pushDataLayer`.
- `src/_project-state/` — this folder; living docs.
- `src/_project-state/Part-1-Phase-{01,02,03,04,05,07,09,10,12,14,16,18,20}-Completion.md` — Phase 1 completion reports. Phases 12/14/16/18/20 had completion reports pre-existing locally but weren't reflected in this file-map until Phase 2.01.
- `src/_project-state/Part-1-Phase-20-Smoke.md` — Phase 1.20 smoke-test results.
- `src/_project-state/Part-2-Phase-01-Completion.md` — **NEW (Phase 2.01)** Cowork completion report.
- `src/_project-state/Part-2-Phase-05-Completion.md` — **NEW (Phase 2.05)** completion report for the Sanity content-wiring phase.
- `src/_project-state/Part-2-Phase-07-Completion.md` — **NEW (Phase 2.07)** completion report for the Calendly embed phase.
- `src/_project-state/Part-2-Phase-10-Cowork-Handover.md` — **NEW (Phase 2.10)** Cowork Part A handover note (GTM/GA4/Clarity account IDs + GCP carryover commentary). Copied verbatim into worktree from the main tree where Cowork wrote it; audit trail for the three IDs that Phase 2.10 Code references.
- `src/_project-state/Part-2-Phase-10-Completion.md` — **NEW (Phase 2.10)** completion report for the analytics stack phase.
- `src/_project-state/Part-2-Phase-11-Completion.md` — **NEW (Phase 2.11)** completion report for the Spanish translation pass (source-file `[TBR]` upgrade + Sanity project/FAQ patches).
- `src/_project-state/Sunset-Services-Plan.md` — v2 in-progress rewrite (duplicates the root-level file; benign duplication, flagged in `00_stack-and-config.md`).
- `src/_project-state/Sunset-Services-Project-Instructions.md` — v2 project instructions (Phase 2.01 spec expects this at repo root, not here; deferred).

## src/assets/

- `src/assets/home/*.jpg` — Phase 1.07 home placeholders (hero, audience tiles, service tiles, project tiles, about portrait).
- `src/assets/audience/hero-{residential,commercial,hardscape}.jpg` — **NEW (1.09)** 16:9 audience hero placeholders, ~78KB each (noise-tuned).
- `src/assets/audience/projects/{residential|commercial|hardscape}-{1,2,3}.jpg` — **NEW (1.09)** 4:3 audience-landing project tile placeholders, ~3KB each.
- `src/assets/service/hero-{slug}.jpg` — **NEW (1.09)** 16:9 service hero placeholders for all 16 services, ~80KB each.
- `src/assets/service/tiles/{slug}.jpg` — **NEW (1.09)** 4:3 service-tile placeholders for the 16 audience-landing service grids, ~3KB each.
- `src/assets/service/projects/{slug}-{1,2,3}.jpg` — **NEW (1.09)** 4:3 service-detail featured-project tile placeholders, ~3KB each, ~33 total.

## scripts/

- `scripts/gen-home-placeholders.mjs` — Phase 1.07 home asset generator (sharp + gradient + per-pixel noise).
- `scripts/gen-audience-service-placeholders.mjs` — **NEW (1.09)** audience + service asset generator. Same sharp + gradient + noise pattern. Run via `node scripts/gen-audience-service-placeholders.mjs` from the repo root.
- `scripts/seed-sanity.mjs` — **NEW (Phase 2.05)** idempotent migration script. Reads `src/data/*.ts` via tsx-loaded dynamic imports, converts Markdown bodies to PortableText via `marked` → `jsdom` → `@portabletext/block-tools` `htmlToBlocks`, writes 158 documents to Sanity in 8 dependency-correct passes (services → locations → team → reviews → faqs → projects → resources → blog) using `createOrReplace` and deterministic `_id`s. Run via `npx tsx scripts/seed-sanity.mjs`.
- `scripts/translate-sanity-es.mjs` — **NEW (Phase 2.11)** Sanity ES translation script. Mirrors `seed-sanity.mjs` env+client architecture. Per-type CLI dispatch via `--type=<faq|project|service|location|team|review|blogPost|resourceArticle>`, supports `--dry-run` and `--id=<docId>`. Patches project ES fields (12 docs × ~5–8 fields) from Phase 2.11's inline translation map and normalizes 23 FAQ `[TBR]` markers from trailing-suffix to leading-prefix. Reports no-op-with-reason for the other 6 types (EN body null on services/locations/team, already-populated on review/blogPost/resourceArticle). Run via `npm run translate:sanity -- --type=project --dry-run`. **Phase B.01 leaves this script untouched** as the historical record of how the `[TBR]` convention was applied.
- `scripts/strip-tbr-sanity.mjs` — **NEW (Phase B.01)** one-shot Sanity cleanup. Walks every non-system document, recursively strips `[TBR]` markers (leading prefix, trailing suffix, mid-string) from any string field, and patches only top-level fields that actually changed. Idempotent by construction. Mirrors `translate-sanity-es.mjs` env-loader + Sanity client init. Supports `--dry-run`. Run via `node scripts/strip-tbr-sanity.mjs`.

## sanity/

- `sanity/schemas/` — **POPULATED (Phase 2.03)** 12 schema files. Re-exported via `index.ts`.
- `sanity/schemas/index.ts` — **NEW (Phase 2.03)** re-exports `schemaTypes` array — 4 object types first (referenced), then 8 document types. Imported by `sanity.config.ts`.
- `sanity/schemas/objects/index.ts` — **NEW (Phase 2.03)** re-exports the 4 shared localized field objects.
- `sanity/schemas/objects/localizedString.ts` — **NEW (Phase 2.03)** short bilingual strings (titles, deks, eyebrows, alt text). Fields: `en` (string, required), `es` (string).
- `sanity/schemas/objects/localizedText.ts` — **NEW (Phase 2.03)** multi-line bilingual prose. Fields: `en` (text, rows 4, required), `es` (text, rows 4).
- `sanity/schemas/objects/localizedBody.ts` — **NEW (Phase 2.03)** bilingual Portable Text (blog post body, resource article body). Each locale is an array of `block` + `image` (with hotspot, alt = `localizedString`, caption = `localizedString`).
- `sanity/schemas/objects/localizedSeo.ts` — **NEW (Phase 2.03)** bilingual SEO object: `title` (`localizedString`), `description` (`localizedText`). Collapsible/collapsed.
- `sanity/schemas/service.ts` — **NEW (Phase 2.03)** Service document. 14 fields across 4 groups (`content` / `media` / `taxonomy` / `seo`). Slug auto-derived from `title.en`. Conditional `priceIncludes` array hidden unless `pricingMode === 'price'`. References `faq` for `faqs`.
- `sanity/schemas/project.ts` — **NEW (Phase 2.03)** Project document. 20 fields across 5 groups (`content` / `media` / `beforeAfter` / `taxonomy` / `meta`). Inline `galleryEntry` array member (image + alt). Before/after fields hidden when `hasBeforeAfter` is false. References `service` (services) + `location` (city).
- `sanity/schemas/blogPost.ts` — **NEW (Phase 2.03)** Blog post document. Required body = `localizedBody`; required `publishedAt` (datetime); category enum mirrors `BlogCategory` in `src/data/blog.ts` (`how-to` / `cost-guide` / `seasonal` / `industry-news` / `audience`). Default author `'Erick Sotomayor'`. Ordering: `publishedAt desc`.
- `sanity/schemas/resourceArticle.ts` — **NEW (Phase 2.03)** Resource article document. Required body = `localizedBody`; category enum mirrors `ResourceCategory` (`lawn-care` / `hardscape` / `snow-and-winter` / `buying-guides` / `local-permits`); `schemaType` enum (`Article` / `HowTo`).
- `sanity/schemas/location.ts` — **NEW (Phase 2.03)** Location/city document. `name` not localized (proper noun); `featuredServices` array max 6; `trust` object (yearsServing/projectsCount/responseDays); `geo` object (lat/lng); `pin` object (x/y SVG coordinates inside the Phase 1.14 600×500 viewBox).
- `sanity/schemas/faq.ts` — **NEW (Phase 2.03)** FAQ document. Flat `scope` tag with custom validation: accepts `general` or `service:<slug>` / `audience:<slug>` / `city:<slug>` / `blog:<slug>` / `resource:<slug>`. Ordering: `(scope, order)`.
- `sanity/schemas/review.ts` — **NEW (Phase 2.03)** Review document. Required quote + attribution + rating (1–5 integer); `source` enum (`google` / `manual`); optional refs to `location` + `service`.
- `sanity/schemas/team.ts` — **NEW (Phase 2.03)** Team member document. `name` not localized; `role` (localizedString, required); `bio` (localizedText); credentials = array of strings.
- `sanity/lib/` — **POPULATED (Phase 2.05)** runtime helpers consumed by every Sanity-reading page.
- `sanity/lib/client.ts` — **NEW (Phase 2.03)** `createClient` from `next-sanity` with `useCdn: true`, `perspective: 'published'`, reads `NEXT_PUBLIC_SANITY_*` env vars.
- `sanity/lib/image.ts` — **NEW (Phase 2.03)** `urlFor(source)` helper wrapping `@sanity/image-url`. Typed via `Parameters<typeof builder.image>[0]`.
- `sanity/lib/queries.ts` — **NEW (Phase 2.05)** 14 GROQ query helpers covering every Sanity-read page. All bilingual fields return as `{en, es}` objects with EN-coalesced ES fallback baked in at the GROQ layer. Every fetch sets `next: { revalidate: 1800 }`.
- `sanity/lib/types.ts` — **NEW (Phase 2.05)** TypeScript return types for the query helpers (ProjectSummary/Detail, BlogPostSummary/Detail, ResourceSummary/Detail, FaqEntry, ReviewEntry, plus shared Localized + LocalizedBody + SanityImageAsset).
- `sanity/lib/write-client.ts` — **NEW (Phase 2.06)** server-only `createClient` with `useCdn: false` + `token: SANITY_API_WRITE_TOKEN`. Imported only from `src/app/api/quote/*` (never from a client component). Grep confirms zero `src/components/` imports.
- `sanity/schemas/quoteLead.ts` — **NEW (Phase 2.06)** Quote Lead document. 3 field groups (contact/project/meta). Required contact + audience + services + submittedAt. `details` is a flexible audience-conditional object. Preview shows `firstName lastName · audience · status · submittedAt`. Ordering: `submittedAt desc`.
- `sanity/schemas/quoteLeadPartial.ts` — **NEW (Phase 2.06)** Quote Lead — Abandoned document. Deterministic `_id` pattern `quoteLeadPartial-<sessionId>` so partial pushes upsert. Required sessionId + timestamps + lastStepReached. `converted: boolean` flag flipped by /api/quote when matching full lead lands. NEVER stores PII.
- `sanity/schemas/contactSubmission.ts` — **NEW (Phase 2.08)** Contact Submission document. 3 groups (contact / message / meta). Single `name` field (matches the ContactForm UI, not split first/last). Optional email + phone (form-side D14 lock; route mirrors as a server-side guard). `category` enum (residential/commercial/hardscape/other). Required submittedAt + sessionId + locale + status; ordering `submittedAt desc`.
- `sanity/schemas/newsletterSubscriber.ts` — **NEW (Phase 2.08)** Newsletter Subscriber document. Required email + subscribedAt + locale. Async unique-email validation (Studio editor safety net only — runs at publish time, NOT on API writes; the `/api/newsletter` route enforces uniqueness at write time). `unsubscribed` boolean + `unsubscribedAt` (hidden until `unsubscribed=true`). `sourcePage` records the pathname driving the signup. Ordering `subscribedAt desc`.
- `sanity/schemas/servicem8Event.ts` — **NEW (Phase 2.13)** ServiceM8 Event document. 3 groups (event/processing/meta). Event group: eventId, eventType, jobId, payload (raw JSON blob), signatureValid (always true at write time), receivedAt. Processing group: status enum (pending/processing/completed/failed/skipped), processedAt, processingError, relatedProjectId (ref to `project`), telegramApprovalState enum. Meta group: createdAt, lastUpdatedAt. Deterministic `_id` pattern `servicem8Event-<slug(eventId)>` enforced by the persist helper for idempotent replays. Preview `<eventType> — <jobId>` · `<status> · <receivedAt>`; ordering `receivedAt desc`.
- `sanity/schemas/index.ts` — **Modified (Phase 2.13)** registers `servicem8Event` in `schemaTypes`.
- `src/lib/servicem8/schema.ts` — **NEW (Phase 2.13)** Zod schema for the ServiceM8 webhook body. Minimum shape — `eventId`, `eventType: 'job.completed'` literal, `jobId`, ISO-8601 `occurredAt`, `data: z.record(z.unknown())`. Root uses default strip mode (off-spec from the plan's `.passthrough()` — see Decisions log entry 2026-05-14 for the Zod 3.25 type-inference reason).
- `src/lib/servicem8/verifySignature.ts` — **NEW (Phase 2.13)** `verifyServiceM8Signature(rawBody, signatureHeader, secret)`. HMAC-SHA256(rawBody, secret) hex digest; timing-safe compare with length-mismatch guard; never throws (any error returns false).
- `src/lib/servicem8/persistEvent.ts` — **NEW (Phase 2.13)** `persistServiceM8Event({eventId, eventType, jobId, payload, signatureValid, occurredAt})`. Deterministic `_id` via `slugifyEventId()`. Fetch-by-id; duplicate → return without writing; else create with status=`pending`, telegramApprovalState=`not_sent`, createdAt + lastUpdatedAt + receivedAt = now. `occurredAt` carried in the input but preserved in the `payload` blob (not stored as a separate field at this phase). Sanity errors propagate to the route handler.
- `src/app/api/webhooks/servicem8/route.ts` — **NEW (Phase 2.13)** POST handler. `runtime='nodejs'` + `dynamic='force-dynamic'`. Flag-off (`SERVICEM8_ENABLED!=='true'`) returns 200 + `{status:'simulated',reason:'feature-flag'}` with no body parse, no signature check, no Sanity write. Flag-on: raw-body read → `x-servicem8-signature` HMAC verify (401 + invalid-signature on fail) → JSON.parse (400 + invalid-json) → Zod validate (400 + invalid-payload, parse errors NOT echoed) → persistServiceM8Event (200 + sanityDocId on create, 200 + deduped:true on replay; opaque 500 + persist-failed on Sanity error).
- `scripts/test-servicem8-webhook.mjs` — **NEW (Phase 2.13)** synthetic verification harness. Spawns `next start` twice (flag-off + flag-on) and runs the 6 prescribed tests. Reusable; future webhook-route changes can re-run the same battery cheaply. Run via `node scripts/test-servicem8-webhook.mjs` after `npm run build`.
- `.env.local.example` — **Modified (Phase 2.13)** new comment block documenting `SERVICEM8_ENABLED` + `SERVICEM8_WEBHOOK_SECRET`. The orphan `SERVICEM8_ENABLED=false` line previously in the feature-flags block was consolidated into the new Phase 2.13 block.
- `Sunset-Services-Decisions.md` — **Modified (Phase 2.13)** appended 2 entries: (1) `2026-05-14 — Phase 2.12 (native Spanish review) deferred — Phase 2.13 runs next`; (2) `2026-05-14 — Phase 2.13: ServiceM8 Zod root schema dropped .passthrough()` (in-phase off-spec decision).
- `src/_project-state/Part-2-Phase-13-Completion.md` — **NEW (Phase 2.13)** completion report.
- `sanity/schemas/servicem8Event.ts` — **Modified (Phase 2.15)** added `telegramMessageId` field (number, optional, processing group). Used by Phase 2.16/2.17 callers to record the Telegram message ID alongside the event doc.
- `sanity/schemas/telegramApprovalLog.ts` — **NEW (Phase 2.15)** Telegram Approval Log document. Single `meta` group. Fields: kind enum (servicem8_portfolio / blog_draft), targetId, sentMessageId, sentChatId, sentAt, decision enum (pending/approve/reject, default pending), decidedAt, operatorChatId, rawCallbackData. Append-only audit trail; auto-generated `_id` (no dedup). Preview `<kind> — <targetId>` · `<decision> · <sentAt>`; ordering `sentAt desc`.
- `sanity/schemas/index.ts` — **Modified (Phase 2.15)** registers `telegramApprovalLog` in `schemaTypes`.
- `src/lib/telegram/types.ts` — **NEW (Phase 2.15)** Telegram Bot API TypeScript types. `TelegramInlineKeyboardButton`, `TelegramInlineKeyboardMarkup`, `TelegramSendMessageResponse`, `TelegramApiErrorResponse`, `TelegramCallbackQuery`, `TelegramUpdate`. Pure types; no runtime.
- `src/lib/telegram/client.ts` — **NEW (Phase 2.15)** low-level Bot API client. `sendMessage`, `answerCallbackQuery`, `editMessageReplyMarkup`. Flag-gated on `TELEGRAM_ENABLED==='true'` (flag-off logs `[telegram] simulated <method> ...` and returns `{ok:false, simulated:true}`). Flag-on POSTs JSON with 10-second `AbortSignal.timeout`. `TELEGRAM_API_BASE_URL` is the test seam (defaults to `https://api.telegram.org`).
- `src/lib/telegram/approvals.ts` — **NEW (Phase 2.15)** approval kind discriminator + callback_data codec. `ApprovalKind = 'servicem8_portfolio' | 'blog_draft'`. `encodeCallbackData` produces `<kindShort>:<targetId>:<action>` (kindShort = `'sm8'` or `'bd'`). `parseCallbackData` returns null on malformed input. `buildButtonsForKind('servicem8_portfolio', targetId)` returns Approve/Reject pair; `'blog_draft'` throws as Phase 2.16 extension point.
- `src/lib/telegram/persistLog.ts` — **NEW (Phase 2.15)** Sanity persist helper for `telegramApprovalLog`. `createApprovalLog` writes a pending row at send time; `recordDecision` patches operator tap (decision + decidedAt + operatorChatId + rawCallbackData). Both trap Sanity errors and return `{error}` so the route handler can branch.
- `src/lib/telegram/notify.ts` — **NEW (Phase 2.15)** high-level helpers. `notifyOperator({text, parseMode?})` sends a one-way info message to `TELEGRAM_OPERATOR_CHAT_ID`. `requestApproval({kind, targetId, summary})` composes a MarkdownV2 kind-labeled message, attaches the kind's button matrix, sends, and writes a pending audit row. Both helpers never throw — failures return `{sent:false}`. Successful send + failed log returns `{sent:true, messageId}` without `logDocId` and logs the gap for manual reconcile.
- `src/app/api/webhooks/telegram/route.ts` — **NEW (Phase 2.15)** POST handler. `runtime='nodejs'` + `dynamic='force-dynamic'`. Flag-off → 200 + `{status:'simulated',reason:'feature-flag'}`. Flag-on: `X-Telegram-Bot-Api-Secret-Token` verify via `crypto.timingSafeEqual` with length-mismatch guard (401 + invalid-secret) → JSON.parse (400 + invalid-json) → Zod-validate `callback_query` branch only (other update types 200 + ignored:unsupported-update-type) → `parseCallbackData` null → 200 + ignored:unparseable-callback-data → idempotency lookup by `telegramApprovalLog.sentMessageId` (200 + ignored:no-matching-log if missing, 200 + deduped:true if already decided) → route by kind. For `'servicem8_portfolio'`: patch event doc telegramApprovalState + processedAt, patch log row via recordDecision, editMessageReplyMarkup (remove buttons), answerCallbackQuery (clear spinner). Sanity/Telegram failures → 500 + opaque persist-failed.
- `src/app/api/test/telegram-notify/route.ts` — **NEW (Phase 2.15)** TEST INFRASTRUCTURE. Thin POST wrapper around `notifyOperator()` for the verification harness. Gated by `TELEGRAM_TEST_ROUTES_ENABLED==='true'` — returns 404 + `{status:'forbidden'}` in production + preview. MUST NOT be used by production callers (off-spec from plan's `_test` + NODE_ENV gate; see completion report).
- `src/app/api/test/telegram-approval/route.ts` — **NEW (Phase 2.15)** TEST INFRASTRUCTURE. Thin POST wrapper around `requestApproval()` for the verification harness. Same gate + caveats as telegram-notify.
- `scripts/setup-telegram-webhook.mjs` — **NEW (Phase 2.15)** one-time setWebhook registration. Reads `TELEGRAM_BOT_TOKEN` + `TELEGRAM_WEBHOOK_SECRET_TOKEN` from `.env.local`; takes a public webhook URL CLI arg; POSTs to `setWebhook` with `secret_token` + `allowed_updates: ['callback_query']`. Run ONCE per Vercel environment via `npm run telegram:setup -- <url>`.
- `scripts/get-telegram-webhook-info.mjs` — **NEW (Phase 2.15)** debug helper that prints `getWebhookInfo`. Run via `npm run telegram:info`.
- `scripts/test-telegram-bot.mjs` — **NEW (Phase 2.15)** verification harness. Spawns a local mock Telegram API on port 7891 + `next start` twice (Phase A flag-on with 8 tests; Phase B flag-off with 2 tests). Asserts on mock-captured request bodies + Sanity audit rows + servicem8Event patches. Cleans up test docs at the end. All 10 tests pass in Phase 2.15 verification.
- `.env.local.example` — **Modified (Phase 2.15)** new Phase 2.15 block at the bottom documenting `TELEGRAM_ENABLED` + `TELEGRAM_WEBHOOK_SECRET_TOKEN` + the optional `TELEGRAM_API_BASE_URL` test seam.
- `package.json` — **Modified (Phase 2.15)** added `telegram:setup` + `telegram:info` scripts pointing at the two new mjs files.
- `Sunset-Services-Decisions.md` — **Modified (Phase 2.15)** appended 1 entry: `2026-05-15 — Phase 2.14 deferred → Phase 2.15 runs next`.
- `src/_project-state/Part-2-Phase-15-Completion.md` — **NEW (Phase 2.15)** completion report.
- `vercel.json` — **NEW (Phase 2.16)** at repo root. Two `crons` entries: monthly blog at `0 14 1 * *`, weekly SEO at `0 14 * * 1`. Fills Hobby tier's 2-cron budget. Vercel auto-registers both on the next Production deploy.
- `public/images/blog/_placeholder.jpg` — **NEW (Phase 2.16)** 46 KB shared placeholder featured-image asset. Uploaded to Sanity on first cron run with `originalFilename: 'blogDefaultPlaceholder.jpg'`; reused by every auto-published `blogPost`. Cowork swaps for a curated brand-themed image in a parallel-track task.
- `sanity/schemas/blogDraftPending.ts` — **NEW (Phase 2.16)** Blog Draft (Pending) document type. Two groups (content / meta). Content: title (localizedString), dek (localizedText), body (localizedBody Portable Text), metaTitle + metaDescription (localizedString), faqsInline (array of `{q: localizedString, a: localizedText}` objects), categorySlug, slug. Meta: automatedTopicId, topicKeyword, modelUsed, generatedAt (readonly), status enum (pending/approved/rejected), processedAt, publishedBlogPostId, telegramMessageId, telegramApprovalLogId. Preview EN title with `<status> · <generatedAt>` subtitle; ordering `generatedAt desc`.
- `sanity/schemas/blogPost.ts` — **Modified (Phase 2.16)** added three optional readonly meta-group fields: `automatedTopicId`, `automatedGeneratedAt`, `automatedModelUsed`. Populated by Phase 2.16's `publishBlogDraft()` only; manual posts leave them empty. Existing seed posts unaffected. Also adds a new `meta` field group.
- `sanity/schemas/index.ts` — **Modified (Phase 2.16)** registers `blogDraftPending` in `schemaTypes`.
- `src/data/blogTopics.ts` — **NEW (Phase 2.16)** 20 curated SEO topics across 5 categories (residential / commercial / hardscape / location / seasonal). Each entry: stable kebab-case `id`, `keyword`, `category`, optional `cityHint`, `briefForModel`. Type exports: `BlogTopic`, `BlogTopicCategory`, `BlogTopicCityHint`.
- `src/lib/automation/blog/topicPicker.ts` — **NEW (Phase 2.16)** `pickNextTopic()` walks `BLOG_TOPICS` in declared order, returns the first id not yet present on `blogPost.automatedTopicId` OR non-rejected `blogDraftPending.meta.automatedTopicId`. Wraps to `BLOG_TOPICS[0]` when all consumed. Module-scope 60-second cache. Exports `_resetTopicPickerCacheForTests()` for the harness.
- `src/lib/automation/blog/draft.ts` — **NEW (Phase 2.16)** `generateBlogDraft(topic)` calls Anthropic (`claude-sonnet-4-6` default via `ANTHROPIC_MODEL` env) with a brand-voice system prompt + topic-specific user message. Returns a typed `BlogDraft` validated by Zod (title + dek + body blocks + meta + 3-5 FAQ Q&A pairs + kebab-case slug, all bilingual). Two-stage retry on malformed JSON or Zod failure; second failure throws. Type exports: `BlogDraft`, `BlogDraftBlock`.
- `src/lib/automation/blog/persistDraft.ts` — **NEW (Phase 2.16)** `persistBlogDraftPending(draft, topic, modelUsed)` writes a new `blogDraftPending` doc via the Phase 2.05 write client with deterministic-prefix UUID `_id` (`blogDraftPending-<uuid>`). Includes `toPortableTextBlocks(blocks)` — converts the model's structured `{type:'h2'|'p'|'ul'|'ol'}` blocks into Sanity Portable Text with per-block + per-child `_key` values. Returns `{docId}`.
- `src/lib/automation/blog/placeholderAsset.ts` — **NEW (Phase 2.16)** `ensurePlaceholderAsset()` returns the Sanity asset `_id` for the shared blog placeholder. First call reads `public/images/blog/_placeholder.jpg`, uploads via `writeClient.assets.upload` with `filename: 'blogDefaultPlaceholder.jpg'`; subsequent calls look up the existing asset by `originalFilename`. Module-scope memo. Off-spec note in the code: Sanity's upload API doesn't accept a custom `_id`, so lookup-by-filename replaces the plan's deterministic-id approach (equally stable). Exports `_resetPlaceholderCacheForTests()`.
- `src/lib/automation/blog/runMonthly.ts` — **NEW (Phase 2.16)** shared executor `executeMonthlyBlogDraftRun()`. Called by both `/api/cron/blog-draft-monthly` and `/api/test/blog-draft-run` so the harness exercises production code. Flow: flag check → time-based idempotency check (any pending blogDraftPending from last 1 day → noop) → pickNextTopic → generateBlogDraft → persistBlogDraftPending → requestApproval → patch doc with Telegram audit refs. Never throws; returns a discriminated-union `MonthlyRunResult` the route layer stringifies.
- `src/lib/automation/blog/publish.ts` — **NEW (Phase 2.16)** `publishBlogDraft(pendingDocId)` reads the pending doc, resolves placeholder asset, createOrReplaces scoped `faq` docs (`faq-blog-<slug>-<n>` IDs, scope `blog:<slug>`), creates the live `blogPost` with full content + faqs[] refs + placeholder featuredImage + automation meta fields + `publishedAt=now()`, patches the pending doc to `status='approved'` with `publishedBlogPostId`. `rejectBlogDraft(pendingDocId)` patches the pending doc to `status='rejected'` with `processedAt`. Both throw if the doc is missing or already processed (webhook layer's idempotency check prevents repeated execution).
- `src/lib/automation/gsc/fetch.ts` — **NEW (Phase 2.16)** `fetchWeeklySeoSummary()` typed signature for the weekly Google Search Console digest. Body is a TODO guarded by an internal `GSC_ENABLED==='true'` flag check (defense-in-depth). Implementation lands at Phase 3.15 (read service-account JSON, instantiate google-auth-library JWT, call `searchanalytics/query` twice for top queries + top pages). Type exports: `WeeklySeoSummary`, `WeeklySeoQueryRow`, `WeeklySeoPageRow`.
- `src/lib/automation/gsc/summarize.ts` — **NEW (Phase 2.16)** `formatSeoSummaryMessage(data)` renders the typed summary as a MarkdownV2 Telegram body under 4096 chars. Includes totals + top 10 queries + top 10 pages. Ready for the Phase 3.15 flag flip.
- `src/lib/automation/cronAuth.ts` — **NEW (Phase 2.16)** `verifyCronAuth(authHeader, expectedSecret)` Bearer-token comparator with `crypto.timingSafeEqual` + length-mismatch guard. Mirrors Phase 2.15's Telegram secret-verify pattern. Used by both cron routes and the test-blog-draft-run route (with a different secret).
- `src/lib/telegram/markdownV2.ts` — **NEW (Phase 2.16)** `escapeMarkdownV2(text)` escapes Telegram MarkdownV2's strict special characters (`_*[]()~`>#+-=|{}.!\\`) so user/model-derived content can be safely spliced into a MarkdownV2 message body.
- `src/lib/telegram/approvals.ts` — **Modified (Phase 2.16)** ships the `'blog_draft'` button set (Approve & publish / Reject pair). Closes the Phase 2.15 TODO throw. Switch is now exhaustive over `ApprovalKind` with defensive `never` fallback.
- `src/lib/telegram/client.ts` — **Modified (Phase 2.16)** extends `sendMessage()` with optional `replyToMessageId`. Sends Telegram Bot API's `reply_parameters` field so the blog_draft webhook handler's publish/reject confirmation threads visually below the original approval card.
- `src/app/api/cron/blog-draft-monthly/route.ts` — **NEW (Phase 2.16)** Vercel Cron entry point. POST. `runtime='nodejs'` + `dynamic='force-dynamic'`. Auth check then delegates to `executeMonthlyBlogDraftRun()`. Status mapping: simulated/noop/ok → 200, error → 500.
- `src/app/api/cron/seo-summary-weekly/route.ts` — **NEW (Phase 2.16)** second Vercel Cron entry. POST. Same auth pattern. Flag-off (`GSC_ENABLED!=='true'`) → 200 + simulated. Flag-on: `fetchWeeklySeoSummary()` → `formatSeoSummaryMessage()` → `notifyOperator(message, parseMode:'MarkdownV2')`.
- `src/app/api/webhooks/telegram/route.ts` — **Modified (Phase 2.16)** replaces the Phase 2.15 `'blog_draft'` placeholder branch with the real handler. On Approve: `publishBlogDraft(targetId)` → recordDecision → editMessageReplyMarkup → answerCallbackQuery → sendMessage(reply-threaded "✅ Published as blog post `<id>`"). On Reject: `rejectBlogDraft(targetId)` → recordDecision → edit → ack → "✋ Draft rejected. Topic returns to the rotation." follow-up. Errors → 500 + opaque persist-failed.
- `src/app/api/test/blog-draft-run/route.ts` — **NEW (Phase 2.16)** TEST INFRASTRUCTURE. Drives `executeMonthlyBlogDraftRun()` with the separate `TEST_ROUTES_SECRET`. Optional `?reset=true` clears the topic-picker + placeholder-asset caches. Gated by `BLOG_AUTOMATION_TEST_ROUTES_ENABLED==='true'` (unset on Vercel — ships dead).
- `src/app/api/test/blog-draft-decision/route.ts` — **NEW (Phase 2.16)** TEST INFRASTRUCTURE. POSTs `{pendingDocId, decision: 'approve'|'reject'}` invoke `publishBlogDraft` / `rejectBlogDraft` directly, bypassing the Telegram webhook for isolated publish/reject verification. Same gate.
- `scripts/test-blog-automation.mjs` — **NEW (Phase 2.16)** synthetic verification harness. Spawns a local mock Telegram API on port 7892 + `next start` (thrice; flag-on / flag-off / flag-on) with full Phase 2.15 + 2.16 env wiring. Pre-run cleanup sweeps stale test docs. 10 tests: cron auth, flag-off, end-to-end happy path (real Anthropic ~$0.04), idempotency, Approve handler, Reject handler, placeholder asset reuse, topic rotation (real Anthropic ~$0.04), weekly SEO auth, weekly SEO flag-off. All pass. Cleans up tracked docs at end.
- `.env.local.example` — **Modified (Phase 2.16)** new Phase 2.16 block at the bottom: `CRON_SECRET`, `BLOG_DRAFT_CRON_ENABLED`, `GSC_ENABLED`, `GSC_SITE_URL`, commented-out test-only `BLOG_AUTOMATION_TEST_ROUTES_ENABLED` + `TEST_ROUTES_SECRET`.
- `Sunset-Services-Decisions.md` — **Modified (Phase 2.16)** appended 2 entries: (1) `2026-05-15 — Phase 2.16 plan-of-record (automation agent Part A)`; (2) `2026-05-15 — Phase 2.16: monthly cron idempotency went time-based, not per-topic` (in-phase off-spec decision).
- `src/_project-state/Part-2-Phase-16-Completion.md` — **NEW (Phase 2.16)** completion report.
- `sanity/schemas/portfolioDraftPending.ts` — **NEW (Phase 2.17)** Portfolio Draft (Pending) document type. Two field groups (content / meta). Content: title (localizedString), dek (localizedText), body (localizedBody Portable Text), audience enum (residential / commercial / hardscape), serviceSlug (string), locationSlug (string), featuredImage (image), gallery (array of images), proposedSlug (slug). Meta: sourceEventId (the ServiceM8-supplied eventId; idempotency key), sourceEventDocId (Sanity _id of source servicem8Event), modelUsed, generatedAt (readonly), status enum (pending/approved/rejected), processedAt, publishedProjectId, gbpUploadResult (string snapshot of the GBP write-leg outcome), photoStats (off-spec; object: {uploaded, failed}), telegramMessageId, telegramApprovalLogId. Preview EN title with `<status> · <generatedAt>` subtitle; ordering `generatedAt desc`.
- `sanity/schemas/project.ts` — **Modified (Phase 2.17)** added a new readonly "Automation" field group + three optional fields (`automatedSourceEventId`, `automatedGeneratedAt`, `automatedModelUsed`). Populated only by Phase 2.17's `publishPortfolioDraft()`; manual projects authored in Studio leave them empty. Existing seed projects unaffected.
- `sanity/schemas/index.ts` — **Modified (Phase 2.17)** registers `portfolioDraftPending` in `schemaTypes`.
- `src/lib/automation/portfolio/extractJobMetadata.ts` — **NEW (Phase 2.17)** `extractJobMetadata(eventPayload: unknown): JobMetadata` defensively projects the ServiceM8 payload into structured metadata. Lookup orders: jobUuid via `payload.data.uuid → job_uuid → id`, description via `payload.data.job_description → description`, address via `payload.data.job_address → address`, attachmentUrls via `payload.data.attachments[].url` filtered to entries with a `url` string. Inference heuristics: location via substring-match against `LOCATIONS[].name` from `src/data/locations.ts` (case-insensitive); audience via keyword scan (commercial / hardscape / residential keyword lists); service via keyword-to-slug map first-match against real slugs from `src/data/services.ts`. Never throws; every field can be null.
- `src/lib/automation/portfolio/uploadPhotos.ts` — **NEW (Phase 2.17)** `uploadJobPhotos(urls: string[]): Promise<PhotoUploadResult>` walks the URL list, downloads each photo with `AbortSignal.timeout(15_000)` + 10 MB size cap + content-length pre-check, and uploads successes to Sanity via `writeClient.assets.upload('image', buffer, {filename, contentType})`. Returns `{uploaded: Array<{url, assetId, assetRef}>, failed: Array<{url, reason}>}`. Failures (non-2xx, network error, oversize, malformed image) land in `failed[]` with a reason string; pipeline continues. Empty input → empty result.
- `src/lib/automation/portfolio/draft.ts` — **NEW (Phase 2.17)** `generatePortfolioDraft({jobMetadata, photoCount, modelOverride?}): Promise<PortfolioDraft>` calls Anthropic Sonnet 4.6 (env-flippable via `ANTHROPIC_PORTFOLIO_MODEL` → `ANTHROPIC_MODEL` → hardcoded default). System prompt covers brand voice + bilingual rules ([TBR]-prefix on ES) + enum whitelists (audience / serviceSlug from `SERVICES` / locationSlug from `LOCATION_SLUGS`). Zod schema validates output. Two robustness layers: balance-braces JSON extractor handles trailing prose; server-side `backfillTaxonomy()` merges inferred audience/serviceSlug/locationSlug onto the model output if omitted. Two-stage retry on Zod failure; second failure throws. `proposedSlug` derived from `title.en` in code (kebab-case, ≤ 60 chars). Type exports: `PortfolioDraft`, `PortfolioDraftBlock`.
- `src/lib/automation/portfolio/persistDraft.ts` — **NEW (Phase 2.17)** `persistPortfolioDraft({draft, jobMetadata, sourceEventId, sourceEventDocId, modelUsed, featuredImage, gallery, photoStats?})` writes a new `portfolioDraftPending` doc with deterministic-prefix UUID `_id` (`portfolioDraftPending-<uuid>`). Uses the shared `toPortableTextBlocks` helper for EN+ES body conversion. Attaches `_key` per gallery image. `telegramMessageId` + `telegramApprovalLogId` set later by the orchestrator after the Telegram send. Returns `{docId}`.
- `src/lib/automation/portfolio/runPipeline.ts` — **NEW (Phase 2.17)** `runPortfolioDraftPipeline(eventDocId: string): Promise<PipelineResult>` is the on-demand orchestrator. Flow: flag check (`PORTFOLIO_DRAFT_ENABLED='true'`) → source event fetch → source-event-already-handled noop (state ≠ `'not_sent'`) → time-based idempotency (any pending with same sourceEventId, generatedAt > 1 day ago) → metadata extraction → photo download/upload best-effort → featured image fallback (first uploaded asset OR Phase 2.16 placeholder) → Anthropic draft → Sanity persist → MarkdownV2 Telegram approval send → telegramMessageId+logId patch → source event state flip to 'pending'. Errors trigger best-effort `notifyOperator` + rethrow.
- `src/lib/automation/portfolio/gbpPublish.ts` — **NEW (Phase 2.17)** GBP write-leg STUBS deferred to Phase 2.17a. Exports `uploadPhotosToGbp({assetUrls, caption})` and `createGoogleBusinessPost({title, body, primaryPhotoAssetUrl, ctaUrl})` — both gate on `GBP_PORTFOLIO_PUBLISH_ENABLED='true'` AND credential presence (`GBP_OAUTH_CLIENT_ID` + `GBP_OAUTH_CLIENT_SECRET` + `GBP_OAUTH_REFRESH_TOKEN`); both return `{skipped:true,reason:'gbp-deferred'}` at Phase 2.17 defaults. TODO comment block points at Phase 2.17a. Also exports `summarizeGbpResult(result)` helper that converts the result to a short audit-string for the pending doc's `gbpUploadResult` field.
- `src/lib/automation/portfolio/publish.ts` — **NEW (Phase 2.17)** `publishPortfolioDraft(pendingDocId)` reads the pending doc, resolves featuredImage (with placeholder fallback), `createOrReplace`s the live `project` doc with deterministic `_id` (`project-<proposedSlug>`), best-effort GROQ lookup of `services[]` (by slug + audience) and `city` (by slug) — skipped on miss (operator finishes in Studio). Maps draft `dek`→`shortDek` (localizedString), body PortableText→`narrative` (flattened to localizedText via `joinBodyToText`), `title`→`leadAlt`. Sets `publishedAt` + the three `automated*` meta fields. Patches the pending doc to `status='approved'` with `publishedProjectId`. Flips source event's `telegramApprovalState` to `'approved'`. Calls the GBP write stubs and patches `meta.gbpUploadResult` with the summary string. `rejectPortfolioDraft(pendingDocId)` flips both the pending doc and source event to `'rejected'`. Both throw if doc missing or already processed.
- `src/lib/automation/shared/toPortableTextBlocks.ts` — **NEW (Phase 2.17)** factored out from Phase 2.16's blog drafter. Exports `toPortableTextBlocks(blocks)` + `makeKey()` (UUID-based 12-char keys). Used by both blog and portfolio persist helpers — same shape PortableText output across both kinds.
- `src/lib/automation/blog/persistDraft.ts` — **Modified (Phase 2.17)** imports `toPortableTextBlocks` + `makeKey` from the new shared module instead of defining them inline. Re-exports `toPortableTextBlocks` for backwards-compat with any code path that imported it from this file. No behavioral change.
- `src/app/api/webhooks/servicem8/route.ts` — **Modified (Phase 2.17)** imports `after` from `next/server` + `runPortfolioDraftPipeline`. After a successful event persist (non-duplicate path), registers an `after()` callback that fires `runPortfolioDraftPipeline(persisted.sanityDocId)`. The webhook response (200 + sanityDocId) is sent BEFORE the callback runs — ServiceM8 never waits on the ~30–60-second Anthropic call. Pipeline's flag check (`PORTFOLIO_DRAFT_ENABLED!=='true'`) short-circuits when the flag is off, so the callback is always safe to register.
- `src/app/api/webhooks/telegram/route.ts` — **Modified (Phase 2.17)** replaces the Phase 2.15 `'servicem8_portfolio'` stub branch with the real Phase 2.17 implementation. `targetId` is now the `portfolioDraftPending._id` (not the source event _id). On Approve: `publishPortfolioDraft(targetId)` → `recordDecision('approve')` → `editMessageReplyMarkup` → `answerCallbackQuery('Published.')` → `sendMessage` (reply-threaded "✅ Published portfolio entry `<projectId>`" + GBP-status line). On Reject: `rejectPortfolioDraft(targetId)` → `recordDecision('reject')` → edit → ack → "✋ Portfolio draft rejected." follow-up. Errors → `notifyOperator` alert + 200 (NOT 500) to suppress Telegram retries against known-bad state. Also imports `publishPortfolioDraft` + `rejectPortfolioDraft` + `notifyOperator`.
- `src/app/api/test/portfolio-pipeline-run/route.ts` — **NEW (Phase 2.17)** TEST INFRASTRUCTURE. POSTs `{eventDocId}` invoke `runPortfolioDraftPipeline` directly — bypasses the webhook signature + auth flow so the harness can drive against synthetic `servicem8Event` docs. Supports `?probe=extract` query mode that runs `extractJobMetadata` against the body's `payload` field and returns the metadata (lets the harness unit-test the extractor without a 3rd test route). Gated by `PORTFOLIO_AUTOMATION_TEST_ROUTES_ENABLED='true'` (unset on Vercel — ships dead). Auth via `Bearer ${TEST_ROUTES_SECRET}` (reuses Phase 2.16 secret — no new shared secret).
- `src/app/api/test/portfolio-decision/route.ts` — **NEW (Phase 2.17)** TEST INFRASTRUCTURE. POSTs `{pendingDocId, decision: 'approve'|'reject'}` invoke `publishPortfolioDraft` / `rejectPortfolioDraft` directly, bypassing the Telegram webhook. Same flag + auth gate.
- `scripts/test-portfolio-automation.mjs` — **NEW (Phase 2.17)** synthetic verification harness. Spawns a local mock Telegram API on port 7893 + `next start` (multiple flag configurations across tests). Pre-run cleanup sweeps test-prefixed docs. 12 tests pass: test-route auth, test-route flag-off, webhook trigger short-circuit, end-to-end happy path with 2 photos (real Anthropic), end-to-end happy path with 0 photos (placeholder fallback), idempotency replay, source-event-already-handled noop, Approve handler, Reject handler, GBP stubs skipped, photo download best-effort (1 good + 1 404 via httpstat.us), metadata extractor probe (4 hand-crafted payloads). Cleans up tracked docs at end. Total Anthropic cost per run: ~$0.20.
- `.env.local.example` — **Modified (Phase 2.17)** new Phase 2.17 block: `PORTFOLIO_DRAFT_ENABLED`, `GBP_PORTFOLIO_PUBLISH_ENABLED`, commented-out test-only `PORTFOLIO_AUTOMATION_TEST_ROUTES_ENABLED`. Reuses Phase 2.16's `TEST_ROUTES_SECRET`.
- `Sunset-Services-Decisions.md` — **Modified (Phase 2.17)** appended 2 entries: (1) `2026-05-15 — Phase 2.17 plan-of-record (automation agent Part B)`; (2) `2026-05-15 — Phase 2.17: in-phase off-spec additions` (photoStats field, taxonomy backfill, robust JSON extraction, adaptive project schema mapping, probe-mode test route).
- `src/_project-state/Part-2-Phase-17-Completion.md` — **NEW (Phase 2.17)** completion report.

## Phase B.03 — Legal pages + Cookie banner + Consent Mode v2 (added 2026-05-15)

- `src/types/consent.ts` — **NEW (Phase B.03)** `ConsentSignals` (4 categories: necessary | analytics | marketing | personalization) + `ConsentState` (`'pending'` | `'decided' + signals + decidedAt`). Exports `DEFAULT_PREFERENCES` (DM-2 defaults: necessary ON, analytics ON, marketing OFF, personalization OFF), `ACCEPT_ALL`, `REJECT_ALL` constants.
- `src/lib/analytics/consent.ts` — **Refactored (Phase B.03)** from Phase 2.10's binary `'accepted'|'declined'|'unknown'` schema to the 4-category `ConsentState` JSON-serialized in `localStorage['sunset_consent_v2']`. Adds `getEffectiveSignals(state)` helper + a one-shot v1 → v2 migration (any pre-existing `sunset_consent_v1` is deleted on first read; visitor is re-prompted with the new banner). `sunset:consent-changed` CustomEvent name preserved from 2.10 so subscribers keep working.
- `src/hooks/useConsent.ts` — **Refactored (Phase B.03)** migrated to `useSyncExternalStore` (React 19 best practice; eliminates the Phase 2.10 setState-in-effect pattern). Returns `{state, acceptAll, rejectAll, save}` instead of `{state, accept, decline}`.
- `src/lib/analytics/dataLayer.ts` — **Refactored (Phase B.03)** `pushDataLayer` now picks an event category (analytics | marketing | personalization) from the event name and gates on the corresponding signal in `getEffectiveSignals(getConsent())`. Adds `pushConsentUpdate({analytics, marketing, personalization})` which emits BOTH the gtag-arguments-shape row (`{0: 'consent', 1: 'update', 2: {...}, length: 3}` — for GTM's built-in Consent Mode v2 template) AND a named `consent_update` event row (for custom GTM triggers + the B.03 smoke tests). DM-1 mapping: personalization → ad_user_data + ad_personalization. Marketing + Personalization event sets are scaffolded empty for future phases.
- `src/lib/analytics/events.ts` — **Modified (Phase B.03)** added `CONSENT_UPDATE: 'consent_update'` to the `ANALYTICS_EVENTS` table.
- `src/components/analytics/ConsentModeDefault.tsx` — **NEW (Phase B.03)** server component that emits a plain inline `<script>` in `<head>` running `gtag('consent','default',{...four signals 'denied', wait_for_update: 500})` BEFORE GTM loads. App Router pattern (not `next/script beforeInteractive` — that strategy is `pages/_document.js`-only).
- `src/components/analytics/GTMScript.tsx` — **Refactored (Phase B.03)** loads regardless of consent state (was gated on `state === 'accepted'` in 2.10). On every consent decision change, fires `pushConsentUpdate()` to flip the four Consent Mode v2 signals. Uses a ref to deduplicate same-signals re-fires.
- `src/components/analytics/ClarityScript.tsx` — **Refactored (Phase B.03)** mounts the Clarity snippet only after `signals.analytics === true`; on every analytics-signal flip, calls `clarity('consent', signals.analytics)` so Clarity respects its own consent flag in addition to the script-load gate.
- `src/components/analytics/ConsentBanner.tsx` — **Refactored (Phase B.03)** replaces Phase 2.10's binary 2-button banner with a 3-button banner (Accept all / Reject all / Manage preferences) + Privacy policy link. Bottom slide-up via `motion/react` spring (~600ms delay), opaque `--color-bg-cream` surface, `1px var(--color-border-strong)` top border, `0 -4px 16px rgba(26,26,26,0.08)` top shadow. Hand-rolled focus trap between the 4 interactive children at `document`-capture-phase keydown; ESC swallowed (no implicit consent). Hidden on `/request-quote/` (D17 preserved). Mounts `<ConsentPreferencesModal>` as a sibling and routes the Manage button to open it.
- `src/components/analytics/ConsentPreferencesModal.tsx` — **NEW (Phase B.03)** base-ui Dialog + base-ui Switch primitives. Four toggle rows (Strictly necessary locked ON + dimmed + "Always on" label; Analytics; Marketing; Personalization). Inner `<PreferencesForm>` child component mounts only while `open === true` so the draft state re-seeds from the latest saved consent every open cycle (no setState-in-effect dance). Backdrop click-dismissal swallowed via `onOpenChange` reason check; ESC closes modal (banner re-appears). `motion/react` opacity+scale entry; `MotionConfig reducedMotion="user"` from `<MotionRoot>` swaps to opacity-only with prefers-reduced-motion.
- `src/components/ui/Dialog.tsx` — **NEW (Phase B.03)** thin wrapper re-exporting base-ui Dialog primitives (Root, Trigger, Portal, Backdrop, Popup, Title, Description, Close) with the project's `.dialog` + `.dialog-backdrop` classes pre-applied to Popup + Backdrop. Centralizes the import path so callers don't reach into `@base-ui/react` directly.
- `src/components/legal/TermlyPolicyEmbed.tsx` — **NEW (Phase B.03)** client component that mounts Termly's embed script (`https://app.termly.io/embed-policy.min.js`) via `next/script afterInteractive`, renders the targeted `<div name="termly-embed" data-id={docId} data-type="iframe">`, and overlays a first-pass `<style>` block hitting Termly's documented default selectors (`.termly-embed-wrap h1/h2/h3`, `p`, `a`, `ul/ol`) to apply project typography. When the relevant per-locale env var is empty, renders a graceful "Legal content is being prepared" fallback card instead of a broken empty embed.
- `src/components/legal/LegalPageHero.tsx` — **NEW (Phase B.03)** shared light cream-band hero for Privacy + Terms pages. Breadcrumb (light variant) + eyebrow + H1 + "Last updated" subtitle. No photo (legal pages are document-style).
- `src/components/legal/LegalPageBody.tsx` — **NEW (Phase B.03)** shared two-column body: TOC sidebar slot at `lg:` breakpoint (empty at B.03, populated in B.04 when Termly docs land) + `<TermlyPolicyEmbed>` content.
- `src/app/[locale]/privacy/page.tsx` — **NEW (Phase B.03)** server component. Renders LegalPageHero + LegalPageBody. WebPage + BreadcrumbList JSON-LD. (Note: brief specified `(marketing)` route group; flat routing matches the project convention since no other page uses route groups.)
- `src/app/[locale]/terms/page.tsx` — **NEW (Phase B.03)** server component. Same shape as privacy page; WebPage `name` mapped to the Terms page title.
- `src/app/[locale]/layout.tsx` — **Modified (Phase B.03)** added `<ConsentModeDefault />` inside `<head>` before the existing LocalBusiness JSON-LD `<script>`. Phase 2.10's other analytics mount points (GTMNoScript at top of body, ConsentBanner inside NextIntlClientProvider, AnalyticsBridge + GTMScript + ClarityScript outside the provider) preserved.
- `src/messages/en.json` — **Modified (Phase B.03)** rewrote `chrome.consent.{heading, body}` to mention granular control. Added `chrome.consent.banner.{rejectAllCta, manageCta, privacyLink}` + `chrome.consent.modal.*` (15 keys covering title, intro, closeAria, rowsAriaLabel, cancelCta, saveCta, and 4 category rows each with label + description, plus the necessary `alwaysOn` label). Added new `legal.*` namespace: `breadcrumb.{home, privacy, terms}`, `embed.preparingFallback`, `privacy.{meta.title, meta.description, hero.{eyebrow, h1, lastUpdated}}`, and the same shape under `terms.*`.
- `src/messages/es.json` — **Modified (Phase B.03)** same key set as en.json with first-pass `usted`-register ES translations. Flagged for M.03 review in `Sunset-Services-TRANSLATION_NOTES.md`.
- `.env.local.example` — **Modified (Phase B.03)** new comment block documenting the 5 Termly env vars (`NEXT_PUBLIC_TERMLY_WEBSITE_ID`, `NEXT_PUBLIC_TERMLY_{PRIVACY,TERMS}_{EN,ES}_ID`). All ship empty; Cowork populates in Phase B.04.
- `package.json` — **Modified (Phase B.03)** added `test:consent` script entry → `node scripts/test-consent-mode-v2.mjs`.
- `scripts/test-consent-mode-v2.mjs` — **NEW (Phase B.03)** jsdom-based 6-spec / 23-assertion smoke test harness covering the v1 → v2 migration, the consent_update payload shape (Accept all / Reject all / Save analytics-only), DM-1 personalization mapping (controls both ad_user_data + ad_personalization), and the `pushDataLayer` per-category gate. Re-implements the runtime under test inline so the harness doubles as a SPEC contract independent of the source modules.
- `Sunset-Services-Decisions.md` — **Modified (Phase B.03)** appended `2026-05-15 — Phase B.03 (Code) — Legal pages + Consent Mode v2 banner + modal` entry with 8 in-phase improvisations (missing handover, DM ratification gap, routing-convention deviation, file-naming difference, focus-trap implementation, TOC sidebar deferral, Termly CSS overrides best-effort, first-pass copy strings) plus 2 spec-load-bearing technical decisions (App-Router-friendly `<script>` for Consent Mode default; `useSyncExternalStore` for React 19 lint compliance).
- `Sunset-Services-TRANSLATION_NOTES.md` — **Modified (Phase B.03)** appended a "Phase B.03 — Cookie banner + modal + legal page chrome" section documenting the 26 new ES strings, the `usted` register choice, native-review priority items (modal category descriptions, legal page meta descriptions), and 4 open questions for M.03 (Política vs Aviso, Términos del servicio vs y condiciones, "Cookies" loanword choice, gender of "necesarias").
- `src/_project-state/Phase-B-03-Completion.md` — **NEW (Phase B.03)** completion report (this file's companion).
- `src/_project-state/Phase-B-03c-Completion.md` — **NEW (Phase B.03c)** completion report. PARTIAL SHIP: 2 of 5 Termly env vars populated on Vercel (`NEXT_PUBLIC_TERMLY_WEBSITE_ID` env-var id `gDmL9bWsmwK6gQWL` + `NEXT_PUBLIC_TERMLY_PRIVACY_EN_ID` env-var id `SRl4LL06YQVb5CAa`, both type=plain, both production + preview). 3 doc IDs (`PRIVACY_ES`, `TERMS_EN`, `TERMS_ES`) still empty — Cowork to supply. Records format concern: PRIVACY_EN_ID=`13687462` is numeric, not UUID (likely iubenda residue). Records architectural finding: `TermlyPolicyEmbed.tsx`'s `data-type="iframe"` config blocks both document-level CSS overrides AND the brief's Step 6 TOC heading-walk strategy via same-origin policy — Path A vs Path B decision pending Chat. Records token-refresh learning for future Vercel REST API calls. No source code modified. Empirical /en/privacy/ verification appended in section once Preview deploy completes. Also see the appended `Sunset-Services-Decisions.md` 2026-05-15 — Phase B.03c entry.
- `.env.local.example` — **Modified (Phase B.03c)** Phase B.03 block updated with a B.03c partial-populate header documenting the 2/5 state, the format concern, and the fallback consequence for the 3 empty routes. The actual var values stay empty (`.env.local.example` is committed — real values go in gitignored `.env.local`).
- `.env.local` — **NEW (Phase B.03c, gitignored)** worktree-local `.env.local` carrying the 2 Termly IDs upserted on Vercel. Matches `.gitignore:34` `.env*` rule.
- `src/_project-state/current-state.md` — **Modified (Phase B.03)** new most-recent-phase line; prior chain renumbered (2.17 → prior; 2.16 → prior 2; 2.15 → prior 3; 2.13 → prior 4; 2.11 → prior 5; 2.10 dropped from numbered list and now appears as a superseded-by-B.03 note instead).

## dist/ + .sanity/ (gitignored)

- `dist/` — **gitignored** Sanity Studio production build output (~7.9MB, written by `npm run studio:build`).
- `.sanity/` — **gitignored** Sanity dev-server runtime cache (`runtime/` subdirectory).
