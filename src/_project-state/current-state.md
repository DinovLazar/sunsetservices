# Current State

> **Live snapshot of the repo.** Updated end of every phase. If this file disagrees with the live code, the live code wins (per `Sunset-Services-Project-Instructions.md` §8).

---

## Where we are

- **Current phase (branch `phase/b18-portfolio-first-story-projects`, NOT merged; PR open):** **Phase B.18 Code — reusable portfolio importer + first-project scaffolding.** Shipped a committed, dry-run-default, idempotent, manifest-validating, publish-gated, self-verifying importer **`scripts/import-project-folder.mjs`** (one staged Drive job folder → one Sanity `project` **draft**): SHA-256 dedupe, HEIC decode (sharp-HEIF→`heic-convert`→ImageMagick), `.rotate()` auto-orient → 2400px `fit:inside` → q82 mozjpeg → **all EXIF/GPS stripped + strip re-verified before any upload** (D7), BG-01 §9.4 rename, `_processed/index.json` worksheet, deterministic `_id`, asset-lock reuse (`docs/project-import/asset-lock.json`), no-op on unchanged manifest, GROQ slug-collision guard, omit-never-null, `--publish` gated on `clientPhotoPermission`+`citySlug`+`year` (+B.08 revalidate reminder), post-write self-verify table. Plus `docs/project-import/{README.md, manifests/{providence-pointe,wilshire-wojdyla}.json (empty templates — copy pending), asset-lock.json, screenshots/}` and a `TRANSLATION_NOTES §B.18`. **`validate-seo.mjs` stale `scott-and-sarahs`→`granite-fusion-walkway-seating-wall`** (verified vs live Sanity). **Four ground-truth corrections surfaced (code wins):** (1) the **M.18/PSS-002 "story layout" the brief targets does not exist in this repo** — the `project` schema is the legacy shape (`narrative`/`gallery`/`facts`; no `hasStory`, `overview`, story sections, story components, `project.story.*` i18n, or `galleryEntry.ts`/`projectBlocks.ts`), so the importer targets the **legacy schema** and a real story layout is a prerequisite phase; (2) the staged photos are **not on this macOS host** (brief's `C:\sunset-photos\import\` absent); (3) **no `SANITY_API_WRITE_TOKEN`** in `.env.local`; (4) the brief's reference script `hotfix-scott-sarah-project.mjs` is absent (modeled on `upload-m01c-photos.mjs`). Per §2 stop-conditions **nothing was written to Sanity, no photos processed, manifests left as empty templates** — the two live pages await the photos + token on the appropriate host. **Verified:** `build` exit 0 (**202/202**, baseline unchanged — no new routes) · `lint` 0 err (9 pre-existing warnings) · `tsc` 0 err · importer exercised end-to-end on synthetic fixtures (dedupe, `sharp-heif` HEIC decode, classification, **EXIF strip 0-residual on all outputs incl. a GPS-bearing input**, fail-closed manifest validation, token guard, missing-folder §2 stop, idempotent reprocess-skip) · published project count still **10**, **0 drafts** visible · `src/messages/*` untouched (EN/ES leaf parity unchanged). User chose **"build for the real schema"** + **"build code scaffolding now"** after the corrections were surfaced. Full detail in `Phase-B-18-Code-Completion.md`; decisions logged 2026-07-23. **User action to finish B.18:** stage the two Drive folders on a host with a `SANITY_API_WRITE_TOKEN`, author the manifests from the processed photos (view each; EN + drafted ES), fill `confirmed`, run `--commit` (drafts) then gated `--publish`; and (separately) build the M.18 story layout if real story pages are wanted.
- **Prior phase (on `main`, committed + pushed):** **Phase B.15 Code — Snow Removal stock-bridge imagery wired into 4 service pages + the division landing.** Extended `scripts/optimize-stock-bridge.mjs` (`ITEMS` += the 4 snow services + a hero-only division-landing entry via new `heroOut`/`heroOnly` fields) to optimize the 5 B-15-Cowork photos (`docs/stock-bridge/snow-removal/`) into **4 hero + 4 tile assets** (`src/assets/service/{hero-,tiles/}{de-icing,sidewalk-shoveling,driveway-snow-removal,commercial-snow-plowing}.jpg`, 2400×1350 heroes 142–326 KB / 1200×900 tiles) **+ `src/assets/division/hero-snow-removal.jpg`** (16:9 2400×1350, 367 KB, no tile). The 4 snow services **dropped their shared `snow-removal`/`commercial-snow-removal` `imageKey` aliases** (`services.ts`) so they resolve by slug to new `SERVICE_HERO`/`SERVICE_TILE` entries, and each gained manifest-verbatim `photoAlt` (EN) + drafted ES (TRANSLATION_NOTES §B.15, pending native review). New **`DIVISION_HERO: Partial<Record<Division, StaticImageData>>`** map (`imageMap.ts`, only `snow-removal`) + a `DIVISION_HERO[division] ?? AUDIENCE_HERO[meta.heroImageKey]` fallback in `[locale]/[division]/page.tsx` overrides the generic snow-less `commercial` audience hero on `/snow-removal/`; its alt moved to `division.snow-removal.hero.alt` in `messages/{en,es}.json`. **The pre-existing shared assets (`hero-snow-removal.jpg`, `hero-commercial-snow-removal.jpg`, their tiles, the `snow-removal-*`/`commercial-snow-removal-*` project tiles) are byte-identical** — still serve st-charles hero + city grids + the snow services' featured-project rows; blog/resource featured images are independent public-dir files (untouched). Replace-by **2027-01-31** for all 5 new assets. **Verified:** `build` exit 0 (202/202, 0 TS) · `lint` 0 err · `tsc` 0 err · optimizer idempotent (byte-identical re-run; 6 B-14 derivatives unchanged) · all 10 localized snow routes render **distinct** heroes with present/non-empty alt (EN verbatim, ES drafted) via prerendered HTML + live DOM + 6 screenshots · division grid shows 4 distinct new tiles · city grids (aurora/naperville/oak-brook/lombard → commercial-snow-plowing; wheaton/hinsdale/elmhurst/geneva → driveway-snow-removal) show the new tile + descriptive alt · st-charles hero + other 4 division landings unchanged · `validate:a11y` 0/0 (22 URLs) · `validate:links` hard 0 (294 pages) · `validate:mobile` 0 err. **Own decision (surfaced):** built + verified on a dedicated branch `feat/b15-code-snow-integration` first (mirroring the B-15-Cowork branch handling); Goran then chose **merge to `main` + push**, so the branch was fast-forwarded into `main` and pushed. Full detail in `Phase-B-15-Code-Completion.md`; decisions logged 2026-07-04. **B-15 phase pair (Cowork + Code) now closed.**
- **Prior phase (on `main`, committed + pushed):** **Phase B.14 — Stock-bridge imagery wired into 6 service pages.** Optimized the 6 free-license bridge photos sourced in B-13b (`docs/stock-bridge/`) into the live image pipeline. **Shipped:** new idempotent `scripts/optimize-stock-bridge.mjs` (sharp — 16:9 2400px hero + 4:3 1200px tile per source, adaptive JPEG quality to keep every hero < 400 KB, light noise-shaving blur fallback used on `yard-drainage` only); **6 hero + 6 tile assets** written to `src/assets/service/hero-{slug}.jpg` + `tiles/{slug}.jpg` for `gutter-services`, `yard-drainage`, `foundation-repair`, `trenching-excavation`, `sewer-line-replacement`, `conduit-installation`; those 6 services **dropped their B-12 commercial/landscape `imageKey` aliases** in `services.ts` so they resolve by slug to the new `SERVICE_HERO`/`SERVICE_TILE` entries in `imageMap.ts`. New optional `Service.photoAlt` (`{en,es}`) field carries the manifest's verbatim EN alt + a drafted ES translation (logged in TRANSLATION_NOTES §B.14, pending native review); consumed by `ServiceHero`, `AudienceServicesGrid` (division-landing grid), and `LocationServicesGrid` (city-page grid), each falling back to the service name so all other services are byte-unchanged. **foundation-repair intentionally ships the crack-condition photo** (ratified; flagged for the 2026-10-01 replacement pass like all 6). **The 10 remaining new-division services stay on placeholders** pending the diagram-track phase: 7 waterproofing gaps (`basement-waterproofing`, `sump-pumps`, `window-wells`, `crawl-spaces`, `concrete-raising`, `humidity-control`, `radon-mitigation`) + 3 trenchless diagram services (`missile-boring`, `handhole-pull-box`, `pipe-fusing`). **Verified:** `npm run build` exit 0 (202/202 prerendered, 0 TS errors); all 6 hero derivatives 16:9 2400×1350 < 400 KB, all 6 tiles 4:3 1200×900; script idempotent (re-run → identical md5); all 12 localized service pages (6 EN + 6 ES) render the new hero with present/non-empty alt (EN verbatim, ES translated) confirmed via live DOM + SSR HTML + screenshots; both division landing grids + the oak-brook city grid show the 3 (resp. 2) new tiles; 4 placeholder services (`missile-boring`, `sump-pumps`, `radon-mitigation`, `basement-waterproofing`) spot-checked unchanged (alias asset + service-name alt). **Off-spec discovery (surfaced):** `LocationServicesGrid` (city pages) is a third tile surface — cities feature `foundation-repair`/`yard-drainage`, so it was wired too. **Env note:** this fresh checkout had no `node_modules` (ran `npm ci`; `sharp@0.34.5` present via lockfile) and no `.env.local` (build needs the public Sanity `projectId i3fawnrl` from `sanity.config.ts` — added a gitignored local `.env.local` with public read config only, no secrets). Nothing under `docs/stock-bridge/` changed except the manifest status line. Full detail in `Phase-B-14-Completion.md`; decisions logged 2026-07-03.
- **Current phase (branch, NOT merged):** **Phase B.12 — Trenchless & Directional Boring (5th division)**, branch `phase/b12-trenchless-division` (off `main`; PR opened, not merged). Adds a fully-built fifth division on the existing dynamic division/service templates — **indistinguishable from the four siblings**. **Shipped:** `Division` union += `trenchless`; `DIVISIONS` appended + `DIVISION_META.trenchless` (commercial photo aliases); `[data-division='trenchless']` accent = **orange-700**, chip-bg **orange-50** (AA-fixed); dedicated **`TRENCHLESS_FACTORS`** (3 factors, NOT `GENERIC_FACTORS`); **6 services** (`conduit-installation`, `trenching-excavation`, `sewer-line-replacement`, `missile-boring`, `handhole-pull-box`, `pipe-fusing`), each full bilingual (5 whatsIncluded · 4 process · 3 whyUs · explainer · 2 projects · 3 related); **14 routes** (landing + 6 services × EN/ES) prerendered 200; `division.trenchless.*` landing block + 19 cross-ref i18n keys EN/ES lockstep. Surfaced in **navbar mega-panel (6 cols) + mobile drawer · homepage divisions block (5 cards, `lg:grid-cols-5`) + services-overview CTA row · footer · wizard Step-1 tile + `getStep3Group`→residential · `/api/quote` Zod + `quoteLead`/`quoteLeadPartial` Sanity + lead email · contact dropdown (5 touchpoints) · sitemap · JSON-LD · `/projects` filter · all validator path lists · wizard storage `VALID_DIVISIONS` · legal prose · portfolio-automation keywords**. Photos are a **commercial-alias bridge** pending real trenchless photography; honest, fabrication-free copy (no install counts/awards/certs/warranties/equipment/named jobs); `ServiceIcon` registered the trenchless icon set (also fixed Construction/Gauge/MapPin fallbacks). **Verified (clean prod build):** `build` 202/202 (14 trenchless prerendered) · `lint` 0 err · `tsc` 0 src err · `validate:schema` 0/0 · `validate:seo` 0/0 (196 URLs) · `validate:a11y` 0/0 (axe 0 / LH 100; one real orange-700-on-orange-100 contrast defect on the qualifier pills caught + fixed → orange-50) · `validate:mobile` 0 err · `validate:links` hard 0 (all 14 trenchless routes 200) · `test:consent` 23/23 · EN/ES leaf parity **1335=1335** · desktop screenshots confirm 5-card homepage row + 6-col mega-panel, no overflow. **Operator follow-ups:** `npm run studio:deploy` (publish the new `trenchless` option on `quoteLead`/`quoteLeadPartial`/`contactSubmission`); add Sanity FAQ docs `service:trenchless:<slug>` to emit per-service `FAQPage` (landing already has 5 i18n FAQs). **Doc-sync flag:** `Sunset-Services-Plan.md` + BG-01 don't yet describe trenchless. Full detail in `Phase-B-12-Completion.md`; decisions logged 2026-06-23/24; TRANSLATION_NOTES §M.01f1 extended.
- **Prior phase (branch, NOT merged):** **Stars-only rating + full Calendly removal**, branch `chore/stars-only-remove-calendly` (off `main`). Operator-requested follow-up that **supersedes parts of Hand-off B**: (1) **Removed the Google reviews — only the stars remain.** `GoogleRating` is now a decorative 5-star motif (no "4.8", no "37 Google reviews", `aria-hidden`); the 3 homepage review cards are gone; the `LocalBusiness` `AggregateRating` + `Review[]` JSON-LD and `src/lib/constants/reviews.ts` (`BUSINESS_RATING`/`REVIEW_SNAPSHOT`) + the `ratings` i18n namespace were removed; `getPublishedReviews` is no longer called from `HomeSocialProof`. (2) **Fully removed Calendly booking site-wide** ("Book directly / Pick a 30-min slot"): deleted `CalendlyEmbed.tsx` + `ContactCalendlyPlaceholder.tsx`, the Calendly section on `/contact` **and** `/thank-you`, the `contact.calendly` + `thanks.calendly` i18n (EN+ES), the `.env.local.example` Calendly vars, the `sunset:calendly-event` bridge scope + `CALENDLY_*` analytics constants, the chat high-intent banner CTA (`/contact#calendly`→`/contact/`) + chat system-prompt mention, the accessibility-statement `knownLimitations` (Calendly) section, and the privacy/terms Calendly disclosures. The per-city `location.ts` `aggregateRating`/`review[]` mechanism (driven by the still-empty `getPublishedReviewsForCity`) is untouched (emits nothing today). **Verified (final build):** `build` exit 0 · `lint` 0 err · `test:consent` 23/23 · `validate:schema` 0/22 · `validate:seo` 0/182 · `validate:a11y` **20/20** (axe 0 / LH 97–100) · `validate:mobile` 0 err · `validate:links` 0 broken · EN/ES leaf parity **1243=1243**.
- **Last merged phase:** Step 2 / **Hand-off B** (Code — confirmed-facts corrections + `/dev/system` removal + GA4 quote-start + Calendly cut), branch `phase/step2-confirmed-facts` → **merged to `main`** (PRs #17 + #18; #17's source changes were re-landed in #18 after an `index.lock` race), decisions-first commit `c66090d`. **Shipped (Erick-confirmed facts, 2026-06-22):** brand **"Sunset Services U.S."** on all formal/structured surfaces (`BUSINESS_NAME_FULL` + `BUSINESS_LEGAL_NAME = "E VALLE INC"` in `constants/business.ts`; schema `name`+`legalName`, OG `siteName`, root site-name, logo `alt`, contact name, email templates, copyright "© {year} E VALLE INC dba Sunset Services U.S."); conversational "Sunset Services" kept in body/title copy per BG-01 §2.1.1. Founded **2000 / 25+ yrs / 2nd-gen** verified (no 1998/Solis/3rd-gen); **Unilock = 2021** (`blog.ts` "since 2003/23 yrs" reworded; About `body2` 2018-takeover vs 2021-hardscape split); hardscape division 2021, **no install count**; Batavia/Winfield stale route-years softened. **Rating 4.8/37** as single-source `BUSINESS_RATING` (`constants/reviews.ts`) + new `GoogleRating` component on home trust band / About / division social proof / location trust band; valid `AggregateRating(4.8,37)` on the sitewide `LocalBusiness`. **Three real Google reviews** (Mark C, Sally Del Vecchio McKibbon, Kelli Batitsas) verbatim as `REVIEW_SNAPSHOT` → `getPublishedReviews()` falls back to it while Sanity is empty (live feed overrides, no rework) + `Review` schema on `LocalBusiness` (town/date not supplied → attribution-only cards, no `datePublished`; ES = original English). Award stays removed (comments de-litteralized → grep-clean). **Waterproofing** audited present/consistent (no change). **`/dev/system` deleted** (route + demo data; removed from sitemap comment + seo/links validators). **GA4** `quote_start` conversion added (once on wizard mount; quote-submit/contact-submit/chat-lead already wired; `test:consent` 23/23). **Calendly cut**: `.env.local.example` default empty+disabled; embed already hides + tel-fallback when URL empty, no personal link in source. **A11y flake fixed (user-directed):** a pre-existing detail-page `validate:a11y` flake (hero photo-decode race + lighter green-900 scrim + 18px nav links; not from this phase — surfaced + user chose to fix) closed via a validator image-decode settle (`validate-a11y.mjs`), `ServiceHero` black scrim, and `NavbarLink` `min-h-[24px]`. **Verified (final build):** `build` exit 0 (`/dev/system` gone) · `lint` 0 err · `test:consent` 23/23 · `validate:schema` 0/0 · `validate:seo` 0/182 · `validate:a11y` **20/20 ×2 stable** (axe 0 / LH 97–100) · `validate:mobile` 0 err · `validate:links` 0 broken · EN/ES leaf parity **1268=1268**. **Out of scope respected:** no 301s, no social-link changes, no live-feed wiring, no image swaps, no DNS, no Spanish native review, no removed content reintroduced. Full detail in `Hand-off-B-Code-Completion.md`.
- **Prior phase (branch, NOT merged):** Phase M.16 (Code — homepage redesign + sitewide orange dock + Sanity-ready image slots), branch `phase/m16-homepage-redesign` (pushed; Lazar verifies on Vercel Preview, then merges). **Shipped:** decisions-first commit; **orange token layer** (`--color-sunset-orange-50/-100/-300/-500/-700` `-500 #F28C38` + `--color-text-on-orange` + the `.btn-orange` charcoal-text variant + the `.horizon-edge` motif); **`featuredOnHome`** project-schema boolean (Studio redeployed, 1/1 schemas); 3 new GROQ helpers (`getFeaturedHomeProject` / `getFeaturedBeforeAfterProject` / `getPublishedReviews`) + `HomeReviewEntry`; **homepage rebuilt to Concept A** (7 sections — single-image hero replacing the carousel · divisions w/ ◆ UNILOCK chip + degrade-to-3 · dark `#23231D` trust band w/ chips + UNILOCK card (no year) + real-reviews slot · NEW before/after showcase · NEW process · restyled why-Sunset · dark-gradient final CTA), every photo slot **Sanity-asset-first w/ placeholder fallback**, per-item grid scroll animation removed (handover §7); **sitewide orange dock CTA** (`.btn-orange` "Get a Free Estimate", **hidden on `/request-quote/`** — D2 reinstated, reverses M.10 Issue 8) + homepage **solid white dock** over the hero (audience/service over-hero state unchanged); en/es `home.*` rewritten in lockstep (**leaf parity 1335=1335**). `HomeServicesOverview` + `HomeAbout` orphaned off the homepage (files + keys kept); `HomeProjects` untouched (reused by `/about`); `HomeHeroCarousel` **deleted**. **Verified:** `build` 190/190 green · `lint` 0 errors · `validate:a11y` PASS (home EN+ES axe 0 / LH a11y 100 / charcoal-on-orange OK / 2.4.11 0 / 2.5.8 0; 20/20) · `validate:schema` 0/0 (22) · `validate:seo` 0/0 (184 + sitemap + robots) · `validate:mobile` **0 errors** (after fixing one before/after recent-header overflow on narrow viewports) · `validate:links` **0 broken / 0 error** (0 homepage wrong-dest) · `test:consent` 23/23 · dock-CTA hidden on `/request-quote/` (both locales) confirmed in served HTML · **asset-wins VERIFIED** (before/after pair + recent strip + several division cards render REAL Sanity photos) AND placeholder-fallback VERIFIED (hero → `hero.jpg` since nothing is flagged `featuredOnHome` yet). **Lighthouse** (localhost prod-mode, `/`): desktop **97** / a11y 100 / bp 96 / seo 92; mobile **~90 (warm; 72 cold-cache)** / a11y 100 / bp 96 / seo 92 — the two <95 cells are the **documented localhost-canonical artifact** (prod self-canonical ≈100) and the **full-bleed-hero mobile-LCP ceiling** (served hero already 42KB, so not image bytes; LCP ≈3.6s under 4× CPU throttle; closes on Vercel CDN + real M.01 photos; *better* than the old carousel's ~86). **Own decisions surfaced** in `Phase-M-16-Code-Completion.md` §4 (ES marquee-line drafts pending native review; `content-visibility` vs `next/dynamic` for Server Components; static before/after split; orange-700 numerals for AA; D2 reinstatement; dropped services/about sections). **Out of scope respected:** no other page bodies; non-homepage body CTAs stay amber; chat widget + footer untouched; no photo upload run; no secrets touched. Full detail in `Phase-M-16-Code-Completion.md`.
- **Last completed phase:** Phase M.15 (Code — unblocked-work sweep), branch `phase/m15-sweep` → merged to `main` + pushed. **M.14 was already merged to `main`** (`main` byte-identical to `qa/b-09-corrections`); any doc still calling M.14 a "pending branch" is stale (live code wins). **Shipped:** Stream 0 (decision log); Stream 1 (M.14 close-out verified + branch topology reconciled; 2 stale alt-text "Solis"→"Valle"); Stream 4 (Sanity Studio redeployed → `sunsetservices.sanity.studio`, 1/1 schemas); Stream 6 (M.02 perf pass from `perf-m02-lighthouse` — motion/react off the layout chunk, consent-modal dynamic, `latin-ext` dropped, `quality={70}` heroes incl. carousel, `npm run lighthouse` harness); Stream 8 (global reduced-motion CSS guard so the M.02 `MotionConfig` removal can't regress reduced-motion + `docs/a11y/M15-manual-screenreader-checklist.md`); plus an `eslint dist/**` ignore. **Verified:** `build` 190/190 · `lint` 0 errors · `grep -ri solis` zero (code/data/messages/docs) · `validate:a11y` 0 violations / Lighthouse a11y 100 (97 /about) / reduced-motion-OK across 20 EN+ES URLs · `validate:schema` 0/0 (22 URLs) · `validate:seo` 0/0 (184 URLs + sitemap + robots) · `validate:mobile` 0 errors (9 viewports × 31 URLs) · `validate:links` all internal 200/redirect-OK · `test:consent` 23/23 · ServiceM8 flag-off 200-simulated · unsubscribe route 200. **Fresh local Lighthouse** (localhost prod-mode): a11y 97–100, BP 96 (thank-you 73 = Calendly cookies), SEO 92 (localhost canonical artifact; prod self-canonical ≈100), desktop perf 95+ on 5/7, **mobile perf 95+ only on `/qa/`** — documented full-bleed-hero-LCP ceiling + oversized placeholder imagery (real photos = biggest lift). **Deferred:** Stream 2 (no authoritative +2-city list), Stream 3 (no photo corpus reachable / placeholder write token), Stream 7 (Telegram creds absent); **Stream 5** verified already-shipped by B.09. **Carried flags:** Calendly still personal `dinovlazar2011` URL (M.08); `/dev/system` (noindex dev sandbox) not production-gated, still renders demo testimonials; `blog.ts` asserts "Unilock since 2003 / 23 years" pending Erick; mobile-perf 95 + real photos + Upstash KV flip + Telegram wiring are pre-launch follow-ups. Full per-stream detail in `Phase-M-15-Completion.md`.

- **Last completed phase:** Phase M.14 (Code — Goran QA B-09 pre-launch corrections), branch `qa/b-09-corrections`. Applied the correctable B-09 items and removed all false/inflated/unverifiable content so nothing misleading ships; verified content refills in M.14b once Erick confirms facts. **Group A:** A1 `Erick Solis`→`Erick Valle` (home portraitAlt; ES already correct); A2 "Erick or his son picks up"→"Erick picks up" across all 22 city templates + home about line (EN+ES) — no third-generation phrasing left; A3 hardscape pricing — new `HARDSCAPE_FACTORS` (sq ft / materials+base / site access+structures) replaces lawn-care `GENERIC_FACTORS` on all 6 hardscape services (`services.ts`); A4 FAQ literal `###` stripped at component (`FaqAccordion`) + schema (`buildContentFaqSchema`) via new `src/lib/faqText.ts`; A5 Resources mega-panel — index/`[slug]` routes are correct (Sanity-backed); emptied the invented `RESOURCES_PANEL` `placeholderKeys` (headers still link real `/resources/` + `/blog/`); A6 alt text — service-area "Recent projects", project/service tiles, service hero, city cards now carry specific descriptive alts (EN+ES; new `location.projects.tileAlt`); photo *replacement* deferred to M.14b/M.01; A7 noindex — new `isProductionDeploy()` (`VERCEL_ENV`) drives `robots.ts` (`Disallow: /` off-prod) + root-layout `robots:{index:false}` off-prod; production unchanged. **Group B (removed, M.14b refills):** B1 all templated testimonials removed (home, 4 divisions, 24 city arrays), sections hidden cleanly (`HomeSocialProof` now a credentials band; `AudienceSocialProof` shows testimonial heading+grid only when reviews exist; `LocalTestimonials` already guarded); B2 all hardcoded 4.8/4.9 + 200+/287 removed sitewide, replaced with number-free verifiable chips (Family-run / Licensed & insured / EN·ES) — no fabricated `AggregateRating`; B3 "DuPage Tribune · 2024" award removed (home+about), replaced with verifiable credentials; B4 Unilock untied from 2000 (hero "serving DuPage since 2000"); "since 2010/2003", "fifteen years and counting", "380+ installs" removed; "since 2000 / 25+ years" kept consistent; founder contradiction fixed ("Erick founded in 2000"→"Family-run since 2000"); B5 social links env-driven (`NEXT_PUBLIC_SOCIAL_*`), hidden until real profile URLs set. Calendly confirmed env-driven (`NEXT_PUBLIC_CALENDLY_URL`) — no code change. EN/ES leaf parity 1248=1248; `npm run build` green (190/190); `npm run lint` 0 errors. Full disposition table + §5 kept/removed + M.14b checklist in `Phase-M-14-Completion.md`.

- **Prior phase:** Phase B.03e (Code — hard-coded English legal pages; Termly removed), merged. Replaced the Termly iframe with self-contained hard-coded **English-only** Privacy + Terms on all four routes (EN+ES serve the same English text in `.legal-doc` chrome); dropped `TermlyPolicyEmbed.tsx` + script + 5 `NEXT_PUBLIC_TERMLY_*` env vars. See `Sunset-Services-Decisions.md` 2026-06-02 + `Phase-B-03e-Completion.md`.
- **Last completed phase (prior 1):** Phase M.11c (mobile bug sweep), branch renamed from "M.11b", merged to `main`. Multi-subagent mobile-rendering sweep (320–414px + landscape/short/desktop) fixing the 320px /contact + /thank-you cutoff, safe-area insets, and more; ships `scripts/validate-mobile.mjs` (`npm run validate:mobile`). See `Phase-M-11c-Completion.md`.
- **Last completed phase (prior 2):** Phase M.10g (portfolio gallery grouped + labeled by city), branch `phase/m10g-portfolio-location` (NOT merged; user verifies on Vercel Preview, then merges). Renders `/projects` tiles by structured Sanity `city->name`, clusters same-city jobs, location-less last (render layer only). See `Phase-M-10g-Completion.md`; Decisions 2026-05-31.
- **Last completed phase (prior 3):** Phase M.11b (link-integrity crawl & fix), branch `phase/m11b-link-integrity` (NOT merged; user verifies on Vercel Preview, then merges). One Playwright BFS crawl of every EN+ES page found and fixed at source every broken/wrong-destination/missing-anchor internal link; ships `scripts/validate-links.mjs` (`npm run validate:links`). See `Phase-M-11b-Completion.md`; Decisions 2026-05-31.
- **Last completed phase (prior 4):** Phase M.10f (mobile homepage hero — viewport-fit + legibility), branch `phase/m10f-mobile-hero-fix` (pushed, NOT merged; user verifies on Vercel Preview, then merges). Switched the phone hero to `min-h`+`svh` so bottom content no longer clips; strengthened scrim + mobile text-shadow. See `Phase-M-10f-Completion.md`.
- **Last completed phase (prior 5):** Phase M.11 (full multi-agent QA & fix sweep), branch `worktree-phase+m11-qa-sweep` (NOT merged; user verifies on Vercel Preview, then merges). Site-wide discover→fix→verify sweep across 9 atomic commits; all three validation harnesses driven fully green. See `Phase-M-11-Completion.md`; Decisions 2026-05-31. **Operator flag:** rotate + HTTP-referrer-restrict the GCP Places API key (git history + the public client bundle make rotation mandatory).
- **Last completed phase (prior 6):** Phase M.10e (walkthrough bug-fix slice), merged (4 Preview-walkthrough defects + lint cleanup): hero text restored over the carousel, Sunset-mark favicon set, About/home "Recent work" cards repointed to live Sanity slugs, OG/Twitter card audit + 3 closure items. See `Phase-M-10e-Completion.md`; Decisions 2026-05-28.
- **Last completed phase (prior 7):** Phase M.03 (Codex — LLM-driven Spanish review pass), merged. Reviewed the Spanish surface against `Sunset-Services-TRANSLATION_NOTES.md` §M.01f1, applied source + live Sanity `.es` fixes via `scripts/m03-spanish-fixes.mjs`; 4 boundary decisions left for post-launch native review. See `Phase-M-03-Completion.md`.
- **Last completed phase (prior 8):** Phase M.10c (brand identity quick wins — logo + 4-division labels + rotating hero + `/projects` index addendum), merged. Chroma-keyed logo swap, division badges via `getProjectDivision()`, reduced-motion-aware hero carousel, and the `/projects` division filter strip. See `Phase-M-10c-Completion.md`; Decisions 2026-05-27.
- **Last completed phase (prior 9):** Phase B.11 (photo upload on quote wizard Step 3), merged. Real Sanity-backed uploader (drag/click/keyboard, up to 10×10 MB, JPEG/PNG/HEIC/WebP) via new `PhotoUploadField.tsx` + `/api/quote/photo-upload` (seven-stage guard chain, magic-bytes MIME sniff, rate limit); ships `npm run test:photo-upload`. See `Phase-B-11-Completion.md`.
- **Last completed phase (prior 10):** Phase M.10b (content integration — Marcin's brand story + Goran's Q&A library + 7 SEO FAQs), merged. Q&A rebuilt to 7 categories × 28 questions, new HomeWhySunset section, About brand-story enrichment, idempotent `scripts/seed-faq-content-integration.mjs`. See `Phase-M-10b2-Completion.md`; Decisions 2026-05-27.
- **Last completed phase (prior 11):** Phase M.10 (user-walkthrough fixes), merged. Closes 10 Goran-flagged bugs (animation flicker, navbar hover-to-open, per-city hero images, team initials avatars, real blog/resource images, 4-division contact dropdown, wizard navbar parity, footer fixes, new `/accessibility/` page). See `Phase-M-10-Completion.md`; Decisions 2026-05-26.
- **Last completed phase (prior 12):** Phase M.01f1 (Cowork — Spanish first-pass polish + glossary lock), merged. Glossary/register/tone pass over all M.01d/e content; locked the canonical Spanish glossary + register matrix in `Sunset-Services-TRANSLATION_NOTES.md` §M.01f1; normalized CRLF→LF + added `.gitattributes`. See `Phase-M-01f1-Completion.md`.
- **Last completed phase (prior 13):** Phase M.01e-pt2 (wizard division migration + featuredServices enrichment + map label de-overlap), merged. Step 1 = 4-division picker, Step 4 propertyType radio, localStorage v1→v2 migration, `quoteLead`/`quoteLeadPartial` `audience`→`division` rename + migration script, 22-city featuredServices mix, map label allowlist. See `Phase-M-01e-pt2-Completion.md`; Decisions 2026-05-26.
- **Last completed phase (prior 14):** Phase M.01e (visible flip to 4 divisions + 22 cities + Q&A + redirects), merged. Flipped every visible surface to the new IA (8 division-landing routes, 56 service-detail routes, 22 city pages, 1 Q&A page, sitemap/schema/nav/footer/map) + 56 permanent redirects; globally-unique service slugs; `scripts/migrate-faq-to-divisions.mjs`. See `Phase-M-01e-Completion.md`.
- **Last completed phase (prior 15):** Phase M.01d (data + plan restructure: 4 divisions, 22 city pages prepared, new H1, Q&A i18n, project-address strip), merged. `Service` gains required `division`; +14 services (30 total); LOCATIONS extended to 24 (visible stays 6); new home H1; `qa.*` namespace; `stripStreetNumber` helper. Zero visible site change beyond the H1. See `Phase-M-01d-Completion.md`.
- **Last completed phase (prior 16):** Phase B.10 (Google Places address autocomplete on quote wizard Step 4), merged (verified on localhost + Preview). New `src/lib/google/placesAutocomplete.ts` + `useGooglePlacesAutocomplete` hook (legacy `Autocomplete`, US-restricted, manual entry always works); ships `npm run test:wizard-autocomplete`; new `NEXT_PUBLIC_GOOGLE_PLACES_API_KEY` on Vercel. See `Phase-B-10-Completion.md`. **Operator flag:** add GCP HTTP-referrer restrictions to the Places key + wire billing alerts before public launch.
- **Last completed phase (prior 17):** Phase B.09 (chat rate-limiter swap, in-memory → Upstash Redis, flag-gated), merged (verified on localhost + Preview). `src/lib/chat/rateLimit.ts` now two-backend (`memory`/`kv`) via `@upstash/redis`, fail-open; ships `npm run test:rate-limit`; new `CHAT_RATELIMIT_STORE=memory` on Vercel. See `Phase-B-09-Completion.md`.
- **Last completed phase (prior 18):** Phase B.08 (Sanity → site instant revalidate webhook), merged (verified on localhost + Preview). New `src/lib/sanity/revalidation.ts` + `POST /api/revalidate` (signature-verified) flushes tags/paths within ~2s; every GROQ helper tagged; ships `npm run test:revalidate`; new `SANITY_REVALIDATE_SECRET`. See `Phase-B-08-Completion.md`.
- **Last completed phase (prior 19):** Phase B.07 (newsletter unsubscribe page + API), merged (verified on localhost + Preview). New `/[locale]/unsubscribe/[token]` page + `POST /api/newsletter/unsubscribe`, new `unsubscribeToken` Sanity field, token backfill script, noindex + robots Disallow. See `Phase-B-07-Completion.md`.
- **Last completed phase (prior 20):** Phase B.06 (accessibility audit, WCAG 2.2 AA), merged (Vercel Preview verified). New `scripts/validate-a11y.mjs` (`npm run validate:a11y`) across 18 URLs; fixed contrast (new `--color-sunset-green-600`), aria issues, nested-interactive map, target sizes → 18/18 PASS, 0 axe AA. See `Phase-B-06-Completion.md`; remaining carryover: manual NVDA/VoiceOver testing.
- **Last completed phase (prior 21):** Phase B.05 (sitemap, robots, hreflang, canonical — final pass), merged (Vercel Preview verification carried over). Centralized `src/lib/seo/urls.ts`, dynamic `sitemap.ts` (114 entries) + `robots.ts`, new `scripts/validate-seo.mjs` (`npm run validate:seo`) → exit 0 across 118 URLs. See `Phase-B-05-Completion.md`.
- **Last completed phase (prior 22):** Phase B.04 (schema validation pass across every page type), merged (Vercel build verified). Root layout JSON-LD now a `@graph` (LocalBusiness + Organization by `@id`); per-page builders reference by `@id`; conditional Review/AggregateRating scaffold; new `scripts/validate-schema.mjs` (`npm run validate:schema`) → exit 0. See `Phase-B-04-Completion.md`.
- **Last completed phase (prior 23):** Phase B.03 (legal pages + Consent Mode v2 banner + modal + Termly iframe embed), merged — **SUPERSEDED by B.03e** (Termly removed, hard-coded English-only legal). Three-button consent banner + preferences modal + Google Consent Mode v2 wiring remain in effect. See `Phase-B-03-Completion.md`; Decisions 2026-05-15/16.
- **Last completed phase (prior 24):** Phase B.03 prior entries (B.03b Cowork setup, B.03c local-HTML revert, B.03d iframe restore), consolidated into the canonical B.03 entry above. See `Sunset-Services-Decisions.md` 2026-05-15/16.
- **Last completed phase (prior 25):** Part 2 — Phase 2.17 (automation agent Part B — on-demand ServiceM8 → portfolio publish pipeline, Telegram-approval leg), merged. Phase 2.13 webhook triggers the draft pipeline via `after()`; Anthropic-drafted bilingual `portfolioDraftPending` doc → Telegram Approve/Reject → live `project`; GBP write leg stubbed (deferred to 2.17a); ships `scripts/test-portfolio-automation.mjs`. See `Phase-2-17-Completion.md`.
- **Last completed phase (prior 26):** Part 2 — Phase 2.16 (automation agent Part A — monthly AI blog draft cron + weekly SEO summary cron; closes the 2.15 `'blog_draft'` stub), merged. Two Vercel Crons; blog cron fully wired (topic rotation → Anthropic draft → `blogDraftPending` → Telegram approve → live `blogPost`); weekly SEO cron flag-gated; ships `scripts/test-blog-automation.mjs`. See `Phase-2-16-Completion.md`.
- **Last completed phase (prior 27):** Part 2 — Phase 2.15 (Telegram bot infrastructure — operator notifications + approval flows), merged. New `POST /api/webhooks/telegram` (flag-gated, secret-token verified) + `notifyOperator()`/`requestApproval()` helpers + client with test seam. See `Phase-2-15-Completion.md`.
- **Last completed phase (prior 28):** Part 2 — Phase 2.13 (ServiceM8 webhook endpoint + Sanity event queue), merged — note: 2.17 wires an `after()` callback to this route. New `/api/webhooks/servicem8` (flag-gated, HMAC-verified, idempotent) ingests `job.completed` into a `servicem8Event` doc. See `Phase-2-13-Completion.md`.
- **Phase 2.11 (Spanish translation pass — full scope, mixed tone)** dropped off the visible chain after Phase B.03c shift. Still in the file map and at `src/_project-state/Part-2-Phase-11-Completion.md`. Summary still in effect: 191 `[TBR]`-flagged ES strings upgraded across `src/messages/es.json` + `src/data/{locations,projects}.ts` + email templates + chat persona; tone map locked (`usted` for legal/forms/transactional, `tú` for marketing/content/persona); 60+ glossary rows in `Sunset-Services-TRANSLATION_NOTES.md`; Phase B.01 then stripped every `[TBR]` marker.
- **Phase 2.10 (Code: analytics stack — GTM + GA4 + Microsoft Clarity + binary cookie banner + dataLayer bridge) is now superseded by Phase B.03** for the banner + Consent Mode v2 wiring. Phase 2.10's surgical dispatcher renames + AnalyticsBridge document-listener pattern + PII filter all remain in place under B.03; only the consent state schema (boolean → 4-category) + the script-load gates (binary → Consent Mode v2 always-load) changed. 14/14 Phase 2.10 smoke tests should still pass against the refactored modules (the assertions are about event names + PII filtering, not the consent schema).
- **Phase M.03 Spanish review slot closed as Codex-driven LLM review.** The original native-review slot has been completed as an LLM pass against the locked M.01f1 glossary/register matrix. Human native Spanish review remains deferred post-launch by explicit user decision.
- **Phase M.01c (photo curation + Sanity upload prep) ran 2026-05-26 (Cowork) — PARTIAL completion.** Local processing done: 204 HEIC → JPG+WebP, 106 orphan RAWs developed (curated), 45 project photos + 4 team + 8 brand curated into `C:\sunset-photos\processed\`. Full metadata + descriptions in `processed\sanity-upload-plan.json`; video manifest (26 turf→YouTube, 6 drone→Sanity) at `C:\sunset-photos\video-manifest.json`. **Sanity upload NOT yet run** — API is unreachable from the Cowork sandbox, so it's packaged as `scripts/upload-m01c-photos.mjs` (dry-run by default, `--commit` to write; uploads photos, creates 7 real projects, removes the 12 placeholders). **Still pending user action:** run that script; upload 26 turf MP4s to a Sunset Services YouTube channel + fill manifest URLs; compress+upload 6 drone clips to Sanity; confirm cities for 807/811 Edgewater + Scott & Sarah's; add `downers-grove`/`north-aurora` locations; supply real per-city project counts (left UNCHANGED in `locations.ts` — not derivable from a photo sample); Vercel preview spot-check after upload. Full detail + the 12 placeholder slugs in `Phase-M-01c-Completion.md`; decisions appended to `Sunset-Services-Decisions.md`.
- **Resend domain verification deliberately deferred to Phase 3.11/3.12** per the 2026-05-12 decision-log entry. Sandbox sender (`onboarding@resend.dev`) stays; DNS for `sunsetservices.us` is still parked at the old WordPress registrar.
- **Chat rate-limit store running on `'memory'`** — Phase B.09 (2026-05-18) shipped the kv-backed branch via `@upstash/redis`, but the live Vercel value of `CHAT_RATELIMIT_STORE` stays `'memory'` until Cowork installs the Upstash Marketplace integration and flips the flag to `'kv'` on Production + Preview. Memory mode is fine while the site is SSO-protected (abuse risk functionally zero), but **MUST** be flipped before Phase P.06 (DNS cutover / public launch) or anyone can defeat the daily cap by waiting between bursts (counters reset on each ~5–15min Vercel cold start on Hobby). The flip is a 5-minute Vercel dashboard click-through documented in `Phase-B-09-Completion.md` §"User-runnable next step".
- **GTM tag configuration (Phase 2.10 Part B) is Cowork's responsibility post-merge** — the dataLayer infrastructure is in; the actual GA4 tags + Key Event marks happen inside the GTM web UI.
- **Phase 2.14 (GBP + Places) deferred + rolled forward** per the 2026-05-15 decision-log entry. Today is 3 days into Google's 2–6 week GBP API review window, and `GBP_OAUTH_CLIENT_SECRET` / `GBP_OAUTH_REFRESH_TOKEN` are still pending from Goran's side. Phase 2.16 Daily cron (Places-side) remains blocked until 2.14 ships its fetcher; Phase 2.16 Weekly SEO + Monthly blog crons (which DID ship this phase) are NOT Places-dependent. When Phase 2.14 reopens, its follow-up either consolidates the Daily cron into the existing two routes OR triggers the Phase 3.10 Pro upgrade — Hobby's 2-cron budget is full per Phase 2.16's commits.
- **Phase 2.16 Weekly SEO cron flag flips on at Phase 3.15** (`GSC_ENABLED=true`) once Google Search Console ownership verification is set up on the new `sunsetservices.us` domain. Until then the route returns 200 + simulated. The fetcher body is a TODO; the summarizer + route handler are complete on both branches.
- **Auto-published blog posts ship with a placeholder featured image** (`public/images/blog/_placeholder.jpg`, uploaded to Sanity on first cron run with `originalFilename: 'blogDefaultPlaceholder.jpg'`, reused across runs via lookup-by-filename). Operator swaps in a curated brand-themed image from Sanity Studio when ready — single-click in the `featuredImage` field on the auto-published `blogPost` doc.
- **Phase 2.17 ships the Telegram-approval leg of the portfolio pipeline behind two flags** (`PORTFOLIO_DRAFT_ENABLED=false` master + `GBP_PORTFOLIO_PUBLISH_ENABLED=false` sub-flag for GBP). The pipeline triggers from the existing Phase 2.13 webhook via Next 16's `after()` callback; real ServiceM8 events do NOT flow until Erick adopts ServiceM8 AND both `SERVICEM8_ENABLED` and `PORTFOLIO_DRAFT_ENABLED` flip on Vercel. The GBP write legs stay stubbed (`{skipped:true,reason:'gbp-deferred'}`) until Phase 2.17a wires the real GBP client (Phase 2.17a is a one-shot follow-up that opens after Phase 2.14 lands the GBP write client + Goran provides `GBP_OAUTH_CLIENT_SECRET` + `GBP_OAUTH_REFRESH_TOKEN`). Call sites in `publish.ts` stay unchanged when 2.17a flips the stub bodies.
- **ServiceM8 payload shape is an assumption pending real-traffic confirmation.** The `extractJobMetadata` helper falls back defensively (every field can be null) — but at flag-on time, re-confirm the assumed shape against Erick's real webhook output. Lookup order documented in `Sunset-Services-Decisions.md` (2026-05-15 plan-of-record entry).
- **Next phase:** With M.01e-pt2 shipped, **the three M.01e deferrals are closed** (wizard division migration + Step 4 propertyType radio + featuredServices enrichment + map label de-overlap) and the wizard surface now matches the visible-site IA. **Bucket B was fully closed at B.10** AND the last Phase 1.20 placeholder behavior is closed (B.01 ES `[TBR]` strip → B.03 legal/consent → B.04 schema → B.05 sitemap/robots/hreflang → B.06 a11y → B.07 unsubscribe → B.08 webhook revalidation → B.09 chat rate-limit store → B.10 address autocomplete). **Phase B.10 Cowork follow-up (GCP HTTP referrer allowlist on the Places API key)** is mandatory before Phase P.06 DNS cutover and is the highest-priority post-merge task — see "What does NOT work yet" above for the field-by-field block. **Phase B.08 user-runnable finish-up** (Sanity webhook in `manage.sanity.io`) is also still pending — a 5-minute click-through per `Phase-B-08-Completion.md` §"User-runnable next step". Until then the route is signature-verified and inert. **Phase B.03b Termly finish-up — MOOT as of Phase B.03e (2026-06-02):** the Termly iframe was replaced by hard-coded English-only Privacy + Terms on all four routes (no missing Termly doc IDs, no fallback card); the 5 `NEXT_PUBLIC_TERMLY_*` env vars were removed from `.env.local` + `.env.local.example` (Vercel-side removal pending a valid Vercel token — see `Phase-B-03e-Completion.md`). Still-valid user-side pre-launch task: Tag Assistant Consent Mode v2 validation (the consent banner is unchanged). plus Google Search Console + Bing Webmaster sitemap submission once the production domain cutover lands (Phase P.08), which is what B.05's sitemap + robots was building toward. Other unblocked phases that can run in any order: Part 2 — Phase 2.18 (Part 2 QA + integration sweep + Part 2 completion report); Phase 2.17a (GBP write client); Phase M.01 (end-to-end smoke / make-it-work bucket start). Phase 2.14 (GBP + Places) still waits on Google's GBP API approval + Goran's OAuth handoff. **M.01e-pt2 operator follow-ups:** (1) run `npx tsx scripts/migrate-quotelead-audience-to-division.mjs` against the live Sanity dataset (idempotent — maps existing `quoteLead` + `quoteLeadPartial` docs from `audience` → `division` + `propertyType`); (2) re-run the three validation harnesses (`npm run validate:schema`, `npm run validate:seo`, `npm run validate:a11y`) against `localhost:3000` (or Vercel Preview) per the same flow M.01e documented. **Next slot after M.01e-pt2 is M.01f** (live Google reviews + ES first-pass for new content).
- **Phase M.01a (Photo source discovery & inventory) closed 2026-05-20.** Read-only inventory of the photo source drive is complete. **M.01 is now split into M.01a (this phase — DONE) and M.01b (Curation + Sanity upload — NEXT, blocked on Chat's review of the inventory).** Inventory report: `src/_project-state/Phase-M-01a-Photo-Inventory.md`; completion report: `src/_project-state/Phase-M-01a-Completion.md`. Key findings for M.01b planning: (1) the real source path is **`D:\Sunset Shared Drive`** (external drive "HV620S"), NOT `D:\Goran PC BackUp` (which mounted empty — the user corrected the path mid-run); (2) the `Extracted\` tree holds only ~51 media files, most of them logos/stock/graphics/AI clips with only a handful of genuine job photos; (3) **the bulk of the real photo library is still inside ~78 un-extracted Google Takeout ZIP parts** in `MARKETING ZIP Filer\` — M.01b must extract these (to local C: disk) before curation; (4) HEIC conversion + RAW (NEF) export steps will be needed; (5) byte sizes / true file dates could not be captured (external drive can't be `stat`-ed by the read-only tools) — see the inventory's "Items flagged for Chat review."
- **Phase M.01b (Takeout extraction & full inventory) closed 2026-05-20.** The 78 Google Takeout ZIP parts were extracted directly from D: to **`C:\sunset-photos\extracted\`** (working folder, NOT in the repo): **77 parts extracted (one was a confirmed duplicate), 0 failures**; parts 002/010/067 absent and accepted as lost. The real photo corpus is now on local disk with true sizes/dates. **Headline: 2,191 media files / ~141 GB across 66 folders — 835 photos, 627 RAW (NEF), 729 video — almost all from 2025 (May–Oct).** The 330 `EditedPhotos` JPGs are the curated web-ready set; per-address job folders (1008 Homerton, 807/811 Edgewater, 6135 Belmont, 1227 Colchester, Scott & Sarah's) map onto project pages. **M.01c (curation + Sanity upload) is NEXT, blocked on Chat reviewing this inventory.** M.01c must: convert 204 HEIC, export 627 RAW (many already have edited JPGs in `EditedPhotos`), and EXCLUDE the Adobe Premiere `.PRV` render-cache `.mov` (~142 files, ~14 GB) plus the `GraphicDesign`/`SocialMedia` non-job assets. Inventory: `src/_project-state/Phase-M-01b-Photo-Inventory.md`; completion: `src/_project-state/Phase-M-01b-Completion.md`; raw data: `C:\sunset-photos\logs\{06-media-files.csv,07-folder-summary.json,08-summary.txt}`.
- **Date:** 2026-07-23

---

## What works

- **Production:** `https://sunsetservices.vercel.app` mirrors `localhost:3000` exactly. All Phase 1.20 functionality verified live (14 routes smoke-tested in Step 9 of the Phase 2.02 plan). **Chat bubble is now live** — `NEXT_PUBLIC_AI_CHAT_ENABLED=true` on Vercel; the bubble mounts on every route except `/request-quote/` and the panel streams real Claude responses.
- **Preview:** every non-`main` branch push produces a preview URL at `https://sunsetservices-git-<branch-or-truncated-hash>-dinovlazars-projects.vercel.app` within 1–2 minutes (Vercel truncates long branch names + appends a 6-char hash). Preview URLs are **Vercel-SSO-protected** by default (Vercel team default as of late 2025) — only authenticated team members can view them; disable SSO in Project Settings → Deployment Protection to share with stakeholders.

## What works on `localhost:3000`

- `/` (and `/es`) — full homepage at EN/ES per Phase 1.06 handover §3–9.
- `/{audience}/` (3 audiences × 2 locales = 6 URLs) — Residential, Commercial, Hardscape audience landings per Phase 1.08 §3 + §3X. Eight content sections + Hardscape's Unilock band (charcoal) + Hardscape charcoal CTA per D6.
- `/{audience}/{service}/` (16 services × 2 locales = 32 URLs) — All 16 service pages render from `src/data/services.ts` with production-grade EN+ES seed content per Phase 1.08 §4.10. All 9 sections per Phase 1.08 §4. Phase 1.10 fixed the previously-broken `/commercial/snow-removal/` URL (was `/commercial/commercial-snow-removal/` in 1.09).
- `/about/` (and `/es/about/`) — six sections per Phase 1.11 handover §2.1: Hero → Brand story → Team → Credentials → Projects teaser (HomeProjects literal reuse, §3.6) → CTA. Surface alternation white/cream ×6. Person × 3 + BreadcrumbList JSON-LD. Visible breadcrumb on hero (on-dark variant) so the visible navigation matches the schema (master plan §2.9).
- `/contact/` (and `/es/contact/`) — five sections per Phase 1.11 handover §2.2: Hero → Info+Form → Map → Calendly section → Service-area strip. Zero body amber CTAs (D11 — page IS the conversion surface). Static map placeholder (D8). Phase 1.11 section chrome (eyebrow + h2 + body, cream surface) preserved; Phase 2.07 swapped the static placeholder card for the real `<CalendlyEmbed/>` inline widget with consent-gated lazy load + flag fallback + `<noscript>` anchor + secondary `tel:` link. ContactPage + BreadcrumbList JSON-LD. ContactForm + CalendlyEmbed are the two client components on the page; everything else is server-rendered. Phase 1.14: now consumes the promoted `<ServiceAreaStrip>` (no `excludeSlug`, default behaviour).
- `/service-areas/` (and `/es/service-areas/`) — Phase 1.14 §3 — four sections: Hero (split text + static SVG map of DuPage with 6 pins) → Cities grid (6 LocationCards) → Outside-area band (no CTA, phone + email links) → CTA. Surface alternation bg/cream. `BreadcrumbList` + `ItemList` of 6 Place items (same-source). Static SVG map is server-rendered, no JS, with `<title>` / `<desc>` per locale and per-pin `<Link>` from `@/i18n/navigation`.
- `/service-areas/{aurora,naperville,batavia,wheaton,lisle,bolingbrook}/` (and `/es/service-areas/<city>/`) = 12 routes — Phase 1.14 §4. Nine sections per city: Hero (compact split, photo right) → LocalTrustBand (3 stat cells) → ServicesGrid (6 ServiceCards mapped from `featuredServices`) → ProjectsStrip (3 placeholder tiles, real wiring in Phase 1.16) → Testimonials (1 card, placeholder copy until Phase 2.15) → WhyLocalPanel (shared portrait + per-city ~120-word prose) → ServiceAreaStrip (`excludeSlug={location.slug}`, hides current city) → FAQ (4 native `<details>`) → CTA (shared `<CTA>` with `tokens={{city}}`). Surface alternation bg/cream × 9 (no two adjacent same). Schema: BreadcrumbList + Place + ItemList of 6 Service items + FAQPage, all same-source with the visible DOM. Hero photo carries `priority` + `fetchPriority="high"` (LCP candidate). Single body amber CTA per page (the bottom CTA section).
- `/projects/` (and `/es/projects/`) — Phase 1.16 §3 — five sections: Hero (compact text on cream) → FilterChipStrip (`?audience=` URL state) → ProjectsGrid (3/2/1-col, 12 placeholder tiles via `ProjectCard`) → Pagination (renders only when filtered total > 12; with 12 entries, hidden) → CTA (cream). Surface alternation cream/bg/cream. Schema: `BreadcrumbList` + `ItemList` of 12 `CreativeWork` (each with `creator: {@id}` referencing sitewide `LocalBusiness`). Filter sanitization: unknown `audience` → All; `page` clamped to `[1, totalPages]`. Self-canonical points to the unfiltered/unpaginated route.
- `/projects/{slug}/` (and `/es/projects/<slug>/`) = 24 routes — Phase 1.16 §4. Seven sections per detail: Hero (compact split, breadcrumb under navbar, lead photo `priority`+`fetchPriority="high"`) → Narrative → Gallery (native `<dialog>` lightbox: focus-trap/restore/Esc/←/→ + counter `aria-live="polite"`, 200ms cross-fade with reduced-motion off) → Facts (`<dl>` semantics, 6 rows, deep links to service detail / city / audience) → BeforeAfterToggle (renders only when `hasBeforeAfter:true`; SSR After fallback for no-JS) → RelatedProjects (3 tiles, deterministic same-audience → same-city → most-recent, sort year desc / slug asc) → CTA (`tokens={{city}}` interpolation, destination `/request-quote/?from=project&slug={slug}`). Surface alternation bg/cream, no two adjacent same. Schema: `BreadcrumbList` + `CreativeWork` (image array same-source with the rendered gallery; `creator: {@id}` references LocalBusiness; `keywords` = audience + service names; `locationCreated.address` Place).
- `/resources/` (and `/es/resources/`) — **NEW (1.18)** evergreen reference index. Four sections: Hero (text-only, white) → FilterChipStrip+Grid (cream) → Help-deciding band (white) → CTA (cream, amber). Single-select `?category=` URL state; filter URLs canonicalize to the un-filtered route. `BreadcrumbList` + `ItemList` JSON-LD same-source with the visible 3-col grid.
- `/resources/{slug}/` (and `/es/resources/{slug}/`) = 10 routes — **NEW (1.18)** Phase 1.18 §4. Six sections per detail: Hero (cream, eyebrow + H1 + dek + ContentMeta — no photo) → ProseLayout body (white, sticky right-rail TOC at xl+, inline collapsed `<details>` below xl, inline `<ServiceCard>` cross-link spliced between H2s where flagged) → FAQ (cream, optional) → Related (white, 3 ContentCards) → CTA (cream, amber). Surface alternation cream/white. Schema: `BreadcrumbList` + (`Article` | `HowTo` per `entry.schemaType`) + `FAQPage` if FAQ. `Article` payload includes `wordCount` computed at build from the EN body via `estimateReadingTime` (200 wpm). 5 entries × 2 locales = 10 SSG routes via `dynamic = 'force-static'` + `dynamicParams = false`.
- `/blog/` (and `/es/blog/`) — **NEW (1.18)** time-stamped post index. Three sections: Hero (text-only, white) → Featured-post 2-col + FilterChipStrip + 3-col Grid (cream) → CTA (white, amber). Featured post composes the locked `card-photo` primitive at a wider span — NOT `.card-featured` (handover §2 D16, zero featured-card constraint). Schema: `BreadcrumbList` + `ItemList`.
- `/blog/{slug}/` (and `/es/blog/{slug}/`) = 10 routes — **NEW (1.18)** Phase 1.18 §6. Seven sections per post: Hero (white, eyebrow + H1 + dek + meta + featured image below the meta strip — option B per ratified D14.4) → ProseLayout body (white, shared band with hero — handover §2 D14 row 2; inline cross-link + optional `<ServiceAreaStrip excludeSlug={citySlug} inline />` near body bottom) → FAQ (cream, optional) → Related (white) → bottom CTA (cream, amber, per-category H2 with `{city}` interpolation). 5 posts × 2 locales = 10 SSG routes.
- `/request-quote/` (and `/es/request-quote/`) — **NEW (1.20), Step 4 Google Places autocomplete added in B.10** quote wizard. Compact page header + step indicator (desktop dots / mobile bar) + tip card + opacity-only step crossfade (200ms) between five steps + Review/Submit. Step 1 = audience tile select (D4) reusing Phase 1.06 audience photos. Step 2 = audience-driven service multi-select with primary radio (D5). Step 3 = audience-conditional details (D6). Step 4 = name/email/phone (US-formatted)/address with IL-defaulted state and **`data-autocomplete-state="loading|ready|error|disabled"` on the street wrapper (B.10) — picking a Google Places suggestion auto-fills city + state + zip; manual entry still works**. Step 5 = single `.card-cream` review with per-step Edit links and an amber Submit. URL-driven step via `?step=N`; autosave Steps 1–3 only to `localStorage` with 30-day expiry behind `NEXT_PUBLIC_WIZARD_AUTOSAVE_ENABLED` (Step 4 PII never persists, per ratified D9). Resume toast on return-visit. Validation on-blur + on-Next with scroll-to-error and focus-to-error (1.11 ContactForm precedent). Sticky-Next is a single `<button>` DOM (CSS positions for mobile vs desktop). Schema: zero (deliberate per D15). Submit handler is a Part-1 simulation (`console.log` + `wizard_submit_succeeded` event + `router.push('/thank-you/?firstName=…')`); Phase 2.06 swaps for real `/api/quote` POST behind `WIZARD_SUBMIT_ENABLED`. Navbar amber "Get a Quote" CTA hidden on this route only (D2).
- `/thank-you/` (and `/es/thank-you/`) — **NEW (1.20), Calendly upgraded in 2.07.** Post-conversion landing. `<meta name="robots" content="noindex,follow">` because the route renders user-supplied data (`?firstName=…`). Five sections: confirmation hero (cream, lucide `CheckCircle2` + eyebrow + H1 with `{firstName}` interpolation) → **real Calendly inline widget via `<CalendlyEmbed/>` (white)** → "What happens next" 3-step explainer (cream) → 3-Q FAQ via native `<details>` (white, no per-item wrapper) → soft return CTAs (cream — projects/blog/home, **zero amber**). `firstName` is server-side-sanitized (HTML-strip + control-char strip + 50-char clamp), falls back to `thanks.titleFallback` when missing. Schema: zero (deliberate per D15). Section 2 header uses `thanks.calendly.h2` ("Book a 30-min consult with Erick") + `thanks.calendly.sub` ("Pick a time that works for you. No commitment.") keys.
- **AI chat widget** — sitewide bottom-right amber bubble (56px circle, lucide `MessageCircle`, `--z-chat: 50`). Hidden on `/request-quote/` only (ratified D17 = B); visible on `/thank-you/` and everywhere else. Gated by `NEXT_PUBLIC_AI_CHAT_ENABLED` flag (default `false` in Part 1; flip to `true` to preview locally). The collapsed shell is **7,978 bytes gzipped (≤ 8KB ceiling)** — verified post-build. The expanded panel (welcome state + 3 suggested-prompt chips, message log with assistant + user bubbles, composer with auto-grow textarea + char hint, lead-capture inline form using `WizardField` at `compact` density, kebab menu with Reset, error states for network/rate/api, mobile bottom-sheet `<dialog>` + `showModal()` with drag handle) is `dynamic`-imported on first bubble click — separate **4.9 KB gzipped chunk**. Streaming-stub tokens at ~30ms with a 400ms typing delay + 800ms typing indicator. Inline content is plaintext + URL auto-link only (ratified D24 = A); full Markdown subset deferred to Phase 2.09. Per-locale `sessionStorage` history namespace (`sunset_chat_history_<locale>`). Cookie consent gate (D29) is a stub default-true; Phase 2.11 wires the real banner.
- `/og/{resource,blog}/{slug}/?locale={en,es}` — **NEW (1.18)** Open Graph image route handlers (`next/og` `ImageResponse`). 1200×630 PNG per detail page; per-locale via the `?locale=` query.
- `/og/fallback` — **NEW (1.18)** sitewide branded OG fallback for index pages and any path without a per-content variant.
- `/dev/system` (and `/es/dev/system`) — design-system smoke-test page, unchanged from Phase 1.04.
- Phase 1.05 chrome (sticky navbar, footer, skip-link, language switcher, sitewide `LocalBusiness` JSON-LD) wraps every page unchanged.
- Navbar State B (translucent + blur) triggers on the homepage hero AND on every audience-landing + service-detail hero (NavbarScrollState extended in 1.09 with a 5-line pathname check).
- Audience-accent CSS custom properties (`--audience-accent` + `--audience-chip-bg`) applied via `data-audience="{slug}"` on the page wrapper. Scoped per page; buttons stay green/amber regardless.
- Per Phase 1.08 §5 schema spec:
  - Audience landings emit `BreadcrumbList` + `ItemList` JSON-LD.
  - Service detail pages emit `BreadcrumbList` + `Service` + `FAQPage` JSON-LD.
  - The sitewide `LocalBusiness` (Phase 1.05) + `WebSite` (homepage only) continue unchanged.
- Per Phase 1.18 §7 schema spec:
  - Resources index + Blog index emit `BreadcrumbList` + `ItemList` JSON-LD.
  - Resource detail pages emit `BreadcrumbList` + (`Article` | `HowTo`) + `FAQPage` (if FAQ) JSON-LD.
  - Blog post pages emit `BreadcrumbList` + `BlogPosting` + `FAQPage` (if FAQ) JSON-LD.
  - Author resolution: `'Erick Sotomayor'` → `Person` with About `#erick` URL; `'Sunset Services Team'` → `Organization` (no URL); other strings → `Person` no URL.
- All 38 (en+es × 19) audience and service routes pre-rendered at build time via `generateStaticParams`.
- FAQ accordions are SSR `<details>` with progressive enhancement to a client island for chevron rotation. **No per-item `<AnimateIn>`** — the primary lever for closing the homepage's mobile P=86 gap on these new templates.

> **Phase 2.01 note:** No `localhost:3000` behavior changed in this phase. Phase 2.01 is account creation only — no routes added, no source code touched. Working tree at end of phase = Phase 1.20 code + Phase 2.01 doc updates.

## What works (Phase B.12 additions)

- **A fifth division — Trenchless & Directional Boring — renders on the existing dynamic templates.** `/trenchless/` (landing) + `/trenchless/{conduit-installation,trenching-excavation,sewer-line-replacement,missile-boring,handhole-pull-box,pipe-fusing}/` and the `/es/...` mirrors all return 200 with full sections (hero · qualifier pills · 6-service grid · featured projects · why-Sunset · social-proof credentials · 5-item FAQ · CTA), driven entirely by `[locale]/[division]/page.tsx` + `[service]/page.tsx` from `DIVISIONS` + `SERVICES` — no new page templates.
- **Data layer:** `Division` union + `DIVISIONS` (appended last) + `DIVISION_META.trenchless` (hero/tiles alias the `commercial` audience assets); dedicated **`TRENCHLESS_FACTORS`** (run-length/depth/ground · pipe diameter/material · site-access/locating/restoration — 3 factors, matches the "depends on three things" explainer lead; never `GENERIC_FACTORS`); 6 `Service` objects with honest, fabrication-free bilingual copy; `[data-division='trenchless']` accent = `--color-sunset-orange-700`, chip-bg `--color-sunset-orange-50` (orange-700 pill text = 4.62:1 AA).
- **Surfaced everywhere the four siblings are:** navbar mega-panel (6 columns, `lg:grid-cols-6`, no overflow) + mobile drawer (auto via `SERVICES_PANEL`); homepage divisions block (5 cards in one row, `lg:grid-cols-5`, no UNILOCK chip) + `HomeServicesOverview` DOT_COLOR + 5-button CTA row; footer link; quote wizard Step-1 tile + `getStep3Group`→`'residential'` + `/api/quote` Zod enum + `quoteLead`/`quoteLeadPartial` Sanity option lists + `QuoteLeadAlertEmail` `DIVISION_LABEL`; contact dropdown + `contactSchema` enum + `contactSubmission` Sanity option + `ContactAlertEmail` label; sitemap (14 routes, auto); JSON-LD (landing BreadcrumbList+ItemList, service BreadcrumbList+Service); `/projects` filter chip taxonomy + project-detail label map; wizard `VALID_DIVISIONS`; legal terms/privacy prose; portfolio-automation keyword table.
- **i18n:** `division.trenchless.*` (mirrors `division.waterproofing.*` 1:1) + `home.divisions.trenchless`, `home.services.cta.trenchless`, `servicePage.related.h2.trenchless`, `chrome.nav.servicesPanel.trenchless*`, `chrome.footer.links.trenchless`, contact `category.trenchless`, `projects.{tag,filter}.trenchless`, `wizard.division.trenchless` + `wizard.step2.title.trenchless` — EN/ES leaf parity **1335 = 1335** (identical key paths). Nav column header + homepage chip + projects filter use the short form **"Trenchless" / "Perforación"**; the full **"Trenchless & Directional Boring" / "Perforación Direccional y Sin Zanja"** is used on the landing, footer, contact dropdown, and wizard tile.
- **Verification (clean prod build):** `build` 202/202 · `lint` 0 err · `tsc` 0 src err · `validate:schema` 0/0 · `validate:seo` 0/0 (196 URLs) · `validate:a11y` 0/0 (axe 0 / LH a11y 100) · `validate:mobile` 0 err · `validate:links` hard 0 (14 trenchless routes 200) · `test:consent` 23/23. Detail in `Phase-B-12-Completion.md`.
- **Bridge state (expected):** all trenchless imagery is a commercial-alias placeholder pending real photography (swap via `imageMap.ts`/`DIVISION_META` only); service-page `FAQPage` schema + rendered FAQ appear once Sanity `service:trenchless:<slug>` FAQ docs are added (operator follow-up + Studio re-deploy for the new option lists).

## What works (Phase B.03e additions)

- **All four legal routes render hard-coded English legal text.** `/privacy/`, `/es/privacy/`, `/terms/`, `/es/terms/` return HTTP 200 with the full self-contained Privacy Policy + Terms of Service — no Termly iframe, no `app.termly.io` request, no "being prepared" fallback. Content lives in `src/content/legal/{privacy,terms}.tsx` (each exports `title`, `effectiveDate`, `lastUpdated`, and a `Body` React component of semantic `<h2>`/`<p>`/`<ul>`/`<li>`/`<strong>`). Editing legal text is now a code change + redeploy — intentional git audit trail (the M.12 Erick handover must say legal edits go through the developer).
- **English-only, served on both locales.** EN + ES routes render the SAME English document wrapped in `<div lang="en">`; a localized `legal.englishOnlyNote` line sits above it, hero eyebrow + breadcrumb stay localized. Effective Date + Last Updated = **June 2, 2026**, shown at the top of each document and in the hero subtitle (`legal.<type>.hero.lastUpdated` is now an ICU `{date}` template fed from the content module — single source of truth).
- **Termly fully removed.** `src/components/legal/TermlyPolicyEmbed.tsx` deleted; the `embed-policy.min.js` script + iframe div + the B.06 progressbar `MutationObserver` a11y workaround gone; `legal.embed.*` keys dropped; the `.termly-embed-wrap` CSS replaced by a namespaced, token-driven `.legal-doc` prose block (with the M.11c `overflow-wrap` mobile hardening); `.termly-ids.txt` + its `.gitignore` line deleted; all 5 `NEXT_PUBLIC_TERMLY_*` removed from `.env.local` + `.env.local.example`. `grep -rn` finds zero `TermlyPolicyEmbed` / `app.termly.io` / `embed-policy.min.js` / `NEXT_PUBLIC_TERMLY_`. (Vercel-side env-var removal is the one open item — blocked on a valid Vercel token; vars are inert.)
- **Cookie-consent banner + Consent Mode v2 untouched.** Banner still links to `/privacy/`; `npm run test:consent` passes 23/23. Wizard consent (`WizardStep5Review`) + footer legal links resolve to the new `/privacy/` + `/terms/`. The AI chat widget has no privacy link.
- **Accessibility statement corrected.** `accessibility.body.knownLimitations` dropped the now-obsolete "Termly-hosted privacy policy iframe" item; the Calendly widget remains the listed third-party-embed limitation. Both locales + render in parity.
- **Verification:** `build` ok · `tsc` 0 · scoped `eslint` 0 · `validate:schema` 0/0 (legal PASS, 3 blocks each) · `validate:a11y` 0/0 (axe AA 0, Lighthouse 100, one H1) · `validate:seo` legal routes PASS (the run's 18 errors are the known stale-local-Sanity-cache blog/project red herring per M.11c-E1; Vercel Preview resolves them to 0/0). Detail in `Phase-B-03e-Completion.md`.

## What works (Phase M.11c additions)

- **`npm run validate:mobile`** — a new Playwright headless-Chromium mobile-rendering gate (`scripts/validate-mobile.mjs`) driving the full phone-viewport matrix (320 / 360 / 375 / 390 / 414px + 667×375 landscape + 390×620 short-height + 768 + 1280 desktop), flagging horizontal scroll / cutoff / overflow, capturing per-viewport screenshots to a gitignored dir, exiting 0 only at zero errors. The 6th validation harness. **Every representative surface renders with no horizontal scroll / no cutoff across 320–414px + landscape/short/768**, 0-error on localhost AND Preview; **1280 desktop 0/0 — desktop unchanged** (all fixes mobile-first / responsive-prefixed).
- **320px horizontal cutoff on /contact + /thank-you fixed** — the Calendly embed's flat `minWidth:320` is now `min(320px,100%)`.
- **Notched-phone safe areas resolve** — `viewport-fit=cover` set on the viewport export, so `env(safe-area-inset-*)` (consent banner, wizard sticky bar, chat composer + bubble) takes effect on notched devices.
- **Width-containment + legibility hardening:** the `.pac-container` (Google Places dropdown) + `.termly-embed-wrap` wrapper (our wrapper only) can no longer push the page wider than the viewport; prose uses `overflow-wrap:anywhere`; the chat bubble gains a bottom safe-area inset.
- **`AudienceHero` (all 4 division landings) migrated to `min-h`+`svh`** (the one hero not already on the M.10f pattern), so it grows to fit content on short / mobile viewports.
- **Tap + overflow polish:** 5 amber CTAs clamp min-width (`280px`→`min(280px,100%)`), 2 wizard toasts clamped to viewport width, service-area map pins enlarged (16→22) for tappability.

## What works (Phase M.10g additions)

- **`/projects` + `/es/projects` read as a city-grouped portfolio.** Every tile with an assigned city shows a clean **city-only** label (e.g. `Aurora`) sourced from the project's structured Sanity `city` reference (`city->name`); same-city jobs cluster; projects with no city render no label and sort **last**. Order: city A→Z, location-less last, `year` desc then `slug` A→Z within a city, pinned to the `'en'` collator so EN + ES cluster identically. Division filter chips unchanged; sort + labels hold within each filtered view.
- **Shared `ProjectCard` consistency.** The same city label renders on the homepage / About "Recent work" row (`HomeProjects` — had no location line before) and `RelatedProjects`. "Recent work" keeps its recency order (the new sort is `/projects`-index only).
- **One resolver, one source of truth.** `src/lib/projects/resolveProjectCity.ts` resolves the label (structured `cityName` → static `locations.ts` table by slug → `undefined`, never the raw slug), shared by tiles + sort. `Project.cityName?` carried through both Sanity adapters.
- **Privacy rule holds.** Label is city-only; titles still pass through `stripStreetNumber()`. (Street names still in some Sanity titles are deferred content for Goran/Erick.)
- **Data caveat:** only 2 of 10 Sanity projects carry a `city` reference, so only 2 tiles are labeled (Aurora) until the content team assigns the rest — the mechanism auto-labels + clusters as cities are added.

## What works (Phase M.11b additions)

- **`npm run validate:links`** — a permanent Playwright headless-Chromium BFS link-integrity gate (`scripts/validate-links.mjs`): seeds from `/sitemap.xml` + reachable-excluded routes, BFS-crawls every same-origin internal link in both locales, classifies broken / slash-only / locale-prefix / different-page / missing-anchor + dead-external + wrong-destination, exits non-zero on any hard internal failure (`--strict` also fails on warnings). The 5th validation harness.
- **Remaining link fixes (beyond M.11's deep-link fix):** ES project-detail breadcrumbs no longer double-prefix to `/es/es`; `/contact#calendly` scrolls to the Calendly section (`id` added); the two genuine trailing-slash 308 sources (empty-`citySlug` project city link + blog CTA) are gone; division-landing + service-detail "featured projects" tiles link the division-filtered `/projects?division=<slug>` index (were 404ing on synthetic / imageKey slugs).
- **Confirmed clean (no change needed):** all 22 cities' featuredServices `(division,slug)` pairs, the SVG map pins (aria-label↔slug), the `?division=` projects filter chips (all 4), the PortableText link serializer, the wizard `?division=` reader, NAP `tel:` / `mailto:` everywhere, and zero `?audience=` / hardcoded `/en/` / old-IA in-app links.

## What works (Phase M.11 additions)

Phase M.11 was a QA-and-fix sweep on the `worktree-phase+m11-qa-sweep` branch (pending merge). **If this disagrees with the live `main`, `main` wins until merged.**

- **All three validation harnesses fully green** for the first time since the M.10c drift: `validate:schema` 22/22 · 0/0, `validate:seo` 184 URLs · 0/0 (the 4 pre-existing `aurora-driveway-apron` errors + a newly-surfaced 6-slug sitemap drift both reconciled), `validate:a11y` 20/20 · 0/0. `tsc` 0, `lint` 0, `build` 190 pages, all 5 npm `test:*` pass.
- **The projects-filter deep-link works from every division landing + service-detail page** — the "View all / View our work" CTAs were emitting `?audience=` (silently ignored) instead of `?division=`; now they pre-filter correctly.
- **The quote-wizard Step-3 question group is consistent end-to-end** — landscape no longer shows mismatched/empty commercial fields at the Step-5 review or leaks stale residential answers; switching division mid-flow clears stale Step-3 answers. (Trade-off: commercial-landscape leads answer the residential question set; a future enhancement could restore commercial-landscape questions by collecting `propertyType` before Step 3.)
- **About-page team cards render on the cream surface** (`card--cream` typo fixed); **18 new city pages show project tiles** instead of empty green boxes.
- **A cluster of WCAG 2.2 AA gaps closed** — focus-visible rings on the chat composer + wizard tiles, valid mega-panel ARIA (no `role=menu` without arrow-key nav), ≥24/44px target sizes, accurate `aria-modal`, translated chat typing label, reduced-motion + pagination + step-indicator contrast.
- **JSON-LD cleaner** — `Person.image` no longer emits a 404 URL on `/about`, the homepage WebSite no longer advertises a non-existent `/search`, and the service-detail FAQPage can't emit an empty `mainEntity`.
- **i18n tidier + more correct** — ~470 orphan keys removed, EN/ES leaf-key parity restored (1352 = 1352), the transactional ES surfaces (contact form, thank-you, chat lead capture, Calendly fallback) use `usted`, and the owner's name reads "Erick Valle" (was "Erick Solis"). `[TBR]` strings untouched (0 stripped). ES-locale-aware links in the 3 visitor confirmation emails; the chat knowledge-base founding year is 2000 (was 2001).
- **A build-time guard now enforces globally-unique service slugs**; the leaked GCP Places key is redacted on-branch (rotation is an operator action — see `Phase-M-11-Completion.md` §5).

See `Phase-M-11-Completion.md` for the full finding/fix breakdown, flag-and-log handoffs, and verification matrix.

## What works (Phase M.03 additions)

- **Codex Spanish review pass completed across active source surfaces.** `src/messages/es.json`, `src/data/{services,locations,divisions,blog,resources}.ts`, visitor-facing email copy, chat persona/knowledge labels, and the FAQ migration seed copy were read against the locked M.01f1 glossary/register matrix and patched where the Spanish was grammatically wrong, glossary-drifted, or too English-shaped.
- **Live Sanity `.es` fields received deterministic fixes.** `scripts/m03-spanish-fixes.mjs` patched 37 docs on first run and reports 0 changes on re-run. Idempotent and committed for audit/replay.
- **Human native Spanish review remains explicitly deferred.** M.03 closes the launch-blocking Codex pass only; the 2026-05-27 Decisions entry records that launch still has not had human native review and that the 4 M.01f1 boundary cases remain for post-launch native judgment.
## What works (Phase M.10b additions)

- **`/qa/` expanded to Goran's 7-category, 28-question library** (was 25×5 in M.01e). Categories in display order: Project Planning & Process (5), Pricing & Investment (4), Materials & Installation (5), Retaining Walls & Drainage (3), Outdoor Living Features (3), Maintenance & Warranty (4), Trust & Credibility (4). Each category renders an eyebrow ("Planning" / "Pricing" / etc.) above the H2. Hero dek updated for the new count. CTA + schema unchanged.
- **Homepage "Why Sunset" section** between the projects band and the bottom CTA. Eyebrow "Why Sunset" (ES: "Por qué Sunset") + H2 "We answer to our customers — not outside investors." (ES: "Le respondemos a nuestros clientes — no a inversionistas externos.") + 1-line dek. 8 cards in a `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` grid; each has a 44×44 sunset-green-50 icon disc (lucide: Home / Shield / DollarSign / Award / FileCheck / Compass / Heart / TrendingUp), label, 1-sentence body. Cards from Marcin's "Why Homeowners Choose Sunset" list verbatim (EN). Cream surface; existing `.card` styling.
- **`/about/` brand-story band shows 4 paragraphs.** p1 (Nick founds in 2000 — adds "locally owned and family operated since day one — based in Aurora, IL"), p2 (Erick takes over in 2018 — adds "we answer to our customers, not to outside ownership groups or private-equity quotas"), p3 (Marcin leads hardscape — adds workmanship / fair-pricing / family-handoff framing), p4 (NEW: brand-values summary — "our goal isn't to sell projects — it's to help homeowners make smart investments"). Existing eyebrow + 2-line H2 + image column + bottom link row unchanged.
- **`scripts/seed-faq-content-integration.mjs` ready to run.** Operator runs `npx tsx scripts/seed-faq-content-integration.mjs` with `SANITY_API_WRITE_TOKEN` to seed 7 SEO-targeted FAQs: 5 on `/hardscape/patios-walkways/` (paver patio cost, pavers vs home value, concrete vs pavers for winters, paver patio lifespan, best paver brand), 1 on `/service-areas/naperville/` (HOA approval), 1 on `/hardscape/driveways/` (winter driveway option) — EN + ES. `order: 100+` keeps them at the end of each scope. Idempotent.

## What works (Phase M.10 additions)

- **Scroll-triggered animations no longer flicker.** `AnimateIn` / `StaggerContainer` / `StaggerItem` render at the `animate` state on first paint via `initial={false}`. No SSR→hydration `opacity: 0` flash. Trade-off: lost the scroll-triggered entrance fades on the global primitives.
- **Navbar mega-panels open on hover (desktop) and tap (touch).** `MegaPanelTrigger` + Services/Resources panels use `onMouseEnter`/`onMouseLeave` instead of `onPointerEnter`/`onPointerLeave` so touch falls back to tap-to-open via `onClick`. Open delay 80ms; close grace 150ms. Click + keyboard (Enter / Space / Esc) + focus trap + click-outside-to-close preserved.
- **22 city pages + service-areas grid cards show distinct hero photos.** `LOCATION_HERO` + `LOCATION_CARD` in `src/data/imageMap.ts` mapped to 24 unique photos (Aurora→hero-hardscape, Naperville→hero-residential, Hinsdale→hero-pergolas-pavilions, Oak Brook→hero-outdoor-kitchens, Plainfield→tile-retaining-walls, Wheaton→hero-lawn-care, Batavia→hero-landscape-design, Glen Ellyn→hero-tree-services, Elmhurst→hero-patios-walkways, Clarendon Hills→hero-fire-pits-features; 10 of 22 spot-checked, all distinct).
- **Service-page hero CTAs no longer crowd the next section.** `ServiceHero.tsx` inner-content `pb` ramp is now `pb-10 sm:pb-14 lg:pb-16 xl:pb-20` (was `pb-8 sm:pb-10 lg:pb-12`). 56 routes (28 services × 2 locales) fixed in one template change.
- **Team cards on `/about/` show brand-cream initial avatars.** Erick / Nick / Marcin render "EV" / "NV" / "M" in sunset-green gradient tiles via the new `<InitialsAvatar/>` primitive. Real-photo swap path preserved (`team.ts` photo field optional).
- **5 blog posts + 5 resource articles show real photos.** Each card's `featuredImage` resolves to a distinct topically-matched real photo (patio-cost→hero-patios-walkways, lawn-calendar→hero-lawn-care, snow-for-commercial→hero-commercial-snow-removal, sprinkler-tune-up→hero-sprinkler-systems, why-unilock-premium-pavers→projects/driveways-1; resources: patio-materials-guide→projects/patios-walkways-2, how-to-choose-a-landscaper→hero-landscape-maintenance, lawn-care-glossary→projects/lawn-care-2, snow-service-levels→projects/commercial-snow-removal-2, dupage-hardscape-permits→projects/retaining-walls-1). All 10 distinct.
- **Contact form Service-category dropdown is the 4-division model.** `/contact/` shows Landscape / Hardscape / Waterproofing / Snow Removal / Other (ES: Paisajismo / Hardscape / Impermeabilización / Remoción de Nieve / Otra). Updated through `ContactForm.tsx`, `contactSchema.ts` Zod enum, `contactSubmission.ts` Sanity option list, `ContactAlertEmail.tsx` label rendering (with `(legacy)` fall-through for old enum values), and `messages/{en,es}.json`. Studio re-deploy required to push the new option list.
- **Navbar identical on `/request-quote/`.** The orange "Solicitar Presupuesto Gratis" CTA now renders in the wizard navbar (1 amber CTA, matching 1 of the 2 amber CTAs on `/about/`). Mobile drawer's parallel carve-out reverted.
- **Footer reflects the 22-city shape.** Curated 6 cities (Aurora / Naperville / Wheaton / Batavia / Oak Brook / Hinsdale) + "See all 22 cities →" link mirror the navbar mega-panel pattern. Lisle + Bolingbrook removed. The stray `bg-white` Unilock-badge chip in `FooterBrand` is gone.
- **`/accessibility/` and `/es/accessibility/` return 200 with real content.** Server-rendered, mirrors the LegalPageHero chrome shape but renders real prose (no Termly embed). 5 sections — Our commitment, What we've done, Known limitations, Feedback (mailto + tel), Last reviewed. New `accessibility.*` i18n namespace in both locales (ES `usted`). Footer's `/accessibility/` link now resolves.
- **Sitemap, SEO + a11y harness configs include the new routes.** `src/app/sitemap.ts` lists `/accessibility` (EN + ES); `scripts/validate-seo.mjs` `EXPECTED_PATHS` adds it; `scripts/validate-a11y.mjs` adds `/accessibility` with label `accessibility-statement`.

## What works (Phase M.01f1 additions)

- **Canonical Spanish glossary locked.** `Sunset-Services-TRANSLATION_NOTES.md` §M.01f1 holds the variety lock (LatAm/Mexico-leaning), the surface-by-surface `tú`/`usted` register matrix, the 4 division names, all 28 service names, 14 common terms, brand/proper-noun list, tone guidance with good-vs-bad examples, boundary-case decisions, and the open questions for Erick.
- **Spanish surface for M.01d/e/e-pt2 is glossary- and register-consistent.** The 4 renamed services (Espacios Bajo el Piso, Nivelación de Concreto, Limpieza de Aceras, Remoción Comercial de Nieve) are consistent across `services.ts` + the nav mega-panel; 0 lingering old names in `src/`+`scripts/`. New service content uses "estimado" uniformly. `qa.*` + the Sanity FAQ source are clean `usted`; marketing surfaces are clean `tú`.
- **`es.json` parses cleanly via Node** and differs from the prior commit only by the 4 intended nav-label realignments.
- **M.03 review pool ready.** Erick reviews one coherent body of text, with every deliberate ambiguity (cocheras-vs-entradas, Presupuesto-vs-estimado, Hardscape, wizard register, arado calque) pre-flagged in TRANSLATION_NOTES §9.
- **Working tree hygiene:** line endings normalized to LF, `.gitattributes` added, `es.json` NUL corruption fixed. 3 unrelated uncommitted files (ProjectGallery / RelatedProjects / ServiceAreaMap) deliberately left untouched.

## What works (Phase M.01e-pt2 additions)

- **Quote wizard fully on the 4-division model.** Click "Get a quote" on any of `/landscape/`, `/hardscape/`, `/waterproofing/`, `/snow-removal/` → wizard Step 1 pre-selects the division via `?division=<slug>`. Step 2's service list is filtered by `getServicesForDivision()`. Step 3's question set is keyed by `(division, propertyType)` via the new `getStep3Group()` helper (hardscape always uses hardscape group; snow-removal uses commercial; landscape + waterproofing default to residential; landscape + propertyType=commercial switches to commercial). Step 4 carries a new required `propertyType` radio at the top (residential/commercial); Next blocked until selected. Step 5 review renders Division + Property type rows.
- **localStorage migrated v1 → v2.** Storage key bumped `sunset_wizard_progress_v1` → `sunset_wizard_progress_v2`. One-shot migrator on first read of v2: `audience: 'hardscape'` → `division: 'hardscape'`; `audience: 'residential'|'commercial'` → division dropped (visitor re-picks). After migration, v1 key removed. Resume toast suppressed when migration salvaged nothing.
- **Zod schemas migrated.** `/api/quote` requires `division` + `propertyType`. `/api/quote/partial` accepts optional `division`; no `propertyType` (Step 4 PII boundary). Both reject unknown keys.
- **Sanity `quoteLead` + `quoteLeadPartial` schemas migrated.** `audience` renamed to `division` (4-value list); new required `propertyType` on `quoteLead`. Idempotent migration at `scripts/migrate-quotelead-audience-to-division.mjs` (operator runs once after deployment). Lead-email templates render Division + Property type rows (`QuoteLeadAlertEmail.tsx`). Division-landing CTAs (`AudienceHero.tsx` + `AudienceCTA.tsx`) deep-link to `?division=<slug>` (were `?audience=`).
- **22 city pages show the canonical 6-service mix.** 2 Landscape + 2 Hardscape + 1 Waterproofing + 1 Snow Removal per locked decision #11 of M.01e (per-city heuristic in `Sunset-Services-Decisions.md` 2026-05-26 M.01e-pt2 entry).
- **Service-areas SVG map labels read cleanly.** 8-city static-label allowlist (Aurora, Naperville, Wheaton, Batavia, Oak Brook, Hinsdale, Plainfield, St. Charles); the other 14 render only the pin dot with `aria-label` on the link. Dense-cluster overlap eliminated.
- **New analytics events.** `wizard_division_selected` (Step 1 tile select) + `wizard_property_type_selected` (Step 4 radio select). Both informational; both registered in `ANALYTICS_EVENTS` + `WIZARD_EVENTS`.

## What works (Phase M.01e additions)

- **4 division landings live** at `/<locale>/<division>/` for `landscape`, `hardscape`, `waterproofing`, `snow-removal` (8 routes × 2 locales).
- **28 service detail pages live** at `/<locale>/<division>/<slug>/` (56 routes). Service slugs are now globally unique; the 2 audience-duplicated `snow-removal` entries retired and merged into `driveway-snow-removal` + `commercial-snow-plowing`.
- **22 city pages live** at `/<locale>/service-areas/<slug>/`. Lisle + Bolingbrook retired (404 by `isLocationSlug`; redirect maps `/service-areas/lisle/` + `/service-areas/bolingbrook/` → `/service-areas/`).
- **1 Q&A page live** at `/<locale>/qa/`. 25 questions × 5 categories as `<details>` accordions. `BreadcrumbList` schema; no `FAQPage` (per locked decision — avoids diluting per-service/per-city FAQ schemas).
- **Homepage 3-audience block → 4-division block.** `HomeAudienceEntries.tsx` renders 4 division cards. Hero unchanged.
- **Navbar mega-panel rewired to 5 columns.** 4 division columns + 1 service-areas column. Mobile drawer mirrors via existing `DrawerAccordion` logic.
- **Footer service links migrated.** `chrome.footer.links.{residential, commercial}` → `{landscape, waterproofing}`. `snowRemoval` URL flipped from `/residential/snow-removal/` to `/snow-removal/`.
- **Service-areas map redrawn with 22 pins** via Web Mercator projection (`projectGeo.ts`). Pin coordinates computed once + committed into `locations.ts` so the SVG renders deterministically (label de-overlap closed by M.01e-pt2).
- **56 permanent redirects in `next.config.ts`.** 14 source/destination pairs × 2 locales × 2 slash variants. Hardscape service URLs unchanged.
- **Sanity FAQ migration script** at `scripts/migrate-faq-to-divisions.mjs` (idempotent). Operator runs once with `SANITY_API_WRITE_TOKEN`.

## What works (Phase M.01d additions)

- **Data foundation for the 4-division / 22-detail-city / Q&A site shape.** `src/data/services.ts` exports `Division` type and 30 services (16 existing + 14 new), each with a required `division` field. `src/data/locations.ts` exports 24 cities (6 existing + 18 new) with full bilingual content.
- **Backwards-compat preserved.** The 38 audience-aware service routes (`/residential/<slug>/`, `/commercial/<slug>/`, `/hardscape/<slug>/`, ×2 locales) and 6 city detail pages still render unchanged. `LOCATION_SLUGS` stays at 6 entries so `isLocationSlug('hinsdale')` returns false → new-city URLs 404 by design.
- **Homepage H1 now reads "Build your outdoor legacy"** (EN) / "Construye tu legado al aire libre" (ES, `tú` register).
- **Q&A i18n strings ready.** `qa.*` namespace in both locales: meta, hero (eyebrow/h1/dek), 5 categories, 25 questions with 2–4 sentence answers, CTA. ES uses `usted`. M.01e builds the page.
- **Project address strip wired.** `stripStreetNumber(address)` helper at the top of `src/app/[locale]/projects/[slug]/page.tsx` strips leading street numbers (and slash-separated multi-numbers like "807/811 ") from the metadata title + breadcrumb. No-op for current placeholder projects; activates when the M.01c uploader lands real content.
- **Three root docs reflect the locked target shape.** `Sunset-Services-Plan.md` §3 + §4 + §11; `Sunset-Services-Phase-Plan-Continuation.md` Bucket M table extended with M.01d/M.01e/M.01f rows + dependency note; `Sunset-Services-Decisions.md` new 2026-05-26 entry covering all 15 locked decisions + 4 in-phase off-spec resolutions.
- **Zero new visible routes this phase.** Sitemap, schema ItemList on `/service-areas/`, `CitiesGrid.tsx`, `ServiceAreaMap.tsx` updated to consume the 6-visible subset (via `LOCATION_SLUGS` or new `getVisibleLocations()`). The `/[locale]/[audience]/[service]/` `generateStaticParams` filters out new-division services with no `audience`. Visible sitemap, served URLs, and SVG-map pins stay at 6 cities + 16 (audience-aware) services.

## What works (Phase B.10 additions)

- **Google Places address autocomplete on quote wizard Step 4.** The Phase 1.20 `data-autocomplete-stub="address"` marker is replaced by `data-autocomplete-state="loading|ready|error|disabled"` on the same wrapper. Manual entry stays fully functional (D5 — autocomplete is additive, never blocks Next). Helper text appears only during `loading` ("Loading address suggestions...") or `error` ("Type your address manually.") states, EN + ES.
- **`src/lib/google/placesAutocomplete.ts`** is the entire B.10 source surface (~190 LoC). Exports `parsePlaceToFields(place): Partial<AddressFields>` (D6 partial-fill map — returns ONLY keys whose source components were present in Google's `address_components` so manual values on other fields are preserved) and `useGooglePlacesAutocomplete({inputRef, onPlaceSelect})` (lazy-loads the Maps JS library via a module-scope singleton on Step 4 mount, attaches the legacy `google.maps.places.Autocomplete` with `componentRestrictions:{country:['us']}` + `fields:['address_components']` + `types:['address']`, returns `{state, error?}`, cleans up via `google.maps.event.clearInstanceListeners()` on unmount for StrictMode safety). One TEST-only hook: `readApiKey()` checks `window.__SUNSET_PLACES_KEY_OVERRIDE__` first so the harness can exercise the disabled branch without a second `next build` (`NEXT_PUBLIC_*` is inlined at build time). Never set in production.
- **`WizardField` accepts an optional `inputRef`** (forwarded to `text`/`email`/`tel` inputs only), used by Step 4's street field. Backwards-compatible.
- **`fireWizardEvent(WIZARD_EVENTS.ADDRESS_AUTOCOMPLETED, {step: 4, source: 'autocomplete'})`** fires after every successful place select with at least one field filled. Dispatched on `document` as `sunset:wizard-event` with flat-shape `detail: {name: 'wizard_address_autocompleted', step: 4, source: 'autocomplete'}` per the Phase 2.10 `AnalyticsBridge` convention (minor off-spec from the plan's nested `{event, payload}` which would have broken the bridge — logged 2026-05-19). Registered in `ANALYTICS_EVENTS.WIZARD_ADDRESS_AUTOCOMPLETED` + `WIZARD_EVENTS.ADDRESS_AUTOCOMPLETED`. Informational, not a conversion; zero PII-suspect keys.
- **Verification harness** at `scripts/test-wizard-autocomplete.mjs` (`npm run test:wizard-autocomplete`). Spawns `next start` on port 3076; Playwright headless Chromium. Pre-seeds `window.google.maps.places.Autocomplete` as a Mock constructor via `page.addInitScript` (the hook's already-loaded short-circuit picks it up — zero GCP quota burn). Pre-seeds `localStorage['sunset_wizard_progress_v1']` with valid step 1/2/3 data so the WizardShell offers a Resume toast — clicking Resume + Next is the only reliable way to land at Step 4 (direct `?step=4` deflects via the `effectiveStep` memo when `step1.audience` is empty). Waits on `form[aria-labelledby="wizard-step3-h2"]` then `…step4-h2` between clicks so the AnimatePresence exit-animation race doesn't trip the wrong-form Next. 8 tests: T1 (loading→ready + correct constructor options) / T2 (4-field fill from Aurora, IL synthetic place) / T3 (partial fill missing postal_code preserves manual zip) / T4 (event captured, correct flat-shape detail + zero PII) / T5 (manual entry + Next still enabled) / T6 (disabled branch via the `window.__SUNSET_PLACES_KEY_OVERRIDE__=''` escape hatch). **8 / 8 PASS on localhost AND Preview.**
- **Vercel env var `NEXT_PUBLIC_GOOGLE_PLACES_API_KEY`** upserted on Production + Preview (`plain`, id `GumaZ2umvA7TLHzd`). Same value as the existing `GOOGLE_PLACES_API_KEY` (source for Phase 2.16's daily Google reviews cron when GBP/Places access lands). `NEXT_PUBLIC_*` cannot be sensitive — client-bundle-embedded by design; the security boundary is the GCP-side HTTP referrer allowlist.

## What works (Phase B.09 additions)

- **`src/lib/chat/rateLimit.ts` is now backend-swappable** via `CHAT_RATELIMIT_STORE`. Memory mode (current Vercel + local-dev default) preserves the Phase 2.09 in-process `Map` logic bit-for-bit. KV mode hits Upstash Redis via `@upstash/redis@^1.38.0` using `SET chat:burst:<ip> 1 NX EX <2s>` + `INCR chat:daily:<ip>` + conditional `EXPIRE 86400` + one `TTL` read on over-limit. Whole KV body in `try / catch` with fail-open on any Redis error (logs `'[ratelimit] kv check failed'`, returns `{allowed:true}`).
- **Async `checkRateLimit(ip)` surface.** Both branches return `Promise<RateLimitResult>`. `src/app/api/chat/route.ts` adds the single `await`; `git grep -n 'checkRateLimit' src/` confirms no other call sites.
- **`@upstash/redis` auto-detects credential prefixes.** Reads `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` first, falls back to the older `KV_REST_API_*` aliases. Either Marketplace vintage works with zero config.
- **Verification harness** at `scripts/test-rate-limit.mjs` (`npm run test:rate-limit`). Spawns up to 5 isolated `next start` instances. T1–T4 always run against memory; T5–T8 against KV cleanly skip when no Upstash creds present. The marquee KV test (T7) sends 2 POSTs from a fresh IP against boot A, kills the server, sends 1 POST against boot B → 429 daily — proving Redis-side persistence across cold starts. Pre-cleanup `DEL`s the test keys via Upstash REST `pipeline`. **Final localhost: 8 / 8 PASS (T1–T4 real, T5–T8 SKIPPED).**
- **Vercel env var `CHAT_RATELIMIT_STORE=memory`** upserted on Production + Preview (`plain`). The four Upstash credential vars deliberately NOT pre-seeded (empty values would shadow the Marketplace auto-injection).
- **Curl-smoke against the Preview `/api/chat`** confirms memory-mode is live: POST #1 from `8.7.6.5` → 400 invalid, immediate POST #2 same IP → 429 `reason:'burst'` Retry-After: 2.

## What works (Phase B.08 additions)

- **`POST /api/revalidate`** is live behind HMAC-SHA256 signature verification (`SANITY_REVALIDATE_SECRET`; `parseBody` from `next-sanity/webhook`). On a valid fire it routes through `revalidateForDocument()`, calls `revalidateTag(tag, 'max')` for each tag in the doc-type's mapping (Next 16's required two-arg form) + `revalidatePath(concretePath, 'page')` for each concrete EN + ES path, returns `{status:'ok', docType, revalidatedTags, revalidatedPaths, unhandled?}`. 401 missing/invalid-signature, 400 missing `_type`, 500 revalidate-failed (Sanity retries), 200 + `unhandled:true` on unknown `_type` (ack). `runtime = 'nodejs'`, `dynamic = 'force-dynamic'`.
- **`src/lib/sanity/revalidation.ts`** is the single source of truth for "when `_type` X publishes, which tags + paths invalidate." Exports `revalidateForDocument(payload)` + `SanityRevalidationPayload` / `RevalidationResult` types. Owns the `MAPPINGS` table for all 8 doc types. `expandPattern()` resolves `[token]` placeholders: detail patterns (`/blog/[slug]`, `/projects/[slug]`, `/resources/[slug]`) expand to the concrete slug if present, drop otherwise; collection patterns (`/[audience]/[service]`, `/[audience]`, `/service-areas/[city]`) bulk-fan-out to all concrete combos from `src/data/services.ts` (16 (audience, slug) pairs) + `src/data/locations.ts` (6 cities). `withLocales()` emits EN + `/es`; the bare home is the one special case (`/` AND `/es`, not `/es/`).
- **Every GROQ helper in `sanity/lib/queries.ts`** now sets `{cache: 'force-cache', next: {tags: [tag]}}` per the tag schema: projects × 4 → `['project']`; blog × 4 → `['blogPost']`; resources × 4 → `['resourceArticle']`; services chat KB × 1 → `['service']`; locations chat KB × 1 → `['location']`; FAQ scoped × 5 + top FAQ chat × 1 + audience-FAQ stream × 1 → `['faq']`; reviews × 2 → `['review']`. Phase B.07 `getSubscriberByToken` is deliberately UNTAGGED (it uses `writeClient`, never the Next data cache).
- **`POST /api/test/revalidate` flag-gated test route** mirrors the Phase 2.16/2.17 pattern: `REVALIDATE_TEST_ROUTES_ENABLED !== 'true'` → 404 `{status:'forbidden'}`; Bearer auth via the existing `TEST_ROUTES_SECRET`; Zod-validates `{docType, slug?, _id?}` with opaque `invalid-payload`; calls the same helper.
- **Page-level `export const revalidate = 1800`** stays on all 5 Sanity-read route groups (`/blog`, `/blog/[slug]`, `/projects`, `/projects/[slug]`, `/resources`, `/resources/[slug]`, `/service-areas/[city]`, `/[audience]/[service]`) per D2 — the safety net keeps max staleness at 30 min if the webhook breaks.
- **Verification harness** at `scripts/test-revalidate-webhook.mjs` (`npm run test:revalidate`). Spawns `next start` twice (flag-ON, flag-OFF). 14 assertions covering all signature/auth/payload branches + per-doc-type tag and path correctness across blogPost / service / project-without-slug / faq / team / location. Signs valid + invalid bodies via `encodeSignatureHeader` from `@sanity/webhook`. **14 / 14 PASS, 0 FAIL.**

## What works (Phase B.04 additions)

- **Sitewide `@graph` JSON-LD** ships from `src/app/[locale]/layout.tsx` as a single `<script type="application/ld+json">` containing two nodes — `LocalBusiness` (`@id: https://sunsetservices.us/#localbusiness`) + `Organization` (`@id: https://sunsetservices.us/#organization`). Both carry stable identity strings so per-page builders reference them without restating the NAP block.
- **Every per-page `@id` reference resolves.** `Service.provider`, `Article/BlogPosting/HowTo.publisher`, the "Sunset Services Team" author-resolution branch — all emit `{"@id": "..."}` single-key references against the sitewide nodes. The harness verifies this across all 22 representative URLs.
- **`getPublishedReviewsForCity(citySlug, locale)`** at `sanity/lib/queries.ts:398` — new GROQ helper excluding `placeholder !== true` (Phase 2.05's seed testimonials), orders by `publishedAt desc`, returns `PublishedReviewEntry[]` with `quote/attribution/rating/source/publishedAt`. Returns `[]` for every city today; flows real entries when the Phase 2.14 + 2.16 daily Google reviews cron lands.
- **`buildPlaceSchema()` extended** at `src/lib/schema/location.ts` to optionally accept `reviews` + `locale`. When `reviews.length > 0`, the Place node gains `aggregateRating` (`ratingValue` average rounded to 1 decimal, `reviewCount`, `bestRating: 5`, `worstRating: 1`) + `review[]` (each `Review` with `reviewBody`, `reviewRating`, `author: {Person, name}`, `datePublished`). When empty (today), both omitted. SSR-verified on `/service-areas/aurora/`: only base Place fields present.
- **Location-page `ItemList` items flattened** to the `{position, url, name}` ListItem shape audience landings already use (was nested `Service`+`areaServed.Place` stubs). Same `featuredServices` array drives both grid + schema (same-source rule intact). **Projects-index `locationCreated.Place`** now carries a full `PostalAddress` (locality + region + country), matching the project-detail builder.
- **Validation harness** at `scripts/validate-schema.mjs` (`npm run validate:schema`). Walks 22 representative URLs (14 EN content + 2 EN zero-schema + 5 ES + homepage). Layered per page: JSON-parse soundness + required-fields-per-type table (20+ schema types) + `@id` resolution (against each document's `@id`s ∪ the two sitewide IDs) + absolute-URL correctness on `image`/`url`/`logo`/`mainEntityOfPage`/`item`. Best-effort external pass against `validator.schema.org/validate` (skipped on localhost; CAPTCHA-blocked, but internal rules cover the same failure modes). Exits 0 only on zero errors AND zero warnings. Accepts `BASE_URL` + optional `BYPASS_TOKEN` (primes a `_vercel_jwt` cookie via a manual-redirect priming request, forwards it on every fetch — SSO-protected Preview URLs work transparently).
- **Per-page validation snapshot** lives inline in `Phase-B-04-Completion.md` (Production run). Re-run via `npm run validate:schema` — results print to stdout + the gitignored `scripts/.schema-validation-report.json`.

## What works (Phase B.07 additions)

- **`/[locale]/unsubscribe/[token]` route** (EN at `/unsubscribe/[token]`, ES at `/es/unsubscribe/[token]`). Server component server-renders one of 3 initial states from a Sanity lookup (`getSubscriberByToken(token)` uses writeClient to bypass the 60-second CDN window): **confirm** (subscriber found, still subscribed — Mail icon + "Confirm unsubscribe" + green-600 primary CTA), **alreadyUnsubscribed** (CheckCircle2 + "Already unsubscribed" + secondary resubscribe CTA), **invalid** (XCircle + "This link doesn't work" + home link, terminal). Cream-surface hero card on `bg-[var(--color-bg)] py-16 lg:py-24`, `max-w-2xl` centered, rounded-2xl + `box-shadow: var(--shadow-card)`. `dynamic = 'force-dynamic'`, `revalidate = 0`. `generateMetadata` returns `robots: {index: false, follow: false}`.
- **`UnsubscribeActions` client island** owns the 8-state machine: `confirm` / `confirming` / `success` / `resubscribing` / `welcomeBack` / `alreadyUnsubscribed` / `invalid` / `error`. POSTs to `/api/newsletter/unsubscribe` with `keepalive: true`. Uses `.btn .btn-primary .btn-md` (5.2:1 white-on-green) for primary + `.btn .btn-secondary .btn-md` for resubscribe. Min-height 48px, `aria-busy` on loading, `data-loading` for the spinner. Lucide `Mail` / `CheckCircle2` / `XCircle` swap with state. Error Retry re-fires the most recent action.
- **`POST /api/newsletter/unsubscribe` route.** Zod-validates `{token (20..100), action: 'unsubscribe' | 'resubscribe'}`. 6 branches: invalid-payload (400) / invalid-token (404) / already-unsubscribed (200) / already-subscribed (200) / ok (200) / persist-failed (500). Opaque error bodies. Idempotent on both already-* short-circuits. `runtime = 'nodejs'`, `dynamic = 'force-dynamic'`. NOT gated by `NEWSLETTER_SUBMIT_ENABLED` — once subscribed, the right to leave can't be flag-gated.
- **`/api/newsletter` issues an unsubscribe token on every create + resubscribe.** Fresh `crypto.randomUUID()` once at the top of the POST body; included in `client.create({...})` (fresh) AND `client.patch.set({...})` (resubscribe). Resubscribe rotation invalidates the prior welcome email's stale link. `already_subscribed` short-circuit gets no new token. Builds `unsubscribeUrl = canonicalUrl('/unsubscribe/${token}', locale)` and passes it to `<NewsletterWelcomeEmail unsubscribeUrl={…} locale={…} email={…} />`.
- **`EmailLayout` footer unsubscribe link is locale-aware.** EN: "Don't want these emails? **Unsubscribe**." / ES: "¿No desea recibir estos correos? **Cancele su suscripción**." Same `footerLinkStyle` + `copyrightStyle` — no new email tokens.
- **`newsletterSubscriber` schema carries `unsubscribeToken`** (string, readOnly, UUID, optional so pre-backfill docs publish). Existing `unsubscribedAt` extended with `readOnly: true` + helper text (server-driven patch). Studio redeployed.
- **`getSubscriberByToken(token)` in `sanity/lib/queries.ts`.** Returns `NewsletterSubscriberLookup | null`. Uses `writeClient` (`useCdn: false`) so a freshly-issued link works inside the 60-second CDN window AND a freshly-flipped `unsubscribed` value reflects on same-URL refresh. Pre-flight length check (20..100) rejects malformed tokens before the GROQ fetch. **Note**: GROQ param is `$tk`, NOT `$token` (`@sanity/client`'s `QueryParams` marks `token` as `never`).
- **One-shot backfill script** at `scripts/backfill-newsletter-tokens.mjs` (`npm run sanity:backfill-unsubscribe-tokens`). Patches every pre-B.07 `newsletterSubscriber` missing `unsubscribeToken` with a fresh UUID. Idempotent. Ran once: 2/2 dev test docs patched.
- **Indexability defense-in-depth.** Page emits noindex metadata (SEO-harness-verified on both URL variants), does NOT appear in `sitemap.ts` (`[token]` has no enumerable slug list), and `robots.txt` gains `Disallow: /unsubscribe/` AND `Disallow: /es/unsubscribe/` (path matching is host-anchored — the EN entry does NOT cover the ES prefix).
- **New `unsubscribe.*` i18n namespace** in both locales (~30 strings each): meta + 5 surface states (confirm / success / welcomeBack / alreadyUnsubscribed / invalid) + error + homeLink. ES is LatAm Spanish, `usted`, no `[TBR]`. Native review folds into Phase M.03.
- **Two new analytics events** in `ANALYTICS_EVENTS`: `NEWSLETTER_UNSUBSCRIBED = 'newsletter_unsubscribed'` (fires on `success`) + `NEWSLETTER_RESUBSCRIBED_VIA_LINK = 'newsletter_resubscribed_via_link'` (fires on `welcomeBack`). Neither a conversion. Both fire on `document`, carry only `locale` — no email, no token, no PII.
- **Validation harnesses extended.** `scripts/validate-seo.mjs` adds `/unsubscribe/SAMPLE_TOKEN_INVALID` + `/es/...` to `EXCLUDED_PATHS` (NOT in sitemap) and `NOINDEX_PATHS` (noindex robots meta + HTTP 200), plus a robots.txt Disallow assertion. `scripts/validate-a11y.mjs` adds the invalid token to the EN sweep (16 EN + 3 ES = 19 URLs). Both gain `VERCEL_SHARE_TOKEN` env support (additive to `BYPASS_TOKEN`) — Vercel MCP share tokens flow through the same `_vercel_jwt` cookie capture; share token wins when both set.
- **End-to-end smoke verified.** 22-assertion script (signup → token written → confirm → unsubscribe → alreadyUnsubscribed → idempotent → resubscribe → idempotent → confirm again → bogus token invalid → too-short invalid-payload → bad action invalid-payload → ES strings + ES noindex) passes 22/22 on localhost; same on Preview.

## What works (Phase B.06 additions)

- **A11y validation harness** at `scripts/validate-a11y.mjs` (`npm run validate:a11y`). Walks 18 URLs (15 EN + 3 ES parity) through Playwright headless Chromium. Per URL: `@axe-core/playwright` filtered to six WCAG tags `wcag2a` / `wcag2aa` / `wcag21a` / `wcag21aa` / `wcag22a` / `wcag22aa` (best-practice tag excluded per D6); Lighthouse a11y via `chrome-launcher` (separate Chrome, reused, mobile form-factor); programmatic per-element focus walk for SC 2.4.11 (focused bbox vs sticky/fixed overlay intersection > 50% → flag); programmatic interactive-rect collection for SC 2.5.8 (< 24×24 px AND not inline-in-text AND no other interactive within 24px center-to-center → flag). Same env contract as B.04 plus `SKIP_REMOTE` (reserved). Exits 0 only on zero AA violations AND every Lighthouse a11y ≥ 95. Adds `--only=<path>` + `--skip-lighthouse`. Pre-warms routes via plain fetch before Playwright nav. Aggregate `prefers-reduced-motion: reduce` check confirms `matchMedia(...).matches === true` on home (verifies MotionRoot's `<MotionConfig reducedMotion="user">`). JSON sidecar at `scripts/.a11y-validation-report.json` (gitignored). **Final localhost: 18/18 URLs PASS, 0 axe AA, 0 SC 2.4.11 / 2.5.8, all Lighthouse a11y = 100/100.**
- **`--color-sunset-green-600 = #3F7335`** new token in `src/app/globals.css` between green-500 and green-700. AA 5.2:1 white-on-green. Used by `.btn-primary` base background, `.link:hover`, and three Tailwind callsites (`PhoneLink` hover, both mega-panel column-header link hovers). green-500 (#4D8A3F) remains the brand decorative color for non-text contexts. `.btn-primary:hover` still goes darker to green-700 (#2F5D27, 7.7:1).
- **`role="img"` + `aria-label`** applied to the two Unilock-badge placeholder `<div>`s (footer brand block + homepage credentials strip), which were firing `aria-prohibited-attr`.
- **`aria-controls={open ? id : undefined}`** applied to both navbar mega-panel triggers (`MegaPanelTrigger.tsx`) and (defensively) the chat bubble (`ChatBubble.tsx`) — both `AnimatePresence`-mount their panels, so emitting `aria-controls` only when mounted resolves the SC 4.1.2 `aria-valid-attr-value` incomplete.
- **Service-areas SVG map** wrapped in `<nav aria-label={t('title')}>` with `role="img"` dropped from the inner `<svg>` (an `img`-role element can't have focusable descendants per SC 4.1.2; the inner `<title>` + `<desc>` still provide the AT name + description).
- **Termly embed wrapper** ships `role="region" aria-label={t(type === 'privacy' ? 'regionLabelPrivacy' : 'regionLabelTerms')}` on EN (`Privacy policy` / `Terms of service`) + ES (`Política de privacidad` / `Términos del servicio`). New `legal.embed.regionLabelPrivacy` + `regionLabelTerms` i18n keys in both locales. (Both removed in B.03e with Termly.)
- **`PhoneLink` desktop variant** clears SC 2.5.8 — `xl:min-h-0` (rendering ~164×16) bumped to `xl:min-h-[24px] xl:min-w-[24px]`.
- **Per-page a11y findings snapshot** lives inline in `Phase-B-06-Completion.md`. Re-run via `npm run validate:a11y`.

## What works (Phase 2.17 additions)

- **Portfolio-draft pipeline triggers from the ServiceM8 webhook** via Next 16's `after()` post-response callback. Once `SERVICEM8_ENABLED=true` AND `PORTFOLIO_DRAFT_ENABLED=true`, the webhook returns 200 fast and `runPortfolioDraftPipeline(persisted.docId)` runs in the background — ServiceM8 never waits on the ~30–60-second Anthropic call.
- **`runPortfolioDraftPipeline()`** at `src/lib/automation/portfolio/runPipeline.ts` runs the full sequence: feature-flag gate → source-event fetch → already-handled noop → time-based idempotency check → metadata extraction → photo download/upload best-effort → featured-image fallback (placeholder on zero photos) → Anthropic draft → Sanity persist → MarkdownV2 Telegram approval send → telegramMessageId+logId patch → source-event flip to 'pending'. All errors trigger a best-effort `notifyOperator` + rethrow.
- **`'servicem8_portfolio'` Telegram approval flow** is closed (was a Phase 2.15 stub). `targetId` is now the `portfolioDraftPending._id`. On Approve, `publishPortfolioDraft()` creates a live `project` doc with deterministic `_id` (`project-<proposedSlug>`, `createOrReplace`), patches the pending doc to `'approved'`, flips the source event to `'approved'`, calls the GBP stubs (skipped until Phase 2.17a). On Reject, `rejectPortfolioDraft()` flips both to `'rejected'` (terminal). After the side-effect the webhook edits the message to remove buttons, answers the callback, posts a threaded follow-up via `reply_parameters`.
- **`portfolioDraftPending` Sanity document type** in Studio. Content group: title (localizedString), dek (localizedText), body (localizedBody PortableText), audience enum (residential / commercial / hardscape), serviceSlug, locationSlug, featuredImage, gallery (images), proposedSlug (slug). Meta group: sourceEventId, sourceEventDocId, modelUsed, generatedAt (readonly), status enum (pending/approved/rejected), processedAt, publishedProjectId, gbpUploadResult (string snapshot), photoStats (uploaded/failed), telegramMessageId, telegramApprovalLogId. Preview: EN title with `<status> · <generatedAt>` subtitle.
- **`project` Sanity schema extended** with a readonly "Automation" field group (`automatedSourceEventId` / `automatedGeneratedAt` / `automatedModelUsed`). Populated only by `publishPortfolioDraft()`; manual Studio projects leave them empty.
- **`extractJobMetadata()`** at `src/lib/automation/portfolio/extractJobMetadata.ts` defensively projects the ServiceM8 payload into `{jobUuid, description, address, attachmentUrls, inferredLocationSlug, inferredAudience, inferredServiceSlug}`, every field nullable. Inference — location: substring match against `src/data/locations.ts` city names; audience: keyword scan; service: keyword-to-slug first-match (real slugs from `src/data/services.ts`). Hints passed to the drafter; the model may override.
- **`uploadJobPhotos()`** at `src/lib/automation/portfolio/uploadPhotos.ts` walks the attachment URL list, downloads each with a 15-second `AbortSignal.timeout()` + 10 MB cap, uploads successes via `client.assets.upload('image', buffer, {filename, contentType})`. Returns `{uploaded, failed}`; failures (non-2xx, network, oversize, malformed) land in `failed` with a reason. Zero photos → empty result, pipeline continues.
- **Anthropic portfolio drafter** at `src/lib/automation/portfolio/draft.ts` produces structured bilingual JSON (Sonnet 4.6 default, env-flippable via `ANTHROPIC_PORTFOLIO_MODEL` falling back to `ANTHROPIC_MODEL`). System prompt covers brand voice, bilingual rules ([TBR]-prefix on every ES string), allowed enum lists (audience / serviceSlug whitelisted against `SERVICES` / locationSlug against `LOCATION_SLUGS`), structural guidance. Two robustness layers: (1) balance-braces JSON extractor handles trailing prose; (2) server-side taxonomy backfill merges inferred audience/serviceSlug/locationSlug onto model output if omitted. Two-stage retry on Zod failure. `proposedSlug` derived from `title.en` (kebab-case, ≤ 60 chars, ASCII-only).
- **`publishPortfolioDraft()` / `rejectPortfolioDraft()`** at `src/lib/automation/portfolio/publish.ts`. Approve does deterministic-_id `createOrReplace` of the live `project`, best-effort GROQ lookup of `services[]` (by slug + audience) + `city` (by slug) — skipped on miss. `dek`→`shortDek`, body PortableText→`narrative` (flattened via `joinBodyToText`), `title`→`leadAlt`. Calls the GBP stubs and patches `pendingDocId.gbpUploadResult`. Reject flips both docs' statuses. Both idempotent against double-tap (`status !== 'pending'` → throw).
- **GBP write stubs** at `src/lib/automation/portfolio/gbpPublish.ts` — `uploadPhotosToGbp()` + `createGoogleBusinessPost()`. Both gate on `GBP_PORTFOLIO_PUBLISH_ENABLED='true'` AND credential presence; return `{skipped:true,reason:'gbp-deferred'}` until Phase 2.17a (TODO comment points there).
- **Shared `toPortableTextBlocks` module** at `src/lib/automation/shared/toPortableTextBlocks.ts` — factored out of Phase 2.16's blog drafter; both blog + portfolio persist helpers use it. Blog re-exports it so old imports keep working.
- **Two test-only routes** at `/api/test/portfolio-pipeline-run` (drives `runPortfolioDraftPipeline` + supports `?probe=extract` mode against a hand-crafted payload) and `/api/test/portfolio-decision` (POST `{pendingDocId, decision}` invokes publish/reject directly, bypassing Telegram). Both gated by `PORTFOLIO_AUTOMATION_TEST_ROUTES_ENABLED='true'` (unset on Vercel → 404 + forbidden). Reuse the `TEST_ROUTES_SECRET`.
- **Verification harness** at `scripts/test-portfolio-automation.mjs`. Spawns a local mock Telegram API on port 7893 + `next start`. 12 tests pass: test-route auth, test-route flag-off, webhook trigger short-circuit, happy path with 2 photos (real Anthropic), happy path with 0 photos (placeholder fallback), idempotency replay, source-event-already-handled noop, Approve handler (project + automated* meta + flipped pending + flipped source + GBP skipped), Reject handler, GBP stubs return skipped:gbp-deferred, photo download best-effort (1 good + 1 404), metadata extractor unit probe (4 payloads). ~$0.20 Anthropic per run.
- **Two new env vars** on Vercel Production + Preview: `PORTFOLIO_DRAFT_ENABLED=false` (id `WIseQFbqCGYi68mj`, plain), `GBP_PORTFOLIO_PUBLISH_ENABLED=false` (id `4Bw7UctghGS3QusM`, plain). `PORTFOLIO_AUTOMATION_TEST_ROUTES_ENABLED` documented as test-only and UNSET on Vercel.
- **`Sunset-Services-Decisions.md`** carries 2 new entries: (1) Phase 2.17 plan-of-record, (2) in-phase off-spec additions (photoStats field, taxonomy backfill, robust JSON extraction, adaptive project schema mapping, probe-mode test route).

## What works (Phase 2.16 additions)

- **`POST /api/cron/blog-draft-monthly`** is live. With `BLOG_DRAFT_CRON_ENABLED=true` (the Production default) AND a valid `Authorization: Bearer ${CRON_SECRET}`, the route runs end-to-end: pre-picker time-based idempotency check (any pending `blogDraftPending` from the last 1 day → 200 + `noop` + `reason:'pending-draft-exists'`), `pickNextTopic()` walks `src/data/blogTopics.ts` skipping ids already used on a `blogPost` or non-rejected `blogDraftPending`, `generateBlogDraft(topic)` calls Anthropic (`claude-sonnet-4-6` default, Zod-validated JSON, two-stage retry), `persistBlogDraftPending()` writes the doc with auto-converted Portable Text, `requestApproval({kind:'blog_draft', targetId: docId, summary})` sends a MarkdownV2 Telegram message with title + dek + topic keyword + approx word count, the doc is patched with `telegramMessageId` + `telegramApprovalLogId`, returns 200 + `{status:'ok',docId,messageId}`. Auth mismatch → 401 + `invalid-auth`. Flag-off → 200 + `simulated`. Anthropic/Sanity errors → 500 + opaque `cron-failed` AND best-effort `notifyOperator`.
- **Monthly cron registered** in `vercel.json` at `0 14 1 * *` (14:00 UTC on the 1st ≈ 9 AM CDT / 8 AM CST). Auto-activates on the next Production deploy including the file.
- **`POST /api/cron/seo-summary-weekly`** is live, flag-gated. Auth identical to the monthly route. `GSC_ENABLED=false` (default) → 200 + `simulated`. When flipped at Phase 3.15 after Google Search Console domain verification on `sunsetservices.us`, the route fetches the last-7-day summary, formats a MarkdownV2 digest, and sends via `notifyOperator()`. The fetcher body (`src/lib/automation/gsc/fetch.ts`) is a TODO; everything else is complete. **Weekly cron registered** in `vercel.json` at `0 14 * * 1` (14:00 UTC every Monday).
- **`'blog_draft'` Telegram approval flow** closes the Phase 2.15 stub. The webhook routes `'bd:'` callback_data through `publishBlogDraft(targetId)` on Approve (creates a live `blogPost` with placeholder featured image + scoped `faq` docs + automation meta; patches the pending doc to `status='approved'` with `publishedBlogPostId`) and `rejectBlogDraft(targetId)` on Reject (`status='rejected'` with `processedAt`; doc stays for audit; topic returns to the rotation next cycle). After the side-effect, edits the message to remove buttons, answers the callback, posts a "✅ Published as blog post `<id>`" / "✋ Draft rejected. Topic returns to the rotation." follow-up threaded via `reply_parameters`. **`buildButtonsForKind('blog_draft', targetId)`** now ships the real button pair (closes the Phase 2.15 TODO throw).
- **`blogDraftPending` Sanity document type** in Studio under "Blog Draft (Pending)". Content group: title (localizedString), dek (localizedText), body (localizedBody PortableText), metaTitle + metaDescription (localizedString), faqsInline (array of `{q: localizedString, a: localizedText}`), categorySlug, slug. Meta group: automatedTopicId, topicKeyword, modelUsed, generatedAt (readonly), status enum, processedAt, publishedBlogPostId, telegramMessageId, telegramApprovalLogId. Preview: EN title with `<status> · <generatedAt>`. Ordered by `generatedAt desc`.
- **`blogPost` Sanity schema extended** with three optional readonly meta fields (`automatedTopicId`, `automatedGeneratedAt`, `automatedModelUsed`). Populated only by `publishBlogDraft()`; manual posts + existing seed posts unchanged.
- **20 curated SEO topics** at `src/data/blogTopics.ts` across 5 categories (residential / commercial / hardscape / location / seasonal). Each entry: stable kebab-case `id`, `keyword`, `category`, optional `cityHint`, `briefForModel`. Operator can edit the file directly.
- **`pickNextTopic()`** at `src/lib/automation/blog/topicPicker.ts` returns the first `BLOG_TOPICS` entry whose `id` is NOT in the used set (union of `blogPost.automatedTopicId` + non-rejected `blogDraftPending.meta.automatedTopicId`). Wraps to `BLOG_TOPICS[0]` when all consumed. Module-scope 60-second cache; opt-in reset via the test route's `?reset=true`.
- **Anthropic draft generator** at `src/lib/automation/blog/draft.ts` produces structured bilingual JSON (EN natural + ES `[TBR]`-prefixed first-pass) via `messages.create` with a brand-voice system prompt. Returns a typed `BlogDraft` validated by Zod (LocalizedString / LocalizedText / block discriminated union / FAQ inline / kebab-case slug). Two-stage retry (JSON.parse, then Zod with the first 5 issue paths). ~$0.04 per draft at Sonnet 4.6.
- **Pre-generation idempotency is time-based** (off-spec from the plan's per-topic check). Any pending `blogDraftPending` from the last 1 day → noop with the existing docId. Reasoning in `Sunset-Services-Decisions.md` 2026-05-15.
- **Shared placeholder featured-image asset** at `public/images/blog/_placeholder.jpg` (46 KB; seeded from `aurora-spring-lawn-calendar.jpg` until Cowork swaps in a curated image). `ensurePlaceholderAsset()` looks up the existing Sanity asset by `originalFilename: 'blogDefaultPlaceholder.jpg'`; first call uploads, subsequent reuse. Off-spec: Sanity's `assets.upload` doesn't accept a custom `_id`, so the deterministic asset _id is replaced with lookup-by-filename (equally stable given the deterministic filename + content-hash dedup).
- **Verification battery (`scripts/test-blog-automation.mjs`)** spawns a local mock Telegram API on port 7892 + `next start` (thrice: flag-on, flag-off, flag-on) with full Phase 2.15 + 2.16 env wiring. Pre-run cleanup sweeps stale test docs. 10 tests: cron auth, flag-off short-circuit, happy path (real Anthropic, ~$0.04), idempotency replay, Approve handler (blogPost + scoped faqs + placeholder asset), Reject handler, placeholder asset reuse, topic rotation (real Anthropic, ~$0.04), weekly SEO auth, weekly SEO flag-off. All 10 pass; cleanup deletes every tracked doc + stray `telegramApprovalLog` rows. ~$0.08 Anthropic per run.
- **Test-only API routes** at `/api/test/blog-draft-run` (drives `executeMonthlyBlogDraftRun`; optional `?reset=true` clears module-scope caches) and `/api/test/blog-draft-decision` (POST `{pendingDocId, decision}` invokes publish/reject directly). Both gated by `BLOG_AUTOMATION_TEST_ROUTES_ENABLED='true'` — unset on Vercel; return 404 + `{status:'forbidden'}` in production + preview.
- **Four new env vars** on Vercel Production + Preview: `CRON_SECRET` (sensitive, id `Q1a70s01S5twGrNk`), `BLOG_DRAFT_CRON_ENABLED=true` (id `o1jiFYQrfYRMjsrs`), `GSC_ENABLED=false` (id `xAQF13CQvTHVlCJ9`), `GSC_SITE_URL=` (empty; id `FIyz1u2Zyaw4V1gJ`). `BLOG_AUTOMATION_TEST_ROUTES_ENABLED` + `TEST_ROUTES_SECRET` documented as test-only and unset on Vercel.
- **`Sunset-Services-Decisions.md`** carries 2 new entries: (1) Phase 2.16 plan-of-record, (2) idempotency design shift (logged when the harness's Test 4 caught the picker-cache-vs-Anthropic-latency interaction).

## What works (Phase 2.13 additions)

- **`POST /api/webhooks/servicem8`** is live and reachable. With `SERVICEM8_ENABLED=false` (the default), the route returns 200 + `{status:'simulated',reason:'feature-flag'}` for any body, any headers, without parsing, signature-checking, or persisting. Verified against `localhost:3037`.
- **HMAC-SHA256 signature verification** when the flag is on. The route reads the raw request bytes (NOT JSON.parse output) and compares `createHmac('sha256', SERVICEM8_WEBHOOK_SECRET).update(rawBody,'utf8').digest('hex')` against the `x-servicem8-signature` header using `timingSafeEqual` with a length-mismatch guard. Mismatched/missing signature → 401 + `reason:'invalid-signature'`, NO Sanity write. Header name + signature format are assumptions pending confirmation when Erick configures the real ServiceM8 webhook.
- **Zod-validated payload schema.** Validates the parsed JSON against `serviceM8WebhookSchema` — required `eventId`, literal `eventType: 'job.completed'`, required `jobId`, ISO-8601-with-offset `occurredAt`, free-form `data` record. Validation failure → 400 + `reason:'invalid-payload'` with the Zod tree NOT echoed (opaque). Invalid JSON → 400 + `reason:'invalid-json'`. Off-spec: root schema uses default strip mode (NOT the plan's `.passthrough()`) because Zod 3.25's inference collapses known keys to `unknown` under `.passthrough()`; default doesn't reject extras either. Logged in Decisions.
- **Deterministic-ID dedup.** Each valid event lands at `servicem8Event-<slug(eventId)>`. First send → 200 + `{status:'ok',sanityDocId}`. Replayed same body → 200 + `{status:'ok',deduped:true,sanityDocId}`, no second write. Sanity failure → opaque 500 + `reason:'persist-failed'` (logged server-side; ServiceM8 retries on 5xx).
- **`servicem8Event` Sanity document type** in Studio under "ServiceM8 Event". Event group: eventId, eventType, jobId, payload (raw JSON blob), signatureValid (always true at write time), receivedAt. Processing group: status enum (pending/processing/completed/failed/skipped, default pending), processedAt, processingError, relatedProjectId (reference to `project`), telegramApprovalState enum (not_sent/pending/approved/rejected, default not_sent). Meta group: createdAt, lastUpdatedAt. Preview: `<eventType> — <jobId>` with `<status> · <receivedAt>`. Ordered by `receivedAt desc`.
- **Verification battery (`scripts/test-servicem8-webhook.mjs`)** spawns `next start` twice (flag-off, flag-on), runs the 6 tests (simulated response; missing signature; valid sig + create; replay dedup; invalid JSON; invalid payload). All 6 passed.
- **Two new env vars** in `.env.local`, `.env.local.example`, and Vercel (Production + Preview, `plain`): `SERVICEM8_ENABLED=false`, `SERVICEM8_WEBHOOK_SECRET=` (empty). The orphan `SERVICEM8_ENABLED=false` previously in the feature-flags block of `.env.local.example` was consolidated into the new Phase 2.13 block.
- **`Sunset-Services-Decisions.md`** carries 2 new entries: (1) Phase 2.12 deferral + Phase 2.13 plan-of-record; (2) Zod root `.passthrough()` drop + fix.

## What works (Phase 2.15 additions)

- **`POST /api/webhooks/telegram`** is live and reachable. With `TELEGRAM_ENABLED=false` (the default), returns 200 + `{status:'simulated',reason:'feature-flag'}` for any body and headers. Flag-on flow: `X-Telegram-Bot-Api-Secret-Token` header verified via `crypto.timingSafeEqual` against `TELEGRAM_WEBHOOK_SECRET_TOKEN` with a length-mismatch guard (401 + `invalid-secret`) → JSON.parse (400 + `invalid-json`) → Zod-validate the `callback_query` branch only (others → 200 + `ignored:'unsupported-update-type'`) → `parseCallbackData` null → 200 + `ignored:'unparseable-callback-data'` → idempotency lookup by `telegramApprovalLog.sentMessageId` (200 + `ignored:'no-matching-log'` if missing; 200 + `{status:'ok',deduped:true}` if already decided) → route by `ApprovalKind`. For `'servicem8_portfolio'`: patch the target `servicem8Event._id` `telegramApprovalState` to `'approved'`/`'rejected'` + set `processedAt`/`lastUpdatedAt`, patch the log via `recordDecision`, edit via `editMessageReplyMarkup`, `answerCallbackQuery`. Sanity/Telegram failures → 500 + opaque `persist-failed` (idempotency handles retries).
- **Telegram low-level client** at `src/lib/telegram/client.ts`. Three methods: `sendMessage`, `answerCallbackQuery`, `editMessageReplyMarkup`. All gated on `TELEGRAM_ENABLED==='true'` — flag-off logs `[telegram] simulated <method> ...` and returns `{ok:false, simulated:true}` with NO HTTP request. Flag-on POSTs JSON to `<TELEGRAM_API_BASE_URL or 'https://api.telegram.org'>/bot<token>/<method>` with a 10-second `AbortSignal.timeout`. NEVER throws — failures land as `{ok:false, error}`. Base URL configurable via `TELEGRAM_API_BASE_URL` (unset in production; the harness overrides to `http://127.0.0.1:7891`).
- **Telegram high-level helpers** at `src/lib/telegram/notify.ts`. `notifyOperator({text, parseMode?})` sends a one-way info message to `TELEGRAM_OPERATOR_CHAT_ID` (read at call time so a launch-handover swap from Goran to Erick takes effect without redeploy). `requestApproval({kind, targetId, summary})` composes a MarkdownV2 message with a kind-specific label prefix (`*ServiceM8 portfolio publish*`), attaches the kind's inline-keyboard matrix from `buildButtonsForKind`, sends, and on success writes a `decision:'pending'` row in `telegramApprovalLog`. Both never throw and return `{sent, messageId?, logDocId?, simulated?}`. If the send succeeds but the audit-row write fails, returns `{sent:true, messageId}` without `logDocId` (the tap lands on `no-matching-log` and is ignored; visible in logs for manual reconcile).
- **Approval domain module** at `src/lib/telegram/approvals.ts`. Discriminator + codec for `ApprovalKind = 'servicem8_portfolio' | 'blog_draft'`. `encodeCallbackData({kind, targetId, action}) → '<kindShort>:<targetId>:<action>'` where `kindShort` is `'sm8'` or `'bd'` (kept short for Telegram's 64-byte `callback_data` cap). `parseCallbackData(raw)` returns `null` on any malformed input. `buildButtonsForKind('servicem8_portfolio', targetId)` returns the Approve/Reject pair; `buildButtonsForKind('blog_draft', ...)` throws a marked TODO error (Phase 2.16 extension point).
- **Sanity audit-row helper** at `src/lib/telegram/persistLog.ts`. `createApprovalLog({kind, targetId, sentMessageId, sentChatId})` writes a new `telegramApprovalLog` with auto-generated `_id` + `decision:'pending'`. `recordDecision({logDocId, decision, operatorChatId, rawCallbackData})` patches the tap. Both trap Sanity errors and return `{error}` so the route can return an opaque 500.
- **`telegramApprovalLog` Sanity document type** in Studio under "Telegram Approval Log". Fields (one `meta` group): kind enum (servicem8_portfolio / blog_draft), targetId, sentMessageId, sentChatId, sentAt, decision enum (pending / approve / reject — default pending), decidedAt, operatorChatId, rawCallbackData. Preview: `<kind> — <targetId>` with `<decision> · <sentAt>`. Ordered by `sentAt desc`. Append-only (one row per outbound approval).
- **New `telegramMessageId` field on `servicem8Event`** (Processing group, optional number) for the Phase 2.16/2.17 caller to record the message ID alongside the event doc.
- **One-time `setWebhook` scripts.** `npm run telegram:setup -- <public-url>/api/webhooks/telegram` registers the URL, passes `TELEGRAM_WEBHOOK_SECRET_TOKEN` as `secret_token`, restricts `allowed_updates` to `['callback_query']`. `npm run telegram:info` prints `getWebhookInfo`. Run ONCE per environment after a fresh secret or URL change — manual one-time user step after the flag flips.
- **Verification battery (`scripts/test-telegram-bot.mjs`)** spawns a local mock Telegram API on `http://127.0.0.1:7891`, then `next start` twice (flag-on Phase A with 8 tests, flag-off Phase B with 2). All 10 pass; cleans up the synthetic `servicem8Event` + `telegramApprovalLog` docs (post-run count 0).
- **Test-only API routes** at `/api/test/telegram-notify` + `/api/test/telegram-approval`. Thin POST wrappers around `notifyOperator()` + `requestApproval()`. Gated by `TELEGRAM_TEST_ROUTES_ENABLED==='true'` (unset on Vercel — 404 + forbidden in production + preview). Both carry inline `MUST NOT be used by production callers` comments. Phase 2.16/2.17 callers import the helpers directly from `@/lib/telegram/notify`.
- **Two new env vars** in `.env.local`, `.env.local.example`, and Vercel (Production + Preview, `plain`): `TELEGRAM_ENABLED=false` (id `ZEYSU6TLzHrNBv59`), `TELEGRAM_WEBHOOK_SECRET_TOKEN=<64-hex>` (id `XKYeFYLkwzN4PnUG`). Third var `TELEGRAM_API_BASE_URL` documented as optional (default `https://api.telegram.org`; only the harness sets it).
- **`Sunset-Services-Decisions.md`** carries 1 new entry: Phase 2.14 deferral + Phase 2.15 plan-of-record.

## What works (Phase 2.10 additions)

- **Binary cookie consent banner** at the bottom of every route except `/request-quote/`. Cream surface (`--color-bg-cream`) + thin top border + 2-col desktop / stacked mobile + ≥48px buttons + iOS safe-area-inset padding. EN copy + `[TBR]`-flagged Spanish under `chrome.consent.{heading,body,acceptCta,declineCta,ariaLabel}`. Hidden on the wizard route (D17). Hidden completely when `NEXT_PUBLIC_ANALYTICS_ENABLED !== 'true'`.
- **Accept → GTM + Clarity load.** "Accept all" → `localStorage['sunset_consent_v1']='accepted'` → `<GTMScript>` + `<ClarityScript>` mount → `googletagmanager.com/gtm.js?id=GTM-NL5XX4DV` + `clarity.ms/tag/wqodtpq86q` fire within ~2s. First dataLayer entry is `{event: 'consent_accepted', gtm.uniqueEventId: 3}`.
- **Decline → no scripts.** "Decline" → `localStorage['sunset_consent_v1']='declined'` → no GTM/Clarity, `window.dataLayer === undefined`, no `consent_*` push (silent by design). Banner stays dismissed across reloads.
- **Master kill switch.** `NEXT_PUBLIC_ANALYTICS_ENABLED=false` → no banner, no `<GTMScript>`, no `<ClarityScript>`, no `<GTMNoScript>` iframe. Vercel Analytics (`@vercel/analytics`) unaffected.
- **dataLayer bridge.** `src/components/analytics/AnalyticsBridge.tsx` subscribes to `sunset:wizard-event` / `sunset:contact-event` / `sunset:newsletter-event` / `sunset:chat-event` / `sunset:calendly-event` CustomEvents on `document` (off-spec — the plan said `window`, but dispatchers fire on `document` with `bubbles: false`). Each forwards through `pushDataLayer(name, payload)` which applies (a) the kill-switch gate, (b) the consent gate, (c) a defensive PII filter stripping `email`, `firstName`, `lastName`, `fullName`, `phone`, `address`, `streetAddress`, `city`, `state`, `zipCode`, `message`, `name`.
- **Dispatcher rename pass aligned wire names with Cowork's GTM tag plan.** Surgical change in 6 files:
  - `wizard_submit_succeeded` → `quote_submit_succeeded` (← CONVERSION)
  - `wizard_step_completed_<n>` → `wizard_step_advanced` (with `{step}` payload, single event)
  - `chat_panel_opened` → `chat_opened`
  - `newsletter_submit_succeeded` → `newsletter_subscribed` (← CONVERSION)
  - `newsletter_submit_already_subscribed` → `newsletter_already_subscribed`
  - Added `chat_banner_book_clicked` + `chat_banner_quote_clicked` CustomEvent dispatches (Phase 2.09 set the data-attrs only).
- **Calendly postMessage wiring.** `src/components/calendly/CalendlyEmbed.tsx` gains two effects: (1) a `calendly_widget_loaded` fire inside the existing IntersectionObserver `inject()` callback, and (2) a `message` listener mapping Calendly's broadcasts (`calendly.event_type_viewed`, `calendly.date_and_time_selected`, `calendly.event_scheduled`) into `sunset:calendly-event` CustomEvents. Existing lazy-load + consent stub + fallback paths untouched. `calendly_booking_scheduled` is the CONVERSION event.
- **Four new env vars on Vercel Production + Preview** (`plain`): `NEXT_PUBLIC_GTM_ID=GTM-NL5XX4DV` (id `nCMCsddKl918a76Z`), `NEXT_PUBLIC_GA4_MEASUREMENT_ID=G-RY6NT70SH7` (id `W1sg2noqokq2ntOW`), `NEXT_PUBLIC_CLARITY_PROJECT_ID=wqodtpq86q` (id `gdkiC6dAlkMYyHOE`), `NEXT_PUBLIC_ANALYTICS_ENABLED=true` (id `6cFnFt7qECEiidqP`).
- **GTM `<noscript>` iframe** at the top of `<body>` per Google's install guide. Always renders (gated only by env, not consent).
- **Z-index ladder gains `--z-consent: 60`** alongside the existing `--z-toast: 60` (same tier). Banner sits above `--z-chat: 50`, below `--z-banner: 70` (skip-link).
- **`/api/chat/lead` confirmation** unchanged from Phase 2.09 — the lead-capture event names (`lead_capture_submit_attempted`, `lead_capture_submit_succeeded`, `lead_capture_submit_failed`) already matched the plan spec and didn't need renaming.

## What works (Phase 2.09 additions)

- **Real Anthropic-powered chat** at `/api/chat`. POSTing `{messages, locale, sessionId}` returns an SSE stream of `data: {type:'token', content}` events terminated by `data: {type:'done', stopReason, usage}`. First token of the first turn in ~1–2s warm; subsequent turns get a prompt-cache hit so the system prompt costs ~10% of its first-turn price. Tested live — turn 1 reports `cache_creation_input_tokens=2290`; turn 2 reports `cache_read_input_tokens=2290`. Model is `claude-sonnet-4-6` (env-flippable via `ANTHROPIC_MODEL`).
- **Sanity-grounded knowledge digest.** `src/lib/chat/knowledgeBase.ts` builds a ~2–3K-token plain-text digest covering identity / cities / services (3 audiences × ~6 each) / team bios / top 10 FAQs / contact + hours. Built from new chat-specific GROQ helpers in `sanity/lib/queries.ts` (`getAllServicesForChat`, `getAllLocationsForChat`, `getTopFaqsForChat`) plus the Phase 1.12 `src/data/team.ts`. Memoised per-locale at module load with a 30-min TTL.
- **`flag_high_intent` tool** — Claude calls it ONLY when the visitor signals readiness to transact (scheduling, contract, availability, pricing for a named project). Verified: "I want to schedule a paver patio install for next month at my Naperville house" triggers it; "What services do you offer in Naperville?" does NOT. SSE relays the call as `data: {type:'high_intent', reason}` mid-stream.
- **In-panel high-intent banner** (Phase 1.20 rewrite). Amber-tinted (`#FDF7E8` + `border-left: 3px solid amber-700`) banner between the message log and composer when `flag_high_intent` fires. Two CTAs: primary "Book a consult" → `/contact#calendly`, ghost "Get a quote" → `/request-quote`. × dismiss. Clears automatically at the START of each new outgoing turn.
- **Two-tier in-memory rate limiter** (`src/lib/chat/rateLimit.ts`). 1 message / 2s burst → 429 `{reason:'burst', retryAfter:2}` + `Retry-After: 2` header. 50 messages / IP / 24h daily → 429 `{reason:'daily', retryAfter:~86400}`. Both verified by burst test (2 POSTs in <1s) and daily test (temporarily `CHAT_DAILY_LIMIT_PER_IP=2`, 3 POSTs). Counters reset on Vercel cold start; carryover docs the Phase 3.10 swap window.
- **Master backend kill switch** (`AI_CHAT_ENABLED`). When `false`, `/api/chat` returns 503 `{status:'disabled'}` and `/api/chat/lead` returns 200 `{status:'simulated'}`. Frontend flag (`NEXT_PUBLIC_AI_CHAT_ENABLED`) independent — the bubble still mounts; the panel shows `chat.errors.disabled` on send.
- **`/api/chat/lead` lead-capture endpoint.** Honeypot-before-Zod → Sanity write (`chatLead` doc) → branded `ChatLeadEmail` via `sendBrandedEmail()` (sandbox-routed; subject `[SANDBOX → info@sunsetservices.us] New chat lead — <name>`) → Mautic stub. Smoke test wrote doc `I3ROLIaQjmV1gZbnPZ7fMY`. Honeypot test (POST with `"honeypot":"spam"`) returns silent `{status:'ok'}`, no Sanity write. `triggerReason` carries the most recent `flag_high_intent` reason if captured this session, plus the visitor's phone when offered (schema is email-first — phone lives in `triggerReason`).
- **New `chatLead` Sanity document type** in Studio. Fields: name, email (email validator), locale (en/es), pageContext (URL), sessionId, capturedAt, triggerReason, status (new/contacted/qualified/closed), mauticSynced, transcriptExcerpt (max 20 messages, each `{role, content, ts}`). Studio deployed via `npm run studio:deploy`.
- **ChatLeadEmail branded template** (`src/lib/email/templates/ChatLeadEmail.tsx`) — header → contact block → optional page-context line → optional amber-tinted "Why this was flagged ready to book" callout → conversation excerpt (last 10 messages with alternating bg + role prefix + timestamp) → primary "Reply by email" + ghost "Call now" CTAs → small "View in Sanity Studio →" link. Composes `EmailLayout` + `EmailButton`, passes `intendedRecipient` for the Phase 2.08 sandbox banner.
- **Four new env vars** on Vercel Production + Preview: `AI_CHAT_ENABLED=true` (id `tXQSNzrDQhahQGDw`), `ANTHROPIC_MODEL=claude-sonnet-4-6` (id `pZQ5919NuIX7uGe6`), `CHAT_DAILY_LIMIT_PER_IP=50` (id `c8OD1NkikDWG1cde`), `CHAT_BURST_INTERVAL_MS=2000` (id `B1mvzx5z7EUDRtmI`). Plus `NEXT_PUBLIC_AI_CHAT_ENABLED` flipped `false` → `true` (id `iKcdy783WIIy0dSz`).
- **Chat session-ID** in `sessionStorage` at key `sunset_chat_session_id` (Phase 1.20 only stored history). `clearHistory()` removes both keys. The session ID flows through `/api/chat` + `/api/chat/lead` payloads so Erick can correlate transcript → lead in Sanity.
- **Browser smoke verified.** Real browser session via Preview MCP: bubble visible on `/`, panel opens, real Claude-streamed reply for "How much for a paver patio in Naperville?" (no false high-intent), high-intent message triggers banner with both CTAs at correct localized routes, lead form submit creates Sanity doc + sends branded email + dismisses the form + appends confirmation bubble + "Open the full form →" link, reload restores session history, navigating to `/es/` switches chat chrome to Spanish.

## What works (Phase 2.08 additions)

- **Five branded HTML email templates** under `src/lib/email/templates/`: `QuoteLeadAlertEmail`, `QuoteConfirmationEmail`, `ContactAlertEmail`, `ContactConfirmationEmail`, `NewsletterWelcomeEmail`. Each composes the shared `EmailLayout` primitive (header wordmark + body slot + NAP footer) with inline-styled brand tokens from `src/lib/email/tokens.ts`. EN copy is the spec; inline ES on every visitor-facing template is flagged `[TBR]` for Phase 2.13 native review.
- **`sendBrandedEmail()` utility (`src/lib/email/send.ts`).** Single entry point for every Resend send. When `RESEND_DOMAIN_VERIFIED=false`: reroutes to `RESEND_TO_EMAIL`, prefixes the subject with `[SANDBOX → <intended>]`, injects `intendedRecipient` so `EmailLayout` renders the in-body yellow sandbox banner. When `true`: send goes through normally. Single env-var flip, zero code changes. Never throws — returns `{ok, messageId?, error?}` (Phase 2.06 durability pattern).
- **`/api/quote` refactored** to use the branded templates AND now sends a second email: the visitor confirmation (`QuoteConfirmationEmail`). The old `src/lib/quote/resend.ts` plaintext sender is now a thin wrapper around `sendBrandedEmail()` (`sendQuoteLeadAlertEmail` + new `sendQuoteVisitorConfirmationEmail`). The Zod schema gained `locale: z.enum(['en','es']).default('en')` and the wizard's `buildPayload()` now forwards `locale`.
- **`/api/contact` new route.** Mirrors `/api/quote`'s architecture: honeypot-before-Zod → Sanity write → branded `ContactAlertEmail` to Erick → branded `ContactConfirmationEmail` to visitor (only when an email was provided) → Mautic stub. Master kill switch `CONTACT_SUBMIT_ENABLED`. Enforces D14 "at least one of email or phone" as a server-side guard.
- **ContactForm wired to `/api/contact`.** The Phase 1.11 Part-1 simulation handler is gone. The form now POSTs `keepalive: true`, fires `sunset:contact-event` CustomEvents for the Phase 2.11 GTM bridge, surfaces inline error messages on network/server failure, clears to the success state on `status: 'ok'`.
- **`/api/newsletter` new route.** Honeypot-before-Zod → checks for an existing subscriber by email → creates fresh / resubscribes-on-unsubscribed / returns `already_subscribed` if active → branded welcome email → Mautic stub. Master kill switch `NEWSLETTER_SUBMIT_ENABLED`. Sanity's async unique-email validation runs at publish time only — the route handler's pre-check is the authoritative duplicate guard for API writes.
- **Footer `<NewsletterSignup/>`** (`src/components/forms/NewsletterSignup.tsx`) replaces the Phase 1.05-J placeholder (`FooterNewsletter.tsx`, deleted). 2-column desktop / stacked mobile, brand-token styling (green-500 button, white input on dark footer), honeypot field, real `/api/newsletter` POST with `keepalive: true`, success / already_subscribed / invalid / error inline states wired to `aria-live="polite"`. Mounted as a new TOP section above the Footer's brand+links columns (now 2-column). Hidden on `/request-quote/` (D17).
- **Two new Sanity document types**: `contactSubmission` (name + email/phone/category/message + locale + sessionId + status) and `newsletterSubscriber` (email + subscribedAt + sourcePage + locale + unsubscribed flag). Both deployed via `npm run studio:deploy`.
- **Three new env vars** on Vercel (Production + Preview) + `.env.local` + `.env.local.example`: `RESEND_DOMAIN_VERIFIED=false` (sandbox routing; flips Phase 3.11/3.12), `CONTACT_SUBMIT_ENABLED=true`, `NEWSLETTER_SUBMIT_ENABLED=true`.
- **Shared `generateUuid()` extracted** to `src/lib/sessionId.ts`. The wizard's localStorage-backed `getOrCreateSessionId` imports from there; contact + newsletter forms generate a fresh UUID per submit.

## What works (Phase 2.07 additions)

- **Real Calendly inline widget on `/contact/` and `/thank-you/`** (EN + ES). When `NEXT_PUBLIC_CALENDLY_ENABLED=true` AND `NEXT_PUBLIC_CALENDLY_URL` is non-empty AND consent is granted, the official Calendly inline widget mounts via the canonical `<div class="calendly-inline-widget" data-url=... />` + `widget.js` pattern.
- **Lazy-load behavior.** `widget.js` is NOT in the network waterfall on `/contact/` until the widget approaches the viewport (`IntersectionObserver`, `rootMargin: '200px 0px'`). On `/thank-you/` it's near the top, so it loads almost immediately (correct). Idempotent: `<script src*="calendly.com/.../widget.js">` is checked before injection so multiple instances don't double-load.
- **Consent gating.** Mirrors the Phase 1.20 D29 chat-widget stub — default-true. When the real banner lands in Phase 2.11, the same hook semantics flip the gate. 2.07 leaves a `useState<boolean>(true)` placeholder with a comment.
- **Flag fallback.** Setting `NEXT_PUBLIC_CALENDLY_ENABLED=false` (or empty URL) renders a static fallback card with a `tel:` button + (when URL is set) a direct anchor. No broken iframe, no JS errors. **`<noscript>` fallback** with a plain anchor renders inside the widget container.
- **Component file path.** `src/components/calendly/CalendlyEmbed.tsx` — client component, takes `locale: 'en'|'es'`, `namespace: 'contact.calendly'|'thanks.calendly'`, `minHeight?: number` (default 680), `surface?: 'bg'|'cream'` (informational only).
- **i18n keys added per locale:** `contact.calendly.{sub,fallbackCta,fallbackLink,iframeLabel}` + `thanks.calendly.{h2,sub,fallbackCta,fallbackLink,iframeLabel}`. ES flagged `[TBR]`. The existing `contact.calendly.h2` ("Pick a 30-min slot.") was preserved per the plan's "do not rewrite existing wording" rule.
- **Vercel env vars** `NEXT_PUBLIC_CALENDLY_URL` + `NEXT_PUBLIC_CALENDLY_ENABLED` provisioned on Production + Preview via REST upsert (`/v10/projects/<id>/env?upsert=true`). Plain type — the URL is public.
- **Calendly URL is the user's personal testing account** (`calendly.com/dinovlazar2011`). Swap to Erick's real Sunset Services URL before DNS cutover — Phase 3.12 checklist item per Decisions "2026-05-12".

## What works (Phase 2.06 additions)

- **Wizard Submit is now real.** Step 5 amber Submit POSTs to `/api/quote`. The route writes a `quoteLead` document to Sanity, sends a plain-text lead-alert email to `RESEND_TO_EMAIL` via Resend (sandbox sender `onboarding@resend.dev` at 2.06; branded 2.08), pushes to a no-op Mautic stub, returns `{status: 'ok', sanityDocId}`; the wizard clears autosaved Steps 1–3 + the session ID and routes to `/thank-you/?firstName=…`. Sanity write is durable-first: a Resend failure (e.g. sandbox-mode 422 to an unverified TO) is logged but the lead still lands in the Studio + the visitor sees the success path.
- **Partial-record capture on Steps 1–3 transitions.** Advancing past Step 1, 2, or 3 fires a `keepalive: true` POST to `/api/quote/partial`. The route upserts a `quoteLeadPartial` document at deterministic ID `quoteLeadPartial-<sessionId>` — fetch-then-patch-or-create preserves `firstSeenAt` while advancing `lastUpdatedAt` + `lastStepReached`. When a matching full `quoteLead` later lands, the partial's `converted` flips to `true`. NEVER fires on Step 4→5 (PII boundary; the partial endpoint's Zod schema rejects PII fields too as a defensive backstop).
- **`WIZARD_SUBMIT_ENABLED=false` master kill switch.** Both routes return 200 + `status: 'simulated'` with zero side effects — Sanity untouched, Resend not called, Mautic stub line not fired, no error logging. The wizard treats the response identically to a real success and redirects to `/thank-you/`, so the route stays demoable with the backend off.
- **Honeypot anti-spam on Step 5.** A visually hidden `#company_website` input (off-screen, `aria-hidden="true"`, `tabIndex={-1}`, `autoComplete="off"`) gets a value when a naive bot fills every input. The route checks for this BEFORE Zod and returns silent 200. (The previous `honeypot: max(0)` inside the Zod schema was rejected because the 400 body exposed the field name.)
- **Two new Sanity document types in the Studio.** "Quote Lead" (8 contact + 6 project + 6 meta fields, ordered by submittedAt desc) and "Quote Lead — Abandoned" (sessionId + timestamps + last-step + converted flag, ordered by lastUpdatedAt desc). Both deployed via `npm run studio:deploy`.
- **Mautic stub gated on `MAUTIC_ENABLED`.** Both `pushFullLeadToMautic()` and `pushPartialLeadToMautic()` log a single no-op line and return `{synced: false}` at 2.06. Flipping `MAUTIC_ENABLED=true` + populating `MAUTIC_BASE_URL` + `MAUTIC_API_KEY` switches them to the real implementation (TODO block marked `Phase 2.x`) without any other call-site edit.
- **Session ID linkage between partials and full submits** via a client-side localStorage UUID at key `sunset_wizard_session_id`. Generated via `crypto.randomUUID()` with a tiny RFC4122 v4 fallback. Cleared on successful submit.
- **Six new env vars** in `.env.local`, `.env.local.example`, and Vercel (Production + Preview): `WIZARD_SUBMIT_ENABLED=true`, `MAUTIC_ENABLED=false`, `MAUTIC_BASE_URL=`, `MAUTIC_API_KEY=`, `RESEND_FROM_EMAIL=onboarding@resend.dev`, `RESEND_TO_EMAIL=info@sunsetservices.us` (overridden to `dinovlazar2011@gmail.com` at the 2.06 dev window per the 2026-05-12 Resend TO routing decision). `WIZARD_SUBMIT_ENABLED` was previously `false` on Vercel (Phase 2.02 default) — flipped to `true` this phase.
- **`/api/quote` + `/api/quote/partial` routes** both pin `runtime = 'nodejs'` (the Sanity write client needs Node-only APIs) + `dynamic = 'force-dynamic'` (every request mutates external state).
- **`zod@^3.25.x` pinned** (resolved from the spec's `^3.23` range). Server-only — the Zod imports never reach the client bundle.

## What works (Phase 2.05 additions)

- **Sanity-driven content** — Erick (or any editor) can log into `https://sunsetservices.sanity.studio`, edit a project description / blog post / resource article / FAQ / review placeholder, publish, and see the change on `localhost:3000` AND `sunsetservices.vercel.app` within 30 minutes. No code edits, no redeploy.
- **GROQ query helpers** (`sanity/lib/queries.ts`) — 14 typed helpers covering every Sanity-read page: project index/detail, blog index/detail, resource index/detail, per-service FAQs, per-city FAQs, per-blog FAQs, per-resource FAQs, per-audience FAQs (unused at launch), per-city reviews. All bilingual fields return as `{en, es}` so the Phase 1.x `data.title[locale]` access pattern works unchanged.
- **PortableText rendering** on blog + resource detail pages via `ProseLayoutPT` (`@portabletext/react@4`). Same `.prose__*` typography as Phase 1.18, same sticky right-rail TOC at xl+, same inline `<ServiceCard>` cross-link splice between H2s, same inline `<ServiceAreaStrip>` near body bottom for city-targeted posts. Server-safe helpers (`countWordsInBlocks`, `extractHeadingsFromBlocks`, `extractHowToStepsFromBlocks`) live in `portableTextHelpers.ts`.
- **ISR (30 min)** on every Sanity-read page: `/projects` (index ƒ + 24 detail SSG), `/blog` (index ƒ + 10 detail SSG), `/resources` (index ƒ + 10 detail SSG), `/[audience]/[service]` (32 routes), `/service-areas/[city]` (12 routes).
- **Image fallback rule** is layered: Sanity asset wins when present, else falls back to `imageMap.ts` placeholders (projects) or `/images/{blog,resources}/<slug>.jpg` (Phase 1.18 path convention). 2.05 ships with every Sanity image field empty; Phase 2.04 fills them in without page-code changes.
- **FAQ single source of truth: Sanity.** Per-service scope tag is `service:<audience>:<slug>` (residential vs commercial snow-removal stay separate). Per-city `city:<slug>`. Per-blog `blog:<slug>`. Per-resource `resource:<slug>`. All FAQ-emitting pages (service detail × 32, location × 12, blog × 10, resource × 10) use `buildContentFaqSchema` (already-projected `{q, a}` strings).

## What works (Phase 2.03 additions, unchanged)

- **Standalone Sanity Studio at `https://sunsetservices.sanity.studio`** — Sanity-hosted (not embedded in the Next.js app per user preference). All 8 document types appear in the left navigation: Service, Project, Blog Post, Resource Article, Location, Faq, Review, Team. Empty of content; Phase 2.04 starts uploading photos. `studioHost: 'sunsetservices'` + `deployment.appId: 'hza6xflhrkuygkrhketq6uhj'` in `sanity.cli.ts` pre-lock both the hostname + the application ID, so every subsequent `sanity deploy` is non-interactive.
- **Sanity schemas** — 12 files under `sanity/schemas/` (4 shared objects: `localizedString`, `localizedText`, `localizedBody`, `localizedSeo`; 8 documents: `service`, `project`, `blogPost`, `resourceArticle`, `location`, `faq`, `review`, `team`). Each document carries field groups (`content`/`media`/`taxonomy`/`seo`/etc.) + a `preview` block with a useful subtitle. All image fields use `{hotspot: true}`. References wired (e.g. `project.services` → `service`, `location.featuredServices` → `service`, `faqs` arrays → `faq`). `service.priceIncludes`, project before/after fields, and similar conditional fields use `hidden: ({parent}) => ...`.
- **Sanity client + image builder** — `sanity/lib/client.ts` (read-only, CDN, `perspective: 'published'`) and `sanity/lib/image.ts` (`urlFor()` helper). Imported nowhere in the Next.js app yet — Phase 2.05 starts fetching.
- **`npm run studio:dev` / `studio:build` / `studio:deploy`** — three new scripts. `studio:dev` boots the Studio at `localhost:3333`; `studio:build` produces `dist/` (gitignored); `studio:deploy` publishes to `sunsetservices.sanity.studio`.
- **Sanity CORS origins** — `localhost:3000`, `sunsetservices.vercel.app`, `sunsetservices.us` (plus the auto-registered `localhost:3333` for the Studio dev server). All registered with `--no-credentials`.
- **`npm run build` still produces 118 pages** with the new `sanity/` imports compiled but unused. Vercel preview rebuild on push verified green.

---

## What does NOT work yet

- **Post-launch native Spanish review still deferred.** Phase M.03 was LLM-driven, not human native review. Launch acceptance item "native Spanish review complete" remains technically unmet by explicit user decision; the future native pass should prioritize the chat persona, quote wizard, transactional emails, Q&A/FAQ corpus, and blog/resource bodies.
- **Daily Google reviews cron** (Phase 2.16 carryover). The 2.16 plan bundled three crons (monthly blog + weekly SEO + daily reviews); only the first two shipped. Daily reviews depends on the Phase 2.14 Places fetcher, itself deferred awaiting Google's GBP API approval + Goran's `GBP_OAUTH_CLIENT_SECRET` handoff. When 2.14 re-opens, its follow-up either consolidates Daily into the existing two routes (Hobby's 2-cron budget is full) OR triggers a Phase 3.10 Pro upgrade unlocking unlimited crons.
- **Weekly SEO summary fetcher body is a TODO** (Phase 2.16). `src/lib/automation/gsc/fetch.ts` exposes the typed `fetchWeeklySeoSummary()` signature but the body throws `'GSC fetcher implementation pending — Phase 3.15'` until the operator (a) verifies Google Search Console ownership on the new `sunsetservices.us` domain, (b) provisions the service-account JSON, and (c) flips `GSC_ENABLED=true` on Vercel Production. Route handler + summarizer are complete on both branches; the flag-on branch is dead until Phase 3.15.
- **Auto-published blog posts ship with a generic placeholder featured image** (Phase 2.16). `public/images/blog/_placeholder.jpg` (46 KB; seeded from `aurora-spring-lawn-calendar.jpg`) is uploaded to Sanity on first cron run with `originalFilename: 'blogDefaultPlaceholder.jpg'` and reused via lookup-by-filename. Cowork swaps in a curated brand image in a parallel task; until then the placeholder is on-brand-adjacent (landscaping) but not curated brand voice. Operator can also swap per-post from Sanity Studio. Acceptable per the 2.16 plan-of-record decision.
- **No native ES review of auto-generated blog posts** (Phase 2.16). The Anthropic generator produces idiomatic Latin-American Spanish, prefixed with `[TBR]` at the field level (matches the Phase 2.11 convention). The first-pass ES folds into the Phase 2.12 native-review queue when 2.12 runs; until then, auto-published ES blog posts ship with `[TBR]` visible to ES visitors. Same UX constraint as the rest of the ES surface; not a 2.16-specific regression.
- **Telegram-side draft editing is out of scope** (Phase 2.16). The webhook supports Approve & publish / Reject only — no "make it shorter" reply, no "regenerate" button. To change angle, Reject; the topic returns to rotation; the next monthly cron generates a fresh draft from scratch.
- **No production caller of `notifyOperator()` yet** (Phase 2.15 additions; 2.16 wired `requestApproval` only). `notifyOperator()` is reachable from the Weekly SEO cron path (flag-gated on `GSC_ENABLED`) and from both cron routes' error paths. No other production caller. Phase 2.17 may wire it for ServiceM8 error alerts and operational alarms.
- **`setWebhook` has NOT been called against any Vercel deployment** (Phase 2.15 carryover). The route exists on Production + Preview but Telegram doesn't know about it. Once the user (a) flips `TELEGRAM_ENABLED=true` on the target environment and (b) runs `npm run telegram:setup -- <public-url>/api/webhooks/telegram` once, Telegram delivers `callback_query` updates to that URL.
- **`TELEGRAM_OPERATOR_CHAT_ID` and `TELEGRAM_BOT_TOKEN` are empty on Vercel** (Phase 2.01 leftover, surfaced during 2.15 verification). Both exist in the parent `.env.local` but the Vercel REST upsert during 2.01 skipped or zeroed the production+preview entries. Must be repopulated before flipping `TELEGRAM_ENABLED=true` — otherwise the live webhook would 401 (no token to bot URL) and `notifyOperator` would log "TELEGRAM_OPERATOR_CHAT_ID is empty" instead of sending. Single REST upsert per var.
- **`TELEGRAM_OPERATOR_CHAT_ID` currently points at Goran, not Erick** (Phase 2.15 carryover). Captured at 2.01 from Goran's Telegram for verification. Swap to Erick's chat ID is a launch-handover step: Erick sends `/start` to the bot, the webhook (once running) logs the `from.id`, Goran swaps the env var, re-deploys.
- **Manual end-to-end Telegram smoke test deferred** (Phase 2.15). The automated harness covers everything except a real Telegram message → operator taps Approve → Sanity updates round-trip, which needs a real bot, a real public URL with `setWebhook` registered, and a real human tap — not automatable. User-driven step after 2.15 merges: flip flag on Preview, run setup script, send a debug message, tap.
- **No real ServiceM8 events flow today** (Phase 2.13 additions). Erick has not adopted ServiceM8 — the webhook endpoint is shipped behind `SERVICEM8_ENABLED=false` so it no-ops until adoption. Verified synthetically only; no live provider. Flip the flag + populate `SERVICEM8_WEBHOOK_SECRET` when Erick adopts ServiceM8 and configures the webhook in their dashboard.
- **No real ServiceM8 events flow today even though the pipeline is built** (Phase 2.17). 2.17 shipped the entire Telegram-approval leg; flow activates the moment Erick adopts ServiceM8 AND `SERVICEM8_ENABLED` + `PORTFOLIO_DRAFT_ENABLED` both flip to true on Vercel. Until then both webhooks short-circuit to `{status:'simulated',reason:'feature-flag'}`.
- **GBP photo upload + Google Post creation are stubbed** (Phase 2.17). `src/lib/automation/portfolio/gbpPublish.ts` exports `uploadPhotosToGbp` and `createGoogleBusinessPost` as named functions gating on `GBP_PORTFOLIO_PUBLISH_ENABLED='true'` + credential presence; both return `{skipped:true,reason:'gbp-deferred'}` at 2.17 defaults. Phase 2.17a (a one-shot follow-up opening after 2.14 lands the GBP write client + Goran provides `GBP_OAUTH_CLIENT_SECRET` + `GBP_OAUTH_REFRESH_TOKEN`) swaps the stub bodies. Call sites in `publish.ts` stay unchanged.
- **ServiceM8 payload shape is assumed, not confirmed** (Phase 2.17). `extractJobMetadata` projects job UUID / description / address / attachment URLs from `payload.data.*` with documented lookup orders and defensive null fallbacks. At flag-on, re-confirm against Erick's real ServiceM8 webhook output — if any field lives under a different key, add a lookup branch.
- **No ServiceM8 API client yet** (Phase 2.17). Inline attachment URLs only — if Erick's real webhook delivers photo references requiring an API fetch (vs. embedded URLs), a small ServiceM8 API client addition is needed. Cross-check at flag-on.
- **Signature header name is an assumption** (Phase 2.13). The route reads `x-servicem8-signature` and expects a hex HMAC-SHA256 digest of the raw body. ServiceM8's actual webhook config will spell out the exact format at flag-on; if reality differs, the swap is a one-function change in `src/lib/servicem8/verifySignature.ts`. Likely Phase 2.17a follow-up.
- **`[TBR]` prefix still visible on ES routes** (Phase 2.13 inherits this from the 2026-05-14 Phase 2.12 deferral). Until either (a) strip-then-review or (b) Phase 2.12-as-originally-specified runs, every ES translated string renders the `[TBR]` prefix verbatim. Hard latest moment to pick a path: before Phase 3.12 (pre-cutover QA). Logged in `Sunset-Services-Decisions.md`.
- **Custom domain `sunsetservices.us` not yet pointing at Vercel.** Old WordPress site still serves the public domain. Cutover in Phase 3.13 after the Vercel Pro upgrade (Phase 3.10).
- **Vercel Analytics retention is short** (Hobby tier feature-limited, ~24 hours of data). Full retention unlocks at Phase 3.10 Pro upgrade.
- **Preview deploys are auth-gated** (Vercel SSO protection on by default for new teams). Public preview URLs require disabling SSO protection in project settings — not yet done, to keep the security default.
- Real photography — placeholders generated by `scripts/gen-audience-service-placeholders.mjs` (Phase 1.09) and `scripts/gen-home-placeholders.mjs` (Phase 1.07). Phase 1.16 ships 12 placeholder projects whose lead + gallery + before/after assets alias to existing audience-project / service-project tiles via `imageMap.ts`. Cowork sources real photos from Erick's Drive in Phase 2.04.
- `/projects/?service=…` filtered routes — 404 by design until Phase 2.x; Phase 1.16 ships only the audience-only filter (handover D2.A).
- **Resend domain still unverified — sandbox routing in effect.** `RESEND_FROM_EMAIL=onboarding@resend.dev` and `RESEND_DOMAIN_VERIFIED=false`. Every branded send (5 templates, 3 routes) reroutes to `RESEND_TO_EMAIL` (`dinovlazar2011@gmail.com`) with a `[SANDBOX → <intended>]` subject prefix and an in-body yellow banner. Visitors do NOT receive confirmation emails during this window. Acceptable per the 2026-05-12 decision-log entry — Sanity write succeeds so no lead is lost, `/thank-you/` has full next-steps, and the dev window is finite. Flip in Phase 3.11/3.12: SPF/DKIM/DMARC records → Resend verifies → flip `RESEND_DOMAIN_VERIFIED=true` + `RESEND_FROM_EMAIL=info@sunsetservices.us` + `RESEND_TO_EMAIL=info@sunsetservices.us`. Single env-var change, zero code.
- **Chat rate limiter is in-memory only.** Counters live in module-scoped `Map`s; they reset on every Vercel function cold start (~5–15 min idle on Hobby) and don't share across parallel instances. Acceptable for the SSO-protected preview window — abuse risk is functionally zero. MUST be replaced with Vercel KV or Upstash Redis before Phase 3.13 DNS cutover; natural swap window is Phase 3.10 (Vercel Pro upgrade). API surface (`checkRateLimit(ip) → {allowed, reason?, retryAfter?}`) was deliberately designed as a single-file swap.
- **Chat lead emails are sandbox-routed** until Phase 3.11/3.12 (inherits the Phase 2.08 Resend constraint). The branded `ChatLeadEmail` lands in `dinovlazar2011@gmail.com` with subject `[SANDBOX → info@sunsetservices.us] New chat lead — <name>` and a yellow in-body banner. Visitor never sees their lead-capture email confirmation during this window. Mitigated: visitor sees the in-panel "Thanks — Erick will be in touch within one business day" + "Open the full form →" CTA, and the Sanity doc is captured durably regardless of email state.
- **Chat output is plaintext only.** Per ratified D24=A, no Markdown rendering in chat — URL auto-link is the only rich format. Carryover to a future phase if the team wants bold/lists/headings to render.
- **Chat bubble + Calendly widget consent gates still default-true** (Phase 2.07 / Phase 1.20 stubs). Phase B.03's banner gates GTM + Clarity tag firing via Consent Mode v2 signals, but does NOT change the chat bubble or Calendly widget — both still load for every visitor. A follow-up phase can decide whether to gate Calendly + the chat panel under the same consent banner.
- **No Telegram lead alerts.** Per Project Instructions §9 + Phase 2.09 Plan §12, Telegram pings on high-intent are explicitly OUT of scope at this phase. On-screen banner only.
- **Calendly URL is the user's personal testing account** (`calendly.com/dinovlazar2011`). Swap to Erick's real Sunset Services URL before DNS cutover — Phase 3.12 checklist item.
- **GCP HTTP referrer restrictions on the Places API key** (Phase B.10 Cowork carryover, security task). `NEXT_PUBLIC_GOOGLE_PLACES_API_KEY` is embedded in the client bundle by design and visible to every visitor — the security boundary is the GCP-side referrer allowlist, NOT the env-var. Without the allowlist, an attacker can run up the GCP bill against Sunset Services' account. 2-minute click-through in GCP Console → APIs & Services → Credentials → the Places API key → Application restrictions → HTTP referrers; allow `http://localhost:3000/*`, `https://sunsetservices.vercel.app/*`, `https://*.dinovlazars-projects.vercel.app/*`, `https://sunsetservices.us/*` (post-P.06). Goran has GCP project ownership. **MANDATORY before Phase P.06 (DNS cutover / public launch)**. Field-by-field block in `Phase-B-10-Completion.md` §"Carryover".
- **GCP billing alerts** (Phase B.10 Cowork carryover, operational task). Confirm $20 monthly cap with email alerts at 50% / 90% / 100% to an inbox Goran or the user actively monitors. The 2026-05-10 "Anthropic-billing-alert risk pattern" applies here too if not configured.
- **GTM web-UI tag configuration** — Phase 2.10 Code shipped the dataLayer infrastructure. Cowork Part B (post-merge) creates the GA4 Configuration tag (All Pages → GA4 with Measurement ID `G-RY6NT70SH7`) + the four Key Event tags (`quote_submit_succeeded`, `contact_submit_succeeded`, `newsletter_subscribed`, `calendly_booking_scheduled`) inside the GTM web UI. Marking each as a Key Event in GA4 waits 24h after first observed fire.
- **Google Consent Mode v2** — Phase B.03 shipped granular 4-category Consent Mode v2 (analytics / marketing / personalization → analytics_storage / ad_storage / ad_user_data + ad_personalization). GTM loads regardless of consent; tag firing inside GTM gates on the four signals. The prior Phase 3.04 placeholder is closed.
- `[TBR]`-flagged Spanish strings — the audience landings and service detail pages ship with first-pass Spanish translations. Native-speaker review happens in Phase 2.13.
- **Sanity Studio content is text-only at Phase 2.05.** 158 documents migrated (services × 16, locations × 6, team × 3, reviews × 6, FAQs × 128, projects × 12, resource articles × 5, blog posts × 5). Every image field is `null`; Phase 2.04 (Cowork) uploads photos. The page-side fallback (`imageMap.ts` for projects, `/images/{blog,resources}/<slug>.jpg` for content) renders correctly until then.
- **Sanity webhook NOT yet configured in `manage.sanity.io`** (Phase B.08). The route is live, HMAC-signature-verified, and signature-locked — but no actual webhook fires until the user runs the field-by-field click-through in `Phase-B-08-Completion.md` §"User-runnable next step" (Sanity dashboard → Project → API → Webhooks → Create webhook, with the projection `{_type, _id, slug.current}` + the same `SANITY_REVALIDATE_SECRET` value as Vercel). Until then, every Sanity-read page falls back to the page-level `export const revalidate = 1800` safety net — Erick's edit → up to 30 min → live. After the click-through, that drops to ~2 seconds.
- **`SANITY_REVALIDATE_SECRET` reused across Vercel Production + Preview** (Phase B.08). Single shared HMAC secret for simplicity. Rotate before Phase P.06 DNS cutover; consider per-environment-distinct secrets in Phase P.01 (Vercel Pro upgrade).
- **SANITY_API_WRITE_TOKEN** is created (Phase 2.05) in both `.env.local` (gitignored) and Vercel (Production + Preview). Used by `scripts/seed-sanity.mjs` and (in Phase 2.16) the automation agent. **No write tokens for read-only Studio users** — Sanity's per-project ACL handles editor permissions natively.
- (Pre-Phase 2.10 note — now stale: AI chat shipped in Phase 2.09; Resend email shipped in Phase 2.08; analytics shipped in Phase 2.10.)
- **GBP API write access + Places API read (Phase 2.01 Step 7) — DEFERRED to new Phase 2.13.2 per user decision.** Phase 2.14 (publish to Google Business Profile) and Phase 2.16 (daily reviews on the site) both wait on Phase 2.13.2 completing first. Phase 2.13.2 itself starts a 2–6 week Google review clock for GBP API access.
- **Cloudflare DNS** — account exists with 2FA enabled, no domain added yet. Domain cutover happens in Part 3 Phase 3.11.
- **Resend domain verification** for `sunsetservices.us` (SPF/DKIM/DMARC records) — Phase 2.08 deferred this to Phase 3.11/3.12 (decision-log entry 2026-05-12). The sandbox routing utility (`sendBrandedEmail()` with `RESEND_DOMAIN_VERIFIED` switch) is in place; the flip is single-env-var.
- **Anthropic billing/security alert routing risk** — the Anthropic API account uses a pre-existing email on a less-used inbox (off-spec from dinovlazar2011@gmail.com per user choice). Neither email-change nor forwarding was set up. If the API ever stops working, first check that inbox for $20-cap or security alerts.

---

## Stack — pinned versions

| Package | Version |
|---|---|
| next | 16.2.4 |
| react | 19.2.4 |
| react-dom | 19.2.4 |
| marked | 18.0.3 |
| typescript | 5.9.3 |
| tailwindcss | 4.2.4 |
| @tailwindcss/postcss | 4.2.4 |
| next-intl | 4.11.0 |
| motion | 12.38.0 |
| lucide-react | 1.14.0 |
| @base-ui/react | 1.4.1 |
| next-sanity | 12.4.0 |
| sanity | 5.25.0 |
| @sanity/vision | 5.25.0 |
| @sanity/image-url | 2.1.1 |
| @portabletext/react | 4.0.3 |
| @portabletext/block-tools | 5.1.1 |
| jsdom | 25.0.1 |
| tsx (devDep) | 4.21.0 |
| styled-components | 6.4.1 |
| @anthropic-ai/sdk | 0.92.0 |
| resend | 6.12.2 |
| zod | 3.25.76 |
| eslint | 9.39.4 |
| eslint-config-next | 16.2.4 |
| @types/react | 19.2.14 |
| @types/react-dom | 19.2.3 |
| @types/node | 20.19.39 |
| Node.js | v24.14.0 |
| npm | 11.9.0 |

Fonts (loaded via `next/font/google`): Manrope (heading) + Onest (body), subsets `latin` and `latin-ext`.

`sharp` is available transitively via `next` and is used by both placeholder generators (`scripts/gen-home-placeholders.mjs`, `scripts/gen-audience-service-placeholders.mjs`) — not a runtime dep.

---

## Repo

- **Remote (SSH):** `git@github.com:DinovLazar/sunsetservices.git`
- **Default branch:** `main`
- **Visibility:** Private
- **Phase 1.07 commit:** `b66b61c` — homepage + seven sections, Lighthouse 95+ desktop / 86 mobile.
- **Phase 1.09 commit:** `3ea2e1c` — `feat(audience+service): 3 audience landings + 16 service detail pages (Phase 1.09)`
- **Phase 1.10 commit:** `d538d62` — `fix(part-1-phase-10): commercial snow-removal slug + EN aria-label bleed + lint`
- **Phase 1.11 commit:** `f3e4995` — `docs(design): Phase 1.11 about + contact design handover` (handover authored out-of-band, brought into git as Phase 1.11 closure before Phase 1.12).
- **Phase 1.12 commit:** `9c4976a` — `feat(about-contact): about + contact pages, person + contact-page schema (Phase 1.12)`
- **Phase 1.14 commit:** `9279efd` — `feat(service-areas): index + 6 location pages (Phase 1.14)`
- **Phase 1.16 commit:** `3b25238` — `feat(projects): portfolio index + 12 detail pages, lightbox, schema (Phase 1.16)`
- **Phase 1.18 commit:** `254c31c` — `feat(content): Resources + Blog routes, ContentCard/Meta/ProseLayout, schema, OG images (Phase 1.18)`
- **Phase 1.20 commit:** `5dee0b1` — `feat(wizard,chat): quote wizard + AI chat widget UI; Part 1 acceptance pass (Phase 1.20)`
- **Phase 2.01 cleanup commit:** `c6a962c` — `chore(pre-2.01): restore Phase 1.20 baseline + commit untracked Part-1 artifacts` (restored 12 code regressions, committed 4 design handovers + 8 Lighthouse reports, kept in-progress v2 doc rewrites, added /.claude/ to gitignore)
- **Phase 2.01 archive commit:** `f97efca` — `chore(docs): archive v1 docs into /archive/v1/ (Phase 2.01)`
- **Phase 2.01 finalization commit:** `e4b323e` — `chore(phase-2.01): close Phase 2.01 (Account-Creation Runway)` (6 files, +361 / -10; created `Sunset-Services-Decisions.md` + `Part-2-Phase-01-Completion.md`, updated 00_stack-and-config / current-state / file-map / .env.local.example)
- **Phase 2.02 analytics commit:** `ddeed03` — `feat(analytics): add @vercel/analytics to root layout (Phase 2.02)` (3 files, +46; package.json + package-lock.json + `src/app/[locale]/layout.tsx`)
- **Phase 2.02 finalization commit:** `5345b8b` — `chore(phase-2-02): vercel preview deploy completion report + project-state updates` (5 files, +310 / -4; created `Part-2-Phase-02-Completion.md`, updated 00_stack-and-config / current-state / file-map / .env.local.example)
- **Phase 2.03 implementation commit:** `858d829` — `feat(sanity): standalone Studio + 8 schemas + client + image builder (Phase 2.03)` (22 files, +2091 / -1502; +sanity@^5.25.0 / @sanity/vision@^5.25.0 / @sanity/image-url@^2.1.1 / styled-components@^6.4.1, +3 studio scripts, new `sanity.config.ts` / `sanity.cli.ts` at repo root, new `sanity/schemas/**` (12 files) + `sanity/lib/**` (2 files), .gitignore +/dist/ +/.sanity/, .env.local.example +3 NEXT_PUBLIC_SANITY_* lines).
- **Phase 2.03 finalization commit:** `4d5c908` — `chore(phase-2-03): sanity CMS completion report + project-state updates` (4 files, +317 / -9; created `Part-2-Phase-03-Completion.md`, updated 00_stack-and-config / current-state / file-map).
- **Phase 2.03 final SHA-record commit:** `056c348` — `docs(phase-2.03): record final commit SHA (4d5c908) in current-state + completion report`.
- **Phase 2.05 commits** (14, merged to `main` via PR #1 rebase on 2026-05-12):
  - `f163fcc` — `chore(decisions): log Phase 2.05 scope decision`
  - `8cd1cf1` — `feat(deps): add @portabletext/react + @portabletext/block-tools + jsdom + tsx for Phase 2.05`
  - `4622896` — `chore(env): document SANITY_API_WRITE_TOKEN`
  - `02f181e` — `feat(sanity-schemas): +review.placeholder, +resourceArticle.featuredImage/crossLink, +blogPost.crossLink (Phase 2.05)`
  - `6d717b3` — `feat(scripts): seed-sanity.mjs migration script for Phase 2.05`
  - `86cdb97` — `fix(seed): audience-prefixed service _ids + scope tags + env-local priority`
  - `1b915d2` — `feat(sanity): GROQ query helpers + TypeScript return types`
  - `c72a418` — `feat(projects): wire /projects + /projects/[slug] to Sanity reads (Phase 2.05)`
  - `755b0ee` — `feat(blog): wire /blog + /blog/[slug] to Sanity with PortableText rendering (Phase 2.05)`
  - `fc6ca3c` — `feat(resources): wire /resources + /resources/[slug] to Sanity (Phase 2.05)`
  - `bf58d51` — `feat(faq): service + location pages read FAQs from Sanity by scope tag (Phase 2.05)`
  - `3960bc7` — `refactor(data): remove inline FAQ arrays from services + locations (Sanity is now authoritative)`
  - `7c82045` — `chore(phase-2-05): project-state updates`
  - **`2cbcb0e`** — `chore(phase-2-05): completion report` (Phase 2.05 final SHA on main)
- **Phase 2.06 commits** (13, on `claude/priceless-northcutt-4516d5`; harness-provisioned branch — the prompt suggested `claude/phase-2-06-quote-wizard-wiring` but the harness picked a different, functionally equivalent name):
  - `9bdbb3a` — `chore(decisions): log Phase 2.06 decisions`
  - `79f4d17` — `feat(deps): add zod@^3.23 for /api/quote server-side validation (Phase 2.06)`
  - `5bc5f91` — `feat(sanity-schemas): +quoteLead, +quoteLeadPartial (Phase 2.06)`
  - `97efbe0` — `feat(sanity): write client for /api/quote server route (Phase 2.06)`
  - `61ef0c3` — `feat(quote): zod validation schemas for /api/quote payloads (Phase 2.06)`
  - `abcd19b` — `feat(quote): Mautic stub (no-op until MAUTIC_ENABLED=true) (Phase 2.06)`
  - `60c30b5` — `feat(quote): Resend lead-alert email module (Phase 2.06)`
  - `615198f` — `feat(api): /api/quote route — Sanity write + Resend email + Mautic stub (Phase 2.06)`
  - `d053b1c` — `feat(api): /api/quote/partial route — abandoner capture in Sanity (Phase 2.06)`
  - `987012c` — `feat(wizard): session ID generation for lead/partial linkage (Phase 2.06)`
  - `853eab1` — `feat(wizard): wire Step 5 Submit to POST /api/quote + honeypot field (Phase 2.06)`
  - `ae58a8f` — `feat(wizard): fire-and-forget partial push on Steps 1-3 transitions (Phase 2.06)`
  - `5ad8e97` — `chore(env): document Phase 2.06 quote-wizard backend variables`
  - `350d417` — `fix(quote): honeypot before zod + drop empty-string defaults (Phase 2.06)`
- **Phase 2.07 commits** (9, on branch `claude/flamboyant-einstein-069be0` rebased onto Phase 2.06's HEAD on `main` mid-phase when Phase 2.06 landed, then merged):
  - `06f867a` — `chore(decisions): log Phase 2.07 decisions`
  - `85946ff` — `chore(env): document Phase 2.07 Calendly variables`
  - `64fd02a` — `feat(calendly): CalendlyEmbed component + consent gate + lazy load (Phase 2.07)`
  - `b2229a8` — `chore(i18n): Phase 2.07 Calendly strings`
  - `aba209a` — `feat(contact): swap Calendly placeholder for real embed (Phase 2.07)`
  - `9960747` — `feat(thank-you): swap Calendly placeholder for real embed (Phase 2.07)`
  - `6bec87b` — `fix(calendly): role=region on widget div (a11y) (Phase 2.07)`
  - `0bcf355` — `chore(phase-2-07): project-state updates`
  - `b39dcd2` — `chore(phase-2-07): completion report` (note: post-rebase SHA on `origin/main` is `daaea0a` with `4f1344b` as the post-rebase docs-update tip — same content)
- **Phase 2.08 commits** (15, on branch `claude/modest-fermat-515f93` from `origin/main` at `4f1344b`):
  - `bf10869` — `chore(decisions): log Phase 2.08 Resend domain deferral + sandbox routing pattern`
  - `6076c42` — `chore(env): document Phase 2.08 email + contact + newsletter variables`
  - `2dd6552` — `feat(deps): add @react-email/components + @react-email/render for Phase 2.08`
  - `67ee0e4` — `feat(sanity-schemas): +contactSubmission, +newsletterSubscriber (Phase 2.08)`
  - `9141e05` — `feat(email): EmailLayout primitive + sandbox banner + token module (Phase 2.08)`
  - `346683e` — `feat(email): QuoteLeadAlertEmail template (Phase 2.08)`
  - `8470a8f` — `feat(email): QuoteConfirmationEmail template (Phase 2.08)`
  - `c58de6b` — `feat(email): ContactAlertEmail template (Phase 2.08)`
  - `2ce00fc` — `feat(email): ContactConfirmationEmail template (Phase 2.08)`
  - `f762397` — `feat(email): NewsletterWelcomeEmail template (Phase 2.08)`
  - `dbcaf83` — `feat(email): sendBrandedEmail util with sandbox-aware routing (Phase 2.08)`
  - `8cdb923` — `refactor(api/quote): branded lead alert + visitor confirmation via sendBrandedEmail (Phase 2.08)`
  - `55638c9` — `feat(api/contact): contact form backend route + branded emails (Phase 2.08)`
  - `c62b38f` — `feat(contact): wire ContactForm submit to POST /api/contact (Phase 2.08)`
  - `4f43caf` — `feat(api/newsletter): newsletter signup backend (Phase 2.08)`
  - `de66e8e` — `feat(newsletter): footer signup component + Footer integration (Phase 2.08)`
  - `010f222` — `chore(i18n): Phase 2.08 newsletter strings + EN copy refresh`
- **Phase 2.09 commits** (on branch `claude/reverent-lichterman-9bb590` from `origin/main` at `f7eecee`):
  - `44f7fa0` — `chore(decisions): log Phase 2.09 rate-limiter + knowledge-base decisions`
  - `583ac43` — `chore(env): document Phase 2.09 AI chat backend variables`
  - `f019c82` — `feat(sanity-schemas): +chatLead (Phase 2.09)`
  - `32056c4` — `feat(chat): knowledge-base digest builder grounded in Sanity (Phase 2.09)`
  - `23a6d9d` — `feat(chat): system prompt builder + persona + locale + tool def (Phase 2.09)`
  - `2779fba` — `feat(chat): in-memory rate limiter + IP extraction (Phase 2.09)`
  - `6a308a0` — `feat(api/chat): Anthropic SSE streaming + flag_high_intent tool (Phase 2.09)`
  - `edf760b` — `feat(chat): wire panel to real SSE backend + high-intent banner (Phase 2.09)`
  - `c58513f` — `feat(email): ChatLeadEmail template (Phase 2.09) + feat(api/chat/lead): chat lead capture + branded email + Mautic stub (Phase 2.09)`
  - `87ba3f6` — `feat(chat): wire inline lead-capture form to /api/chat/lead (Phase 2.09)`
  - `e9892da` — `chore(i18n): Phase 2.09 chat strings`
- **Phase 2.10 commits** (on branch `claude/thirsty-kowalevski-1b2357` from `origin/main` at `0606124`; PR #5):
  - `36c6b82` — `chore(phase-2-10): copy Cowork Part A handover into repo`
  - `d54ab49` — `chore(decisions): log Phase 2.10 analytics stack scope`
  - `c2ded63` — `chore(env): Phase 2.10 analytics env vars`
  - `a07ec00` — `feat(analytics): event name constants + PII filter (Phase 2.10)`
  - `68df427` — `feat(analytics): consent state helpers (Phase 2.10)`
  - `b8b51ac` — `feat(analytics): safe dataLayer.push with consent + PII gates (Phase 2.10)`
  - `87aaabc` — `feat(hooks): useConsent (Phase 2.10)`
  - `8286e2c` — `feat(analytics): ConsentBanner component (Phase 2.10)`
  - `3b69f90` — `feat(analytics): GTMScript client component (Phase 2.10)`
  - `494c763` — `feat(analytics): GTMNoScript server component (Phase 2.10)`
  - `440fa5f` — `feat(analytics): ClarityScript client component (Phase 2.10)`
  - `9623a9d` — `feat(analytics): AnalyticsBridge — CustomEvents → dataLayer (Phase 2.10)`
  - `8cef1f5` — `feat(calendly): wire Calendly postMessage events to dataLayer (Phase 2.10)`
  - `a8ca92b` — `feat(app): mount analytics + consent in root layout (Phase 2.10)`
  - `9da0f59` — `chore(i18n): Phase 2.10 consent banner strings`
  - `c6f38bf` — `feat(styles): --z-consent CSS variable for Phase 2.10 banner`
  - `9150c4b` — `refactor(analytics): align dispatcher event names with Phase 2.10 GTM spec`
  - `d31d5ff` — `fix(wizard): WizardStickyNav data-analytics-event uses STEP_ADVANCED`
- **Phase 2.13 commits** (on branch `claude/magical-nash-ce2238` from `main` at `694a400` — Phase 2.11 final SHA):
  - `1ece4c1` — `chore(decisions): log Phase 2.12 deferral + Phase 2.13 plan-of-record` (Step 1; ships BEFORE any code work)
  - `3273df7` — `chore(env): document Phase 2.13 ServiceM8 webhook variables`
  - `0a04014` — `feat(sanity-schemas): +servicem8Event (Phase 2.13)`
  - `8952d22` — `feat(servicem8): zod validation schema (Phase 2.13)`
  - `358b359` — `feat(servicem8): HMAC-SHA256 signature verification (Phase 2.13)`
  - `ee575b7` — `feat(servicem8): Sanity persist helper with deterministic-ID dedup (Phase 2.13)`
  - `4d6153c` — `feat(api/webhooks/servicem8): webhook route — flag-gated, signature-verified, idempotent (Phase 2.13)`
  - `13c1f75` — `chore(decisions): log Phase 2.13 Zod root .passthrough() drop + fix schema` (in-phase off-spec decision)
  - `99bae7e` — `test(servicem8): synthetic webhook verification script (Phase 2.13)`

- **Phase 2.15 commits** (on branch `claude/gallant-mccarthy-d5a783` from `main` at `0241a63` — Phase 2.13 merge SHA):
  - `d5bd8c1` — `chore(decisions): log Phase 2.14 deferral + Phase 2.15 plan-of-record` (Step 1; ships BEFORE any code work)
  - `beded32` — `chore(env): document Phase 2.15 Telegram webhook variables`
  - `381411c` — `feat(sanity-schemas): +telegramMessageId on servicem8Event (Phase 2.15)`
  - `2694b31` — `feat(sanity-schemas): +telegramApprovalLog (Phase 2.15)`
  - `52c1abf` — `feat(telegram): TypeScript types for Telegram Bot API objects (Phase 2.15)`
  - `6adc47f` — `feat(telegram): low-level Bot API client with flag-gated simulation (Phase 2.15)`
  - `e2654f0` — `feat(telegram): approval kind discriminator + callback data codec (Phase 2.15)`
  - `a6cd8a1` — `feat(telegram): Sanity persist helper for approval logs (Phase 2.15)`
  - `46b3a05` — `feat(telegram): notifyOperator + requestApproval high-level helpers (Phase 2.15)`
  - `429b4a4` — `feat(api/webhooks/telegram): inbound callback receiver — flag-gated, secret-verified, idempotent (Phase 2.15)`
  - `9a31159` — `feat(scripts): setup + info scripts for Telegram webhook registration (Phase 2.15)`
  - `d581112` — `test(telegram): synthetic verification harness (Phase 2.15)`
- **Phase 2.16 commits** (on branch `claude/angry-ellis-65e94d` from `main` at `fe48b45` — Phase 2.15 merge SHA):
  - `e72bec9` — `chore(decisions): log Phase 2.16 plan-of-record` (Step 1; ships BEFORE any code work)
  - `283d845` — `chore(env): document Phase 2.16 cron + automation variables`
  - `5c6e401` — `feat(sanity-schemas): +blogDraftPending (Phase 2.16)`
  - `f57bdcb` — `feat(telegram): ship 'blog_draft' approval button set (Phase 2.16)`
  - `7626fad` — `feat(data): +blogTopics curated rotation list (Phase 2.16)`
  - `fb4b693` — `feat(blog-automation): topic picker with rotation (Phase 2.16)`
  - `9aed60d` — `feat(blog-automation): Anthropic draft generator (Phase 2.16)`
  - `6f9d0ff` — `feat(blog-automation): Sanity persist helper for pending drafts (Phase 2.16)`
  - `96dbfad` — `feat(api/cron): monthly blog draft route (Phase 2.16)`
  - `25c106f` — `feat(blog-automation): default placeholder featured-image asset (Phase 2.16)`
  - `4523806` — `feat(blog-automation): publish + reject handlers (Phase 2.16)`
  - `24465b9` — `feat(api/webhooks/telegram): wire 'blog_draft' decision branch (Phase 2.16)`
  - `53f2552` — `feat(api/cron): weekly SEO summary route, flag-gated (Phase 2.16)`
  - `0e63268` — `feat(vercel): cron schedules — monthly blog + weekly SEO (Phase 2.16)`
  - `dde6e18` — `refactor(blog-automation): extract shared executor; switch to time-based idempotency`
  - `ca6f3f7` — `chore(decisions): log Phase 2.16 idempotency design shift`
  - `523a623` — `test(blog-automation): synthetic verification harness (Phase 2.16)`
- **Phase 2.17 commits** (on branch `claude/bold-mclaren-4009dc` from `main` at `6fe6326` — Phase 2.16 merge SHA):
  - `381a6b1` — `chore(decisions): log Phase 2.17 plan-of-record` (Step 1; ships BEFORE any code work)
  - `f7a2837` — `chore(env): document Phase 2.17 portfolio pipeline variables`
  - `5821c5f` — `feat(sanity-schemas): +portfolioDraftPending; project.automated* (Phase 2.17)`
  - `49f143e` — `feat(portfolio-automation): job metadata extractor with defensive fallbacks (Phase 2.17)`
  - `876660b` — `feat(portfolio-automation): photo download + Sanity asset upload helper (Phase 2.17)`
  - `0f24f9d` — `feat(portfolio-automation): Anthropic draft generator (Phase 2.17)`
  - `02bbca5` — `refactor(automation): extract toPortableTextBlocks to shared module (Phase 2.17)`
  - `f53a7e0` — `feat(portfolio-automation): Sanity persist helper for portfolio drafts (Phase 2.17)`
  - `223a261` — `feat(portfolio-automation): pipeline orchestrator (Phase 2.17)`
  - `f1a430e` — `feat(api/webhooks/servicem8): wire post-response portfolio pipeline trigger (Phase 2.17)`
  - `7d03454` — `feat(portfolio-automation): GBP write stubs deferred to Phase 2.17a (Phase 2.17)`
  - `31f8f14` — `feat(portfolio-automation): publish + reject handlers (Phase 2.17)`
  - `71f50b1` — `fix(portfolio-automation): narrow createOrReplace input type (Phase 2.17)`
  - `92ac261` — `feat(api/webhooks/telegram): wire 'servicem8_portfolio' decision branch (Phase 2.17)`
  - `fb45b6d` — `feat(api/test): portfolio pipeline + decision routes (Phase 2.17)`
  - `a6701e6` — `fix(portfolio-automation): robust JSON extraction + taxonomy backfill (Phase 2.17)`
  - `5be835d` — `test(portfolio-automation): synthetic verification harness (Phase 2.17)`
  - `9cd9963` — `chore(decisions): log Phase 2.17 in-phase off-spec additions`

---

## Open items / known mismatches with the Plan

> **Phase M.11 (2026-05-31, on `worktree-phase+m11-qa-sweep`) closed several of these and opened a few new flag-and-log items.** Closed: the `/projects/aurora-driveway-apron` 404 drift (reconciled out of both harness configs — `validate:seo`/`validate:a11y` now fully green) + a newly-found 6-slug sitemap drift (added to the harness); the documented orphan i18n keys (`audience.*`, `home.audience.*`, `home.projects.{tag,tile,alt}`, footer `placeholderNote`, `servicePage.related.h2.{residential,commercial}`); the `?audience=` projects deep-link; the wizard Step-3 propertyType ordering bug; assorted a11y/schema/consistency defects. **New flag-and-log handoffs** (full list in `Phase-M-11-Completion.md` §5): 🔴 **rotate + referrer-restrict the GCP Places API key** (redacted on-branch but mandatory); no server-side rate-limiting on the 5 public lead routes; the Sanity `service`/`blogPost`/`resourceArticle` schemas still expose the retired `audience` taxonomy (needs a coordinated schema + data migration); the OG-image route reads the retired static TS seed; and two deferred-integration test harnesses fail on environment quirks (`test-telegram-bot` flag-off, `test-blog-automation` topic-pool exhaustion) — neither an M.11 regression.

- **`@vercel/analytics@^2.0.1` added to root layout** (Phase 2.02). `<Analytics />` mounted as a sibling of `<NextIntlClientProvider>` inside `<body>` of `src/app/[locale]/layout.tsx`. v2.x injects the analytics script client-side via a JS chunk that requests `/_vercel/insights/script.js` on hydration — unlike v1.x's static `<script src="/_vercel/insights/script.js">` tag the Phase 2.02 plan's view-source check assumed. Verified by inspecting the bundled chunk and confirming Vercel serves `/_vercel/insights/script.js` (HTTP 200, ~2.5 KB).
- **`.vercel/` directory present locally and gitignored** (Phase 2.02). Contains `project.json` (project ID `prj_OZ7kKRwIgpqoJGlWD7YguA7qYKbX` + org ID `team_rRKMRUuOrwJk08a4BkSgNYAe`), `README.txt`, plus a `poll-deploy.js` helper + `events.json` diagnostic dump from Phase 2.02 debugging. Re-run `vercel link` to restore if deleted; project + org IDs also visible in the Vercel dashboard.
- **First Vercel deployment 404'd because `framework: null` on the project record** (Phase 2.02). `vercel project add` doesn't auto-detect framework; only `vercel link` (when the linked dir's `package.json` has `next`) sets `framework: nextjs`. Workaround: `PATCH /v9/projects/{id}` with `{"framework":"nextjs"}`, then `vercel deploy --prod --yes`. Future `vercel project add` calls (none expected) should be followed by an explicit framework PATCH or manual link.
- **Vercel CLI plugin agent-mode breaks `vercel env add NAME preview` without an explicit git-branch positional.** Phase 2.02 worked around this by POSTing directly to `/v10/projects/{id}/env` with `target: ['production', 'preview']` in one call. CLI source `commands/env/index.js:948` enforces explicit branch selection in non-interactive mode regardless of `--yes`. See Phase 2.02 report for the full PowerShell helper. **Phase 2.03 reused the same helper** to add the 3 NEXT_PUBLIC_SANITY_* vars; helper script lives at `../../../.vercel/project.json` in worktree contexts and the auth token at `%APPDATA%\xdg.data\com.vercel.cli\auth.json` (auto-refreshed by the CLI — the on-disk token expired between 2.02 and 2.03 and was refreshed transparently by running `npx vercel whoami` once).
- **Phase 2.03 — `styled-components` is a required Sanity Studio peer dep** despite the plan's note that "recent `sanity` versions bundle styling internally." Phase 2.03 hit `Failed to start dev server: Declared dependency 'styled-components' is not installed` on first `npm run studio:dev` and installed `styled-components@^6.4.1` as a direct dep. The plan anticipated this but expected the error from `npm install`, not `sanity dev`. Net: 1 extra package committed; no behavioral change.
- **Phase 2.03 — `sanity deploy` printed an `appId` hint after first deploy** (`hza6xflhrkuygkrhketq6uhj`). Added to `sanity.cli.ts` under `deployment.appId` to make every subsequent deploy non-interactive (first deploy already succeeded non-interactively because `studioHost` was pre-set, but the CLI prints the appId hint on every fresh project's first deploy). Off-spec; documented in Phase 2.03 report.
- **Phase 2.03 — schema field set is deliberately reduced from the TS shapes** in `src/data/*.ts`. The Sanity schemas mirror the *editorial* fields (title, dek, intro, hero image, FAQs, SEO, taxonomy) but omit per-service structural fields (whatsIncluded items, process steps, whyUs cards) that currently live in `src/data/services.ts`. Phase 2.05 will reconcile by either (a) extending the Sanity schemas with the structural arrays before wiring reads, or (b) keeping structural fields in TS code and only reading editorial fields from Sanity. Decision deferred to Phase 2.05.
- **Phase 2.03 — `dist/` and `.sanity/` added to `.gitignore`.** `sanity build` writes a ~7.9MB `dist/` at repo root; `sanity dev` writes a `.sanity/runtime/` cache. Both gitignored at repo root.
- **Phase 2.03 — `git check-ignore` reports a false-positive match for any directory path** because of CRLF line endings in `.gitignore`. The pattern `dist/` correctly went under the new "sanity studio" section as `/dist/` (anchored to repo root) — but the false-positive `check-ignore` output showed `dist/` matching at `.gitignore:44`, which is actually a blank line. The blank line is interpreted by git's ignore parser as a "match any directory" pattern when CRLF is present. Cosmetic only — `git status` (authoritative) correctly shows ignored vs untracked files.



- **Lighthouse mobile Performance gap (Phase 1.07).** Homepage = 86, audience-landing + service-detail = 84–86. Verified in Phase 1.10 §8: LCP 4.1s on every full-bleed-hero page is the structural ceiling. Three out-of-scope paths to ≥95 noted in 1.07/1.10 reports: AnimateIn → CSS-IO refactor, real photographs (Phase 2.04), `next/dynamic` of below-hero sections.
- **NavbarScrollState was modified in Phase 1.09.** The handover §2.6 specified the page mutate `data-over-hero="true"` on `<main>`; we instead extended NavbarScrollState's pathname-based detection to recognize `/{audience}/` and `/{audience}/{service}/` routes. Five-line addition. Same observable behavior; no page-side coupling.
- **Two services share a URL slug (Phase 1.10).** Both `/residential/snow-removal/` and `/commercial/snow-removal/` exist. Lookup is audience-aware via `getService(slug, audience?)`; assets are disambiguated via the new optional `imageKey` field on the `Service` type. The commercial row uses `imageKey: 'commercial-snow-removal'` so existing placeholder assets resolve. Phase 2.04 photo swap targets the same paths Phase 1.09 set; no rename needed.
- **Phase 1.10 modified 2 routes + 2 components** beyond the data-only scope the prompt suggested. Each is the minimum-blast-radius fix to a Phase 1.09 carryover bug the §5 verification suite catches. Surfaced in §11 of Phase 1.10's report for Chat ratification.
- **Class-naming convention.** Phase 1.07's choice to use single-hyphen BEM (`.btn-amber`, `.card-photo`, `.btn-ghost.btn-on-dark`) is carried throughout Phase 1.09 components even though the Phase 1.08 handover writes some examples as double-hyphen. Phase 1.03 tokens win per the handover preamble.
- **`pricing.mode === 'price'` is dead code in Part 1.** All 16 services ship with `pricing.mode: 'explainer'` per D5. State A code paths are wired (including the `priceIncludes` body Erick will populate when he toggles a service in Part 2) but unexercised in Phase 1.09's smoke testing. Surface alternation invariance verified by code inspection: State A and State B both render at the same vertical footprint inside the same `<section>`.
- **Lucide icon coverage.** `src/components/ui/ServiceIcon.tsx` curates a known-safe icon map and falls back to `BadgeCheck` for any unrecognized name. The Unilock badge is hand-rolled inline SVG (Phase 1.03 §8.3). Trades a little bundle-size discipline for crash-proof rendering when an icon name typo lands in `services.ts`.
- **Real Lighthouse runs require a manual sweep.** Phase 1.09 verified ≥95 readiness via build success + structural smoke tests (HTTP 200s on all 19 EN routes, 6 audience landings, 32 service pages, schema present, no `.card-featured`, single body-amber per page). Lighthouse runs on `localhost:3000/residential/` + `localhost:3000/residential/lawn-care/` + `localhost:3000/hardscape/patios-walkways/` desktop + mobile are recommended next; expect ~95+ desktop with the same mobile-P risk Phase 1.07 documented.
- **Sanity project initialized (Phase 2.01).** Project ID `i3fawnrl`, dataset `production`, organization slug `otkKa3xG9`. No API token created yet (Phase 2.03 creates scoped tokens once schemas are defined).
- **Real env vars partially populated (Phase 2.01).** `.env.local.example` documents all Phase 2.01+ variables. `.env.local` (gitignored) now holds real values for: `RESEND_API_KEY`, `ANTHROPIC_API_KEY`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_OPERATOR_CHAT_ID`. Google Places + GBP variables DEFERRED to Phase 2.13.2.
- **GitHub 2FA ENABLED (Phase 2.01, resolving Phase 1.01 carryover).** Authenticator app paired; 16 recovery codes saved in 3 places (credentials file + email + third location). Verified by log-out / log-in cycle.
- **VS Code NOT installed** (carryover from Phase 1.01, user runs Code via Claude desktop app).
- **23 moderate-severity npm vulnerabilities** reported by `npm install` (all transitive).
- **Featured-card discipline (D9, ratified):** `.card-featured` is forbidden on every audience landing and every service detail page. Verified by smoke test: `document.querySelectorAll('main .card-featured').length === 0` is satisfied on all 19 EN pages (and matched on the ES mirror).
- **Phase 1.14 — `ContactServiceAreaStrip` was promoted to `ServiceAreaStrip`** at `src/components/sections/ServiceAreaStrip.tsx` per Phase 1.13 D7b. Adds an opt-in `excludeSlug?: string` prop used on city pages to hide the current city. The `/contact/` page still consumes it with no `excludeSlug` (default behaviour unchanged). The old `src/components/sections/contact/ContactServiceAreaStrip.tsx` was deleted.
- **Phase 1.14 — new shared `<CTA>` component** at `src/components/sections/CTA.tsx`. Accepts `copyNamespace` + `destination` + opt-in `tokens?: Record<string, string>` (forwarded to next-intl ICU as values for `{key}` placeholders) + `surface` + `ariaId`. The existing `HomeCTA`, `AboutCTA`, `ServiceCTA`, and `AudienceCTA` components are intentionally left untouched per the prompt's "existing call-sites unaffected" rule; the new shared CTA powers the two new Phase 1.14 routes (`/service-areas/` index + city pages) only. Future phases can migrate the older CTAs onto it; out-of-scope here.
- **Dev-mode (Turbopack + Tailwind v4) cache quirk (Phase 1.14).** A transient `FORMATTING_ERROR` in the new shared CTA (since fixed) caused Turbopack to render an error page containing React Server Component flight data (`</script><script>self.__next_f.push([1,"--color-sunset-green-300...`). Tailwind v4's content scanner treated the substring as an arbitrary-class generator (`text-[var("])</script>...]`) and emitted a malformed utility class into the generated CSS. The malformed class persists in Turbopack's intermediate cache even after `rm -rf .next` and a fresh server. **`npm run build` (production / non-Turbopack) is unaffected and ships the routes cleanly**; smoke testing was done against `npx next start`. Filed as a Turbopack-side dev-mode quirk; no source-code fix this phase.
- **Phase 1.16 — `ProjectCard` extracted from `HomeProjects.tsx`** to `src/components/ui/ProjectCard.tsx`. Server primitive that receives already-resolved display props (no i18n / data-import inside the card). `HomeProjects.tsx` now consumes it; home and About teasers render unchanged (regression-tested via `npm run build`). `AudienceFeaturedProjects.tsx` and `LocalProjectsStrip.tsx` were intentionally NOT migrated — both have structural differences (StaggerItem wrappers from Phase 1.07/1.09, placeholder caption pattern, no href on local-strip placeholders). Future phases can migrate them; out-of-scope for 1.16.
- **Phase 1.16 — Canonical/hreflang `SITE_ORIGIN` override + trailing-slash divergence (RESOLVED in Phase B.05).** Both items below are closed by B.05's `src/lib/seo/urls.ts` helpers (`SITE_URL`, `canonicalUrl`, `hreflangAlternates`) which every `generateMetadata`, the sitemap, and robots now import. The harness `scripts/validate-seo.mjs` enforces no-trailing-slash + host-match + hreflang reciprocity site-wide on every run. Kept here for audit; previous text follows:
  - Phase 1.16 — Canonical/hreflang URLs used `process.env.NEXT_PUBLIC_SITE_URL || BUSINESS_URL` in the projects routes only. Localhost Lighthouse runs needed this override so the canonical audit's host-match check passed. Earlier service-areas + audience pages emitted absolute production canonicals only — out-of-scope at 1.16. (Now: every route uses the central `SITE_URL` from `@/lib/seo/urls`.)
  - Phase 1.16 — Canonical dropped the trailing slash on the projects routes. Next 16's default `trailingSlash: false` redirects `/projects/` → `/projects`; emitting canonical without the slash matches the served URL. Earlier service-areas + audience metadata still emitted trailing-slash canonicals (pre-existing site-wide divergence); harmonizing was out-of-scope at 1.16. (Now: every canonical and hreflang strips the trailing slash via the central `canonicalUrl(path, locale)` helper.)
- **Phase 1.16 — Slug rename `fire-pits` → `fire-pits-features` in the projects.ts seed.** Handover §11.3 wrote the seed with `fire-pits`; `services.ts` from Phase 1.09 ships the slug as `fire-pits-features` (matching the route URL `/hardscape/fire-pits-features/`). Two affected projects renamed in the seed: `naperville-hilltop-terrace`, `naperville-fire-court`. Build-time assertions in `projects.ts` would have surfaced any drift; verified all 7 distinct service slugs resolve.
- **Phase 1.16 — `HomeProjects` href remap.** The Phase 1.07 placeholder slugs (`naperville-patio`, `wheaton-lawn`, `aurora-hoa`, `glen-ellyn-fire`, `lisle-wall`, `warrenville-garden`) all 404 against the new 12-row seed. Updated to map to the closest seed-slug equivalents: `naperville-hilltop-terrace`, `wheaton-lawn-reset`, `aurora-hoa-curb-refresh`, `naperville-fire-court`, `lisle-retaining-wall`, `batavia-garden-reset`. Home/about visible tile titles still read the older placeholder copy from `home.projects.tile.*`; clicking lands on the real project detail. Erick polishes copy alignment in Part 2.
- **Phase 1.16 — `<CTA>` link prefetch={false}.** The shared CTA's amber `<Link>` now passes `prefetch={false}` because the `/request-quote/` route is 404-by-design until Phase 2.06; without this, Lighthouse logs the RSC prefetch 404 as a Best-Practices error. Production deploys unaffected. Navbar's `/request-quote/` link still prefetches (out-of-scope chrome change).
- **Phase 1.16 — Lighthouse mobile Performance below 95.** `/projects` mobile = 83 (LCP 4.3s); `/projects/<slug>` mobile = 82 (LCP 4.4s). Matches the documented Phase 1.07 structural ceiling — slow-4G simulation + `<AnimateIn>` JS bundle + Next/Image transcoding overhead. Three documented out-of-scope paths to ≥95 (AnimateIn → CSS-IO refactor, real photographs in Phase 2.04, `next/dynamic` of below-hero sections). All other Lighthouse scores ≥95 across the 6 sampled runs (3 routes × 2 devices).

## TODO 1.16 — Real local-projects content (carried forward; Phase 1.16 prompt did not include this)
- `src/components/sections/location/LocalProjectsStrip.tsx` ships 3 placeholder tiles per city in Phase 1.14. The D7.A fallback rule (real city projects; if zero, fall back to closest 3 from neighbor cities AND caption with the actual project city — never fake the city) was NOT wired in Phase 1.16 because the explicit prompt scope was only the projects index + detail templates. With `src/data/projects.ts` now populated, a follow-up phase can wire D7.A — the projects seed contains 2 entries per city for all 6 service-area cities, so the "if zero, fall back" branch never fires for the current seed. Component carries the leading code comment with the rule.

## TODO 2.04 — Cowork
- Real per-city statistics (years serving, projects completed, response time) — currently placeholders in `src/data/locations.ts`:
  - Aurora: 25y / 200+ projects / 5 days
  - Naperville: 25y / 120+ / 5
  - Batavia: 25y / 60+ / 7
  - Wheaton: 25y / 80+ / 5
  - Lisle: 25y / 70+ / 5
  - Bolingbrook: 25y / 65+ / 7
- Real per-city postal codes — currently omitted from `address` in the `Place` JSON-LD (Schema.org accepts the page without `postalCode`).
- Real per-city photography — `src/data/imageMap.ts` aliases each city to an existing audience hero placeholder. The aliases (`LOCATION_HERO`, `LOCATION_CARD`, `LOCATION_PROJECT_TILES`) are documented inline; swapping to real photos requires only changing the static-import sources in `imageMap.ts`.

## TODO 2.07 — Cowork
- Real per-city lat/lng with map-pin precision — currently using representative public-source values in `src/data/locations.ts.geo`. The `Place.geo.GeoCoordinates` JSON-LD accepts the placeholder values; the SVG map pins are positioned via `pin: { x, y }` in seed data (decoupled from `geo`).
- Decide whether to swap the Phase 1.14 static SVG map for a Google Maps iframe. Plan §9 calls Phase 2.07 the optional cutover.

## TODO 2.12 — Native ES review queue (was TODO 2.13)
**Phase 2.11 (Code) closed 2026-05-14.** All `[TBR]`-flagged source-file values now carry real, idiomatic LatAm-MX Spanish (no more English filler) and all in-scope Sanity `.es` fields are populated. Phase 2.12 (Erick + Cowork) strips the `[TBR]` prefix from each surface as approved — the prefix is the only remaining "awaiting review" marker.

**Reading order for Phase 2.12:**
1. `Sunset-Services-TRANSLATION_NOTES.md` "Dialect + tone — locked" — what Code applied.
2. `Sunset-Services-TRANSLATION_NOTES.md` "Glossary" — confirm or override the 60+ canonical term choices.
3. `Sunset-Services-TRANSLATION_NOTES.md` "Native-review priority items" — 7-item queue, start with the chat persona (`src/lib/chat/systemPrompt.ts` `PERSONA_ES`).

**Phase 2.11 [TBR] inventory (where the prefix landed):**
- `src/messages/es.json` — 191 `[TBR]`-prefixed values across `serviceAreas.*`, `location.*`, `projects.*`, `project.*`, `content.*`, `resources.*`, `blog.*`, `chrome.consent.*`, `chrome.footer.newsletter.*`, `contact.calendly.*`, `thanks.calendly.*`, `chat.banner.*`, `chat.leadCapture.*`, `chat.errors.*`.
- `src/data/locations.ts` — 24 strings across 6 cities (whyLocal, meta.description, testimonials preserved).
- `src/data/projects.ts` — 78 strings across 12 projects (title, materials, shortDek, narrative, narrativeHeading, leadAlt, gallery[].alt, beforeAlt, afterAlt).
- `src/lib/email/templates/QuoteConfirmationEmail.tsx`, `ContactConfirmationEmail.tsx`, `NewsletterWelcomeEmail.tsx` — `[TBR]` marker lives in a code comment, not the rendered strings (so recipients don't see `[TBR]` in real emails). All visitor-facing copy is `usted` register.
- `src/lib/chat/systemPrompt.ts` `PERSONA_ES` — `[TBR]` at comment level (never reaches model output). Tú throughout. Highest-stakes single translation; review first.
- `src/lib/chat/knowledgeBase.ts` ES `LocaleLabels` block — `[TBR]` at comment level; glossary-aligned ("hardscape" not "pavimentación").
- `src/app/[locale]/blog/page.tsx` (2), `src/app/[locale]/blog/[slug]/page.tsx` (4), `src/app/[locale]/resources/[slug]/page.tsx` (3) — 9 inline ES TBR strings, position normalized to leading prefix.
- **Sanity:** 12 `project` documents patched via `scripts/translate-sanity-es.mjs` with leading `[TBR]` ES fields (title.es, shortDek.es, narrativeHeading.es, narrative.es, materials[0].es, leadAlt.es, before+afterAlt.es). 23 `faq` documents normalized to leading prefix; the other 24 FAQs already had leading `[TBR]` from seed and 81 FAQs are unflagged (translated in earlier pass, considered done — Phase 2.12 still reads through them).
- **Sanity blog/resource PortableText bodies** were populated from the Phase 2.05 seed (first block carries `[TBR]` prefix on some posts). Block-by-block deep retranslation was out of Phase 2.11 scope per the in-phase TRANSLATION_NOTES decision; Phase 2.12 reads each body for quality + reviews the per-post FAQ blocks separately.

**Phase 2.05 note (still applies):** FAQs are stored in Sanity. Use Studio's text search on `question.es` / `answer.es` for FAQ review.

- `serviceAreas.h1`, `serviceAreas.sub`, `serviceAreas.map.title`, `serviceAreas.map.desc`, `serviceAreas.grid.sub`, `serviceAreas.grid.tagline.{aurora,naperville,batavia,wheaton,lisle,bolingbrook}`, `serviceAreas.outside.body`, `serviceAreas.cta.h2`, `serviceAreas.cta.sub`.
- All `location.microbar.*`, `location.trust.*Label`, `location.services.*`, `location.projects.h2`, `location.projects.placeholderCaption`, `location.testimonials.h2`, `location.whyLocal.h2`, `location.whyLocal.portraitAlt`, `location.faq.h2`, `location.cta.h2`, `location.cta.sub`.
- All `whyLocal.es`, all `testimonials[].quote.es` + `attribution.es`, all `faq[].q.es` + `a.es`, and all `meta.description.es` per city.
- Phase 1.16 — every key in `projects.*` and `project.*` namespaces in `src/messages/es.json` ships with the `[TBR]` flag (handover §7). Per-project ES strings inside `src/data/projects.ts` (`title.es`, `shortDek.es`, `narrativeHeading.es`, `narrative.es`, `materials.es`, `leadAlt.es`, `gallery[].alt.es`, `beforeAlt.es`, `afterAlt.es`) are first-pass placeholders flagged `[TBR]`.
- Phase 2.07 — new Calendly i18n keys in `src/messages/es.json` (flagged `[TBR]`): `contact.calendly.sub`, `contact.calendly.fallbackCta`, `contact.calendly.fallbackLink`, `contact.calendly.iframeLabel`, `thanks.calendly.h2`, `thanks.calendly.sub`, `thanks.calendly.fallbackCta`, `thanks.calendly.fallbackLink`, `thanks.calendly.iframeLabel`. The existing `contact.calendly.h2` ES wording was preserved (not re-flagged).
- Phase 2.08 — new newsletter i18n keys in `src/messages/es.json` (flagged `[TBR]`): `chrome.footer.newsletter.heading`, `chrome.footer.newsletter.helper`, `chrome.footer.newsletter.successMessage`, `chrome.footer.newsletter.alreadySubscribed`, `chrome.footer.newsletter.invalidEmail`, `chrome.footer.newsletter.errorMessage`. The 5 email-template files under `src/lib/email/templates/` carry inline ES copy (lead lines, sign-offs, CTA labels) — every ES literal in those files is flagged `[TBR]` for the same review pass. The original Phase 1.05 `chrome.footer.newsletter.placeholderNote` key is now orphan (component no longer references it); remove in a future i18n cleanup pass.
- Phase 2.09 — new chat i18n keys in `src/messages/es.json` (flagged `[TBR]`): `chat.banner.intro`, `chat.banner.primaryCta`, `chat.banner.secondaryCta`, `chat.banner.closeAriaLabel`, `chat.leadCapture.confirmed`, `chat.leadCapture.error`, `chat.errors.rateLimitedBurst`, `chat.errors.rateLimitedDaily`, `chat.errors.disabled`, `chat.errors.generic`. Inline ES copy lives in `src/lib/chat/systemPrompt.ts` (`PERSONA_ES`) — the hand-authored Spanish persona is flagged `[TBR]` for native review (Phase 2.13). Inline ES copy in `src/lib/chat/knowledgeBase.ts` (`ES` LocaleLabels block — headings, role labels, bio summaries) is also `[TBR]`. The new `ChatLeadEmail` template (`src/lib/email/templates/ChatLeadEmail.tsx`) is EN-only — Erick reads English regardless of lead locale, matching the Phase 2.08 lead-alert template convention.
- Phase 2.10 — new cookie-consent banner i18n keys in `src/messages/es.json` (flagged `[TBR]`): `chrome.consent.heading`, `chrome.consent.body`, `chrome.consent.acceptCta`, `chrome.consent.declineCta`, `chrome.consent.ariaLabel`.

## TODO 2.15 — Real Google reviews
- Replace the placeholder testimonials in `src/data/locations.ts` (`testimonials[]`) with real reviews pulled from the Google Places API. The `LocalTestimonials` component renders `1+` cards per the seed length; no schema change required.
